{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dev-process-toolkit/schemas/layout.schema.json",
  "title": "Layout Marker (Schema R)",
  "description": "specs/.dpt-layout — YAML document recording the current layout version. See technical-spec.md §8.3, AC-47.1.",
  "type": "object",
  "additionalProperties": false,
  "required": ["version", "migrated_at", "migration_commit"],
  "properties": {
    "version": {
      "type": "string",
      "pattern": "^v\\d+$",
      "description": "Layout version; currently v2."
    },
    "migrated_at": {
      "type": "string",
      "format": "date-time",
      "description": "ISO datetime of migration run (or fresh /setup v2 creation)."
    },
    "migration_commit": {
      "oneOf": [
        { "type": "string", "pattern": "^[0-9a-f]{40}$" },
        { "type": "null" }
      ],
      "description": "Full git SHA of the migration commit; null if /setup created a fresh v2 tree."
    }
  }
}
