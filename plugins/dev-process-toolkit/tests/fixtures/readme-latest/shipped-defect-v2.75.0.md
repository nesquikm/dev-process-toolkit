<!-- Recovered verbatim from the shipped defect: the pre-amend release commit (bb8973f, superseded by the amended release commit; the sha is unreachable — this fixture IS the record) line 184.
     v2.75.0's version carrying v2.74.0's codename and the whole of M136's paragraph.
     Do not edit — this is evidence, not a template. -->
See [`CHANGELOG.md`](./CHANGELOG.md) for the full release history. Latest: **v2.75.0 — "Pawl"** (M136, the ratchet that never ran: M132 shipped a skip ratchet whose write side had never executed once — its only order sat behind an optional key this repo does not set and behind a branch creation an already-acceptable branch never performs, while the read side treated the missing number as grounds to refuse, so a clean run became uncertifiable and the baseline got filled in by hand. The baseline is now keyed to the trunk commit and captured in place on a clean trunk checkout, the comparison is over skipped-test identities so a swapped skip cannot hide behind an unchanged count, every refusal hands over a runnable command instead of prose, and the order is proven to fire by executing it rather than by reading it.)
