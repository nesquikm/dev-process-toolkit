---
milestone: M117
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
migration: none
---

# Implementation Plan

## M117 — Smoke-Driver Hygiene {#M117}

**Goal:** Close the ten medium-severity and three low-severity findings from the 2026-07-27 `/conformance-loop` run. None of these block a headless run on their own — they are can't-fail fixtures, falsified documented invariants, stale scaffold and doc text, coverage-accounting gaps, and recurring gate noise. Together they are what makes a healthy run hard to read and a failing run hard to trust.

**Prerequisites:** M116 should land first. STE-429 interacts with STE-419 (fixing the Phase 8 workspace before fixing `/setup`'s first-turn behavior turns a vacuous result into a violation), and STE-425's fixture-disposal work is cleaner once STE-423 has tracker-scoped the fixture paths.

**Release target:** TBD — set at ship time.

**Migration:** `none` — no consumer artifact convention changes. All work is inside the project-local smoke driver, the threat-model documentation, and one gate probe carve-out.

**Provenance:** aggregate report at `/tmp/dpt-conformance-loop-2026-07-27-iter-1.md`; per-tracker findings at `/tmp/dpt-smoke-findings-2026-07-27-{linear,jira}.md`.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-425 | Make smoke fixtures falsifiable and account for unreached coverage | linear:`STE-425` |
| STE-426 | Refresh the driver scaffold snippet and stale documented defaults | linear:`STE-426` |
| STE-427 | Correct the falsified containment and settings-write invariants | linear:`STE-427` |
| STE-428 | Make /report-issue renderable and reliably covered in Phase 8 | linear:`STE-428` |
| STE-429 | Give Phase 8 a scaffold-free workspace per in-scope skill | linear:`STE-429` |
| STE-430 | Widen transient-failure retry and clear recurring gate noise | linear:`STE-430` |

**Tasks:**

- [x] Re-found or retire the nested-spawn negative fixture so it can actually fail under an auto default permission mode — STE-425
  - verify: a mutation check disabling the detector makes the fixture fail; if retired, the pre-flight that refuses over the same pattern is re-justified or dropped
- [x] Render fixture-group outcomes as passed, failed, or not-reached, and never infer a pass from silence — STE-425
  - verify: summary unit cases cover all three; a group that did not execute is named as not-reached
- [x] Document the Phase 8 fixture disposal rule and surface run-created untracked artifacts at end of run — STE-425
  - verify: following the documented step leaves pre-flight #4 passing on a re-run; the closing check names any untracked artifact the run created
- [x] Bring the Phase 1 step 6 scaffold snippet up to the tool-surface union the threat model requires — STE-426
  - verify: scaffolding the snippet verbatim produces a runnable chain; meta-test pins the snippet against the tracked settings file
- [x] Move the snippet to the current entry shape so it stops generating self-inflicted gate advisories, and correct the branch-template default — STE-426
  - verify: a freshly scaffolded test project's gate output carries neither the shape advisory nor the staleness note; the documented template matches what setup emits
- [x] Restate the containment claim to what the guard actually bounds and record the inherited-config write path — STE-427
  - verify: byte assertions over the corrected prose; the rationale names project-root scoping as the reason first-turn assertions need it
- [x] Restate the settings-write invariant to match measured behavior and re-derive the parent-pre-creation rationale — STE-427
  - verify: no argument still cites a no-child-side-merge guarantee; a regression case pins whichever behavior implementation determines is correct
- [x] Emit the canonical refusal marker on the `/report-issue` publish refusal — STE-428
  - verify: the recorded 2026-07-27 transcript scores ok-refused where it previously scored vacuous
- [x] Assert all four Phase 8 fixtures exist before the aggregate verdict, and add a publish-free exercise path — STE-428
  - verify: a three-of-four run names the missing skill instead of reporting an aggregate pass; the dry-run path performs no outward publish
- [x] Run each in-scope Phase 8 skill against a workspace where its Socratic entry is reachable — STE-429
  - verify: every in-scope skill can render a pass under correct behavior; the canonical chain's own test project and captures are untouched
- [x] Report a vacuous outcome as an inconclusive named result — STE-429
  - verify: rendering case asserts the skill is named and the result is not folded into an aggregate pass
- [x] Widen the retry signature to clean-tree connection-drop failures, gated on the tree check rather than the error class — STE-430
  - verify: a clean-tree connection drop retries once; a dirty-tree failure does not auto-retry; the audit row records both attempts
- [x] Permit the heredoc commit form and add the scaffolding carve-out to the reconciliation drift probe — STE-430
  - verify: both commit forms are permitted under the same allow-list entry; a freshly bootstrapped project produces a clean gate with no orphan-milestone row
- [x] Full gate green — M117
  - verify: `bun test` from `plugins/dev-process-toolkit` green with no probe regressions; `/gate-check` clean
