---
milestone: M112
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
migration: none
---

# Implementation Plan

## M112: Non-tty smoke-driver hard gate against leg-stranding {#M112}

**Goal:** Close the two 2026-07-24 `/conformance-loop` manifestations where both nested `claude -p` smoke-test legs exited rc=0 without running the canonical chain and left orphaned tracker data — the Jira leg re-hit F3 fire-and-exit (rationalizing the STE-365 SMOKE-CTX advisory banner as "an interactive parent" while headless), and the Linear leg paused mid-run with a discretionary rate-limit prose question and ended its turn (a silent no-op under non-tty). Convert the advisory SMOKE-CTX posture into a hard, byte-pinned gate, add a marker-routed discretionary-halt guard for mid-run judgment calls, and strengthen the STE-357 final-message self-check into a hard abort-with-teardown so no leg can exit rc=0 with an unfinished chain or orphaned data.

**Prerequisites:** none — the fix sites are the project-local harness skills and are all present on `main`.

**Release target:** TBD at `/ship-milestone` (harness-hardening milestone; ships as a plugin patch/minor per the M95 / M96 / M98 precedent — driver-side, but the repo versions as a whole and carries a CHANGELOG entry).

**Migration:** `none` — project-local harness prose (two driver SKILL bodies) + one doc-conformance meta-test; no consumer-artifact convention changes, so no migration-registry entry.

**FR list** (single FR):

| FR | Title | Tracker |
|----|-------|---------|
| STE-414 | Non-tty smoke-test driver hard gate against leg-stranding (fire-and-exit + discretionary prose-pause) | linear:`STE-414` |

**Tasks:**

- [ ] Write the meta-test `tests/m112-ste-414-nontty-hard-gate.test.ts` (AC-STE-414.1–.6 describe blocks; `repoRoot = join(import.meta.dir, "..","..","..")`; `describe.skip` when a driver SKILL is absent) — RED first
  verify: `cd plugins/dev-process-toolkit && bun test tests/m112-ste-414-nontty-hard-gate.test.ts` — suite runs; AC.1–AC.6 blocks RED (clauses not yet present)
- [ ] smoke-test SKILL: restate the `#### Phase-2-entry context probe — SMOKE-CTX` as a HARD GATE (`[ -t 0 ]`-false binding + forbidden "interactive parent" self-classification) and extend the `#### Phase 2 child-spawn discipline` FORBIDDEN callout to name turn-yield + the interactive-parent rationalization — AC-STE-414.1, AC-STE-414.2
  verify: AC.1 + AC.2 blocks green; `tests/m98-ste-365-fire-and-exit-guard.test.ts` still fully green
- [ ] smoke-test SKILL: add the new `#### Discretionary-halt guard (STE-414)` subsection (marker ⇒ proceed; no-marker ⇒ abort-with-full-teardown; no prose-ask-then-end-turn under non-tty) — AC-STE-414.3
  verify: AC.3 block green
- [ ] smoke-test SKILL: strengthen the `**Final-message self-check (STE-357)**` into a hard abort-with-teardown (incomplete chain OR live pidfile ⇒ loud abort + Phase 5 teardown, never silent rc=0) and route all three abort paths through Phase 5 teardown — AC-STE-414.4, AC-STE-414.5
  verify: AC.4 + AC.5 blocks green
- [ ] conformance-loop SKILL: mirror the hard-gate + discretionary-halt guard + end-of-turn abort-with-teardown into the Phase A / Phase B driver surface (STE-365.4 parity extension) — AC-STE-414.6
  verify: AC.6 block green

**Tests:**
- New doc-conformance meta-test `tests/m112-ste-414-nontty-hard-gate.test.ts` (byte-pins the contract clauses across both driver SKILLs; `describe.skip` on a plugin-only checkout).

**Acceptance Criteria:**
- [ ] every AC in STE-414 (AC-STE-414.1 … .6) passes
- [ ] full `bun test` from `plugins/dev-process-toolkit` green; no existing smoke-test / conformance-loop / upgrade-staleness test regresses; the numbered `/gate-check` probe count is unchanged (no new probe)

**Gate:**

```bash
cd plugins/dev-process-toolkit && bun test
```

## Notes

Origin: 2026-07-24 `/conformance-loop` capture-only run (v2.55.0), both legs stranded. Fix target is the project-local test-driver skills (`.claude/skills/{smoke-test,conformance-loop}/SKILL.md`), not the shipped plugin. The through-line STE-355 → STE-357 → STE-365 → STE-414: each prior FR added a prose guardrail and each was rationalized around at runtime; STE-414 converts the advisory posture to a hard byte-pinned gate and closes the uncovered discretionary-prose-pause escape hatch (the STE-220 → STE-262 advisory-to-byte-checkable escalation, re-applied to the harness).
