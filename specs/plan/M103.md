---
milestone: M103
status: active
archived_at: null
kickoff_branch: feat/ste-380-milestone-next-line
frozen_at: null
shipped_in: null
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M103: Signpost — milestone-bound Next-line recommendation {#M103}

**Goal:** `/spec-write`'s closing `Next:` line recommends `/dev-process-toolkit:implement M<N>` for
milestone-bound FRs instead of unconditionally recommending the single-FR form. The current rule
(STE-181 AC-STE-181.3) keys on new-FR presence, so every real spec session gets the FR-id
recommendation — pointing the operator away from the Phase 5 milestone close that the M-form runs
and the FR-id form silent-skips. New discriminator: **milestone binding** (operator decision,
2026-07-14 `/brainstorm`). Ships with doc-shape probe #66 pinning the rule literals so the
paragraph can't silently regress the way the STE-181 shape ossified.

**Prerequisites:** v2.44.0 shipped (main @ PR #37). Load-bearing existing machinery: STE-181 (the
variant rule being re-keyed, probe #31, the both-shapes template contract), STE-180 (probe #14
advisory — stays as the after-the-fact net), STE-238 (byte-checkable literal-pinning culture),
STE-270 (single-file-scope SKILL.md scan probe shape). No new external dependencies.

**Release target:** v2.45.0 "Signpost". ⚠️ **Contends with M101** (unshipped, on branch
`feat/ste-375-jira-milestone-as-epic`, also targeting v2.45.0). M103 ships first per operator
decision 2026-07-14; M101 rebases onto main and bumps past 2.45.0 at its ship time. `/implement`
Phase 1 + `/ship-milestone` re-validate the target against the live `plugin.json`, so this is a
soft prediction, not a hard claim.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-380 | Milestone-bound `Next:` recommendation — re-key /spec-write's closing-summary rule on milestone binding | linear:`STE-380` |

**Build order:** single FR.

**Tasks:**

_STE-380 — rule re-key + probe:_
- [ ] Re-key the § 7 "Next-line variant rule" paragraph + the tail template's two `Next:` lines/comments in `skills/spec-write/SKILL.md`; delete the retired discriminator sentence — STE-380
  verify: retired literal absent (`grep -c "When the run created a single new FR" skills/spec-write/SKILL.md` → 0); live-tree probe case GREEN
- [ ] Reword `skills/implement/SKILL.md` § Invocation forms lede (drop the "`/spec-write`'s next step" claim for the FR-id form, keep the smoke-driver reference) — STE-380
  verify: `bun test gate-check-implement-invocation-grammar-doc` GREEN
- [ ] Add `adapters/_shared/src/spec_write_next_line_doc.ts` (`runSpecWriteNextLineDocProbe`) — 4 positive literals + 1 negative tripwire, NFR-10 notes, vacuous on absent file — STE-380
  verify: `bun test gate-check-spec-write-next-line-doc` matrix GREEN
- [ ] Register probe #66 in `skills/gate-check/SKILL.md` + bump README probe count 65 → 66 — STE-380
  verify: README-pin meta-test GREEN
- [ ] Sweep calibration snapshots (SKILL.md line caps, adapters STE-token ceiling) as needed — STE-380
  verify: full `bun test` from `plugins/dev-process-toolkit` GREEN

**Tests:**
- `gate-check-spec-write-next-line-doc.test.ts` (4-case matrix + live-tree pass); existing probe #31 test stays GREEN; calibration meta-tests re-pinned

**Acceptance Criteria:**
- [ ] A `/spec-write` run that writes a milestone-bound FR recommends `/implement M<N>` in its closing summary; milestone-less FRs get the FR-id form; cross-cutting-only keeps M<N>
- [ ] The retired new-FR-presence discriminator sentence is gone from `skills/spec-write/SKILL.md`
- [ ] Probe #66 fails the gate if the rule paragraph, template lines, or absence-tripwire regress
- [ ] Probe #31 and the full `bun test` gate stay GREEN

**Gate:** `bun test` (from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN
