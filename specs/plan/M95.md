---
milestone: M95
status: active
archived_at: null
kickoff_branch: chore/m95-smoke-driver-chain-integrity
frozen_at: null
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M95: smoke-driver chain integrity {#M95}

**Goal:** the 2026-07-02 M94-verification conformance run proved the loop now *spawns* reliably but cannot yet *complete* a full canonical chain: the Phase 0.5 cross-tracker mcp-config glob races the concurrent tandem leg (F1), grandchildren die at the 10-minute foreground Bash ceiling or get orphaned when the `-p` child exits mid-stream (F2+F3), and the scaffolded `permissions.allow` is silently inert in untrusted test-project workspaces (F4). Fix all four driver-side findings so a `/conformance-loop` run completes end-to-end, headless, with no silent truncation.

**Prerequisites:** M94 (v2.35.0 "Authorized") — the allow-list fix, `smoke_child_capture.ts` / `stream_json_events.ts` detectors, and Phase 2.X group 8, all of which this milestone builds on. No new external dependencies.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-354 | Scope Phase 0.5 mcp-config cleanup to the resolved tracker | linear:`STE-354` |
| STE-355 | Grandchild spawn lifecycle — deterministic completion + truncation assertion | linear:`STE-355` |
| STE-356 | Pre-seed workspace trust so the scaffolded allow-list is enforcement-effective | linear:`STE-356` |

**Dependency order:** no hard dependencies — STE-354 is an isolated one-line fix; STE-355 and STE-356 are independent mechanisms but both edit `smoke-test/SKILL.md`, so implement sequentially (suggested order STE-354 → STE-355 → STE-356) to avoid working-tree conflicts.

**Tasks:**

- [ ] STE-354 — Scope the Phase 0.5 cleanup to `dpt-smoke-mcp-config-<tracker>.json`, rewrite the STE-186 justification prose, add the isolation sentence to § Operator-driven parallelism. — STE-354
  verify: meta-test asserts the scoped literal present + cross-tracker glob absent + prose agreement.
- [ ] STE-355 — Convert canonical-chain spawns to detached-spawn + pidfile + bounded-poll wrapper; forbid session end with a live pidfile. — STE-355
  verify: meta-test pins the wrapper shape in every Phase 2 spawn fence.
- [ ] STE-355 — Add `assertChainIntegrity` helper (exists + non-empty + `result` event per expected log) wired between Phase 2.X and Phase 3, plus the conformance-loop Phase A leg-completeness mirror. — STE-355
  verify: `bun test` unit — healthy/missing/0-byte/result-absent fixtures; meta-test for both prose sites.
- [ ] STE-356 — Add Phase 1 step 6b trust seeding (jq read-merge-write + backup + atomic mv) and the spawn-time assertion + `workspace_trust_seeded` token. — STE-356
  verify: meta-tests pin the 6b block and the assertion/token; `bun test` green.
- [ ] STE-356 — Add the inert-allowlist runtime detector, amend both threat-model sections, add Phase 5 trust-entry removal. — STE-356
  verify: `bun test` unit — inert-warning fixture ⇒ finding, healthy ⇒ none; meta-tests for threat-model + teardown prose.

**Tests:**
- Meta-tests under `tests/` — Phase 0.5 scoping (STE-354); spawn-wrapper shape + Phase A mirror (STE-355); trust-seeding block + token + threat-model + teardown (STE-356).
- Unit — `assertChainIntegrity` fixtures (STE-355); inert-allowlist detector fixtures (STE-356).
- Runtime (smoke-validated; may ship `[~]` pending a live run) — full canonical chain completes on both legs with zero truncation findings and zero inert-allowlist warnings.

**Acceptance Criteria:**
- [ ] AC-STE-354.1–.2 (scoped cleanup; STE-186 intent preserved).
- [ ] AC-STE-355.1–.3 (poll-until-exit wrapper; chain-integrity assertion; Phase A mirror).
- [ ] AC-STE-356.1–.4 (trust seeding; assertion + token; inert detector + threat-model; teardown removal).

**Gate:** `bun test` GREEN (full gate from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN on this repo.

**Release target:** v2.36.0 (provisional, minor). M92 (`chore/ste-344`) and M93 (`feat/m93`) remain unshipped on sibling branches; the final version resolves at `/ship-milestone` per the release-collision guard.
