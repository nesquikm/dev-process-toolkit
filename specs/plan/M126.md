---
milestone: M126
status: active
archived_at: null
kickoff_branch: fix/m126-shipped-conformance-fixes
frozen_at: null
migration: none
---

# Implementation Plan

## M126 — Shipped-Surface Conformance Fixes {#M126}

**Goal:** Close the seven 2026-08-16 conformance findings that reach real downstream projects. Everything in this milestone lives under `plugins/dev-process-toolkit/`, so this is the only one of the three-milestone program with user-visible change.

**Prerequisites:** none. First milestone of the program; branches from `main` at v2.65.0 "Blueprint" (`f35878d`).

**Release target:** v2.66.0 (provisional; minor — mixed Fixed/Changed, user-visible)

**Migration:** `none` — no consumer artifact changes shape. The § 4b′ subject change alters a commit message the toolkit generates, not a file a consumer stores.

### FRs

| FR | Title | Status |
|---|---|---|
| [STE-477](../frs/STE-477.md) | /implement § 4b′ propagation-commit subject must satisfy the 72-char commit-msg hook | active |
| [STE-478](../frs/STE-478.md) | /spec-write branch-gate-on-main must commit the FR and emit its own capability rows | active |
| [STE-479](../frs/STE-479.md) | /brainstorm must raise the machine-readable refusal when AskUserQuestion is unavailable | active |
| [STE-480](../frs/STE-480.md) | Probe #37 must surface stale prose references, not only directory-tree leaves | active |
| [STE-481](../frs/STE-481.md) | One active plan file per tracker-less project after /setup then /spec-write | active |
| [STE-482](../frs/STE-482.md) | Allow-list: make the /setup merge loud, and grant compound git-commit forms | active |

### Tasks

1. § 4b′ subject stops scaling with the path; body carries it; pre-write length assertion — AC-STE-477.1..3.
   verify: `bun test` green; subject renders ≤ 72 for a ≥ 60-char path fixture; path-embedding mutation goes red.
2. Hook untouched + reference doc + cross-milestone note — AC-STE-477.4..6.
   verify: `templates/git-hooks/commit-msg.sh` byte-identical to HEAD; `docs/implement-reference.md` names the constraint.
3. /spec-write trunk-invocation path commits and reports under its own summary — AC-STE-478.1, .2, .5.
   verify: end-to-end test; `git log <trunk>..HEAD` ≥ 1 and no staged spec files; summary is spec-write's own.
4. Branch-gate + commit capability tokens in assistant text; decline/refusal paths keep their tokens — AC-STE-478.3, .4.
   verify: token assertions assistant-scoped, not raw-log.
5. /brainstorm FIRST ACTION names the marker; non-tty first call is ask-or-refuse — AC-STE-479.1, .2.
   verify: clause byte-matches the four passing skills; no Write/Edit precedes the ask or refusal.
6. Token registration + closing-summary prose + probe with positive AND negative fixtures — AC-STE-479.3, .4.
   verify: probe fails on a marker-stripped fixture and passes on a compliant one.
7. assertFirstTurnShape returns violation not vacuous; meta-test across all FIRST ACTION clauses — AC-STE-479.5, .6.
   verify: unit test on a no-scaffold-write fixture; meta-test enumerates all five skills.
8. Probe #37 reports prose mentions at advisory severity; unmanaged-tree guard intact — AC-STE-480.1, .2, .5.
   verify: prose-only fixture reports exactly one proseMention and zero treeLeaf.
9. Clean the four surviving prose refs in the repo's own cross-cutting specs — AC-STE-480.3, .4.
   verify: `/gate-check` probe #37 clean on this repo; "Delete it then, not before" gone.
10. One active plan file under mode:none; probe #73 exemption narrowed — AC-STE-481.1..5.
    verify: setup-then-spec-write integration test; double-write mutation goes red.
11. /setup allow-list merge emits its count row (both caller-supplied and scaffolded-only paths) — AC-STE-482.1..3.
    verify: merged entry set byte-identical to today's; row fires on the quiet path too.
12. Grant chained and heredoc commit forms in templates/permissions.json — AC-STE-482.4, .5.
    verify: fixture exercising both forms records zero permission denials.

### Dependency graph

All six FRs are independent — six distinct files, no shared surface. Suggested order is severity-first: STE-477 → STE-478 → STE-479 (the three highs) → STE-480 → STE-481 → STE-482.

### Notes

- **Cross-milestone coupling.** STE-477 retires the propagation-commit subject literal that `.claude/skills/smoke-test/SKILL.md` lines 998 and 1006 currently grep for. M127's STE-488 repairs that grep. M126 ships first, so M127's worker sees the new subject already on main.
- **STE-482 disposition.** The allow-list merge is intended behaviour with three independent confirmations (2026-07-20, 2026-07-27, 2026-08-16); the 2026-08-16 jira leg filed it under *What worked* while the linear leg filed it as a finding. The operator decision on 2026-08-17 was to keep the behaviour and add the signal, rejecting both suppression and documentation-only.
- **Prior art applied.** STE-461 supplies STE-477's resolution shape (subject stops scaling, `--no-verify` rejected, pre-write length assertion). STE-401/402 supply STE-479's refusal-contract set (helper + token + prose + probe with both fixtures).
