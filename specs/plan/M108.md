---
milestone: M108
status: active
archived_at: null
kickoff_branch: feat/m108-upgrade-migration-framework
frozen_at: null
migration: none
---

# Implementation Plan

## M108: /upgrade migration framework {#M108}

**Goal:** Ship the registry-driven consumer-artifact migration surface: a new user-invocable `/upgrade` skill walking a version-ordered migration registry (deterministic detectors, script vs assisted kinds, lean rails), seeded with four script entries for accumulated legacy debt, plus the first assisted entry (hybrid, status-aware monolith→per-FR split) and declared+gated ship-time enforcement so future convention changes can never ship without deciding their migration story.

**Prerequisites:** None (M104 shipped the `.dpt` layout + `writeDptGitignore` this milestone reuses).

**Release target:** v2.49.0 (provisional — contends with M101 "Lineage"; whichever ships second re-bumps. STE-393's `MIGRATION_COVERAGE_EPOCH` constant must be corrected at `/ship-milestone` time if the target shifts.)

**FR list** (build order: A → B, A → C; B and C are mutually independent):

| FR | Title | Tracker |
|----|-------|---------|
| STE-391 | /upgrade skill: version-ordered consumer-artifact migration registry + seeded script entries | linear:`STE-391` |
| STE-392 | Assisted migration entry: monolithic specs → per-FR layout (hybrid, status-aware split) | linear:`STE-392` |
| STE-393 | Declared + gated migration coverage: plan-level declaration, /ship-milestone refusal, coherence probe | linear:`STE-393` |

**Tasks:**
- [ ] Registry core — `adapters/_shared/src/migrations/` (`index.ts` types + load-time invariants, `legacy_paths.ts` retired-literal SoT) — STE-391
  verify: registry invariant unit tests green (dup-id + ordering rejection, entry-shape conformance)
- [ ] Seed entries `m104-legacy-state` + `v1-orphans` (detect + apply + fixtures) — STE-391
  verify: per-entry fixture suites green — detect=true on legacy fixture, apply → detect=false, re-apply no-op, real-git `git rm` leg asserted
- [ ] Seed entries `stale-hook-entries` + `permission-shapes` (settings/mcp JSON surgery) — STE-391
  verify: round-trip tests green — stale entries removed, user content byte-identical; never-auto-apply flag pinned
- [ ] Runner `skills/upgrade/SKILL.md` + `/setup` hint row + STE-384 drift allow-list carve-out — STE-391
  verify: SKILL prose greps + `tests/dpt-path-drift.test.ts` green with carve-out tripwire non-vacuous
- [ ] Docs `docs/upgrade-reference.md` + skill-count pins 16 → 17 — STE-391
  verify: README and CLAUDE.md count meta-tests green
- [ ] Split helpers `monolith_split.ts` + assisted registry entry — STE-392
  verify: detector, parser, classifier, and rewriter fixture suites green (split-brain, FR-gap, `[~]` cases)
- [ ] SKILL § Assisted-entries flow (backup → triage → split → freeze) + walkthrough docs — STE-392
  verify: prose greps green (backup-before-mutation ordering, ignored-specs advisory, freeze-everything legality)
- [ ] Coverage helper `coverage.ts` + gate probe #68 `migration_coverage` + Schema T / template `migration:` key + `/ship-milestone` pre-flight — STE-393
  verify: six-path helper unit tests + probe fixture matrix green (each violation class fails, coherent fixture passes)
- [ ] Docs (ship-milestone-reference + upgrade-reference) + probe pins 67 → 68 + this plan's `migration: none` dogfood — STE-393
  verify: full gate `bun test` green from plugins/dev-process-toolkit, no probe regression

**Tests:**
- Per-entry fixture trees (`mkdtempSync`, real git where tracked files move) — the STE-383 AC.5 discipline
- Non-vacuity proofs on every new gate surface (drift carve-out tripwire, coverage-probe mutation check)
- Meta-test calibration: skill count 16 → 17, probe count 67 → 68, SKILL line caps / STE-token ceiling re-pinned as needed

**Acceptance Criteria:**
- [ ] STE-391 ACs 1–9 green (registry, runner, four seed entries, /setup hint, docs+pins, full gate)
- [ ] STE-392 ACs 1–8 green (assisted entry, backup rail, triage, split, freeze, advisory, docs, full gate)
- [ ] STE-393 ACs 1–7 green (declaration key, ship pre-flight, coverage probe, null tolerance, purity, docs+pins, full gate)

**Gate:** `bun test` (from `plugins/dev-process-toolkit`)
