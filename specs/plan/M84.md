---
milestone: M84
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
kind: feature
---

# M84: Public-Surface Drift Cleanup {#M84}

Release target: v2.29.0 (provisional, minor — new probe + doc rewrites)
Codename: TBD (decide at /ship-milestone)

## Summary

Comprehensive cleanup of HIGH + MED drift items surfaced by the 2026-05-20 repo review (`/tmp/dpt-review-findings-2026-05-20.md`, 41 verified survivors / 30 problem areas). M84 closes 17 of 30 problem areas via 9 FRs — the three HIGH-severity user-hostile-drift items (FR1–FR3) shipped first, then six HIGH+MED themed bundles (FR4–FR9) added per the 2026-05-20 brainstorm.

**Tier 1 — user-hostile drift (initial M84 charter, shipped as commit `d3516db`):**

- **STE-315 (A1)** — Add `/gate-check` probe #57 `public_surface_count_drift` and backfill the seven stale-count surfaces (README.md L3 / L10 / L93 / L126 / L150-151 + CLAUDE.md L15-16 + docs/skill-anatomy.md L54 + skills/gate-check/SKILL.md L214 + specs/testing-spec.md L58). Closes the largest documented drift class.
- **STE-316 (A4)** — Rewrite six tracker-mode docs (`ac-sync.md`, `implement-tracker-mode.md`, `spec-review-tracker-mode.md`, `spec-write-tracker-mode.md`, `layout-reference.md`, `setup-tracker-mode.md`) to teach the canonical v2 file-per-FR layout. Current text still teaches the v1 `### FR-N` blocks, which probe #29 hard-rejects.
- **STE-317 (A8)** — Replace obsolete `Bash(<cmd> *)` glob shapes in the four `examples/<stack>/*.md` permission blocks with explicit-subcommand allowlists matching `templates/permissions.json`. Same F3b harness self-modification denial STE-209 closed at the canonical surface.

**Tier 2 — HIGH + MED themed bundles (brainstormed 2026-05-20, six FRs):**

- **STE-318** — Cross-skill contract drift: A2 (TDD 4-stage architecture) + A6 (2-tier resolver phrasing) + A14 (phantom `deps_research_result_shape` probe). Doc/skill prose rewrites + new probe #58 `cross_skill_contract_drift` (durable enforcement per STE-138 pattern).
- **STE-319** — Doc anchor + cap + Schema-tally + attribution: A3 (NFR-1 cap drift, 15 surfaces) + A5 §-anchors (6 dangling `§7.3`/`§9`/`§9.3` citations) + A9 (Schema L/M field tallies) + A17 (`/spec-review` refactor mis-attribution STE-296→STE-308). DD-12.x refs handled by STE-316 (sibling).
- **STE-320** — Code-reviewer + capability registry: A7 (code-reviewer.md L10 disclaimer self-contradicts Pass-1) + A10 (capability-key registry covers 12/18 emitted tokens). Decision per HG95TM AC-23.6: keep Pass-1 spec-compliance, remove the disclaimer; expand the registry to 18 keys + add bidirectional probe.
- **STE-321** — Adapter shape: A11 (`tracker:` in mode-none — code wins, drop from Schema Q + gate-check L26) + A12 (drop `SUPPORTED_TRACKER_KEYS` hardcoded allowlist; cross-check adapter `name:` field). Both decisions resolved in the 2026-05-20 brainstorm.
- **STE-322** — Skill prose hygiene: A13 (`/implement` Phase 1 sub-step labels reorder `0.c→0.e→0.f→0.d` → alphabetic) + A15 (ship-milestone phantom README-structure-count feature — correct the reference doc) + LOW polish A21, A22, A23, A24 bundled for diff coherence.
- **STE-323** — `parseBun` first-match bug: A16 (release audit-trail numbers unreliable). Anchor on Bun `Ran N tests` summary line instead of first per-file pass count.

## In Scope

| FR | Title | Tracker |
|----|-------|---------|
| STE-315 | New /gate-check probe — public-surface count drift | linear:`STE-315` |
| STE-316 | Rewrite tracker-mode docs for v2 file-per-FR layout | linear:`STE-316` |
| STE-317 | Explicit-subcommand example permission blocks | linear:`STE-317` |
| STE-318 | Cross-skill contract drift sweep (TDD 4-stage + 2-tier resolver + phantom probe) | linear:`STE-318` |
| STE-319 | Doc anchor + cap + Schema-tally + attribution cleanup | linear:`STE-319` |
| STE-320 | Code-reviewer scope + capability-key registry expansion | linear:`STE-320` |
| STE-321 | Adapter shape — drop tracker_config allowlist + align mode-none frontmatter | linear:`STE-321` |
| STE-322 | Skill prose hygiene — step labels + phantom ship-milestone feature | linear:`STE-322` |
| STE-323 | parseBun test-count parser — anchor on summary line, not first match | linear:`STE-323` |
| STE-324 | M84 LOW polish bundle — 10 mechanical fixes | linear:`STE-324` |

**Tasks:**

Phase A — Tier 1 user-hostile drift (FR1–FR3, parallel; ALL THREE drafted in commit `d3516db`):

- [x] FR1 (STE-315) — author probe module + sibling test + backfill seven stale-count surfaces + register probe #57 in `skills/gate-check/SKILL.md`
      verify: `bun test plugins/dev-process-toolkit/tests/gate-check-public-surface-count-drift.test.ts` GREEN; `/gate-check` PASS on `main` with probe #57 enabled; `git grep -E "(15 commands|41 probes|51 numbered|22 skills|13 skills)" README.md CLAUDE.md docs/skill-anatomy.md` returns zero hits
- [ ] FR2 (STE-316) — rewrite six tracker-mode docs to teach v2 file-per-FR layout; drop DD-12.x references; fix `docs/layout-reference.md` L62 (mode-conditional `id:`) and L69 (drop v1→v2 rewrite claim)
      verify: `git grep -nE "(### FR-[0-9]+|FR-\\{N\\}|specs/requirements.md.*AC-)" plugins/dev-process-toolkit/docs/{ac-sync,implement-tracker-mode,spec-review-tracker-mode,spec-write-tracker-mode,layout-reference,setup-tracker-mode}.md` returns zero matches; reviewer-prose pass shows each doc reads coherently end-to-end
- [ ] FR3 (STE-317) — rewrite four example permission blocks to explicit-subcommand allowlists matching `templates/permissions.json`; fix bun-typescript.md L52-54 prose-vs-block mismatch; add cross-reference to STE-209 in each rewritten block
      verify: `git grep -nE "Bash\\([a-z]+ \\*\\)" plugins/dev-process-toolkit/examples/` returns zero hits; `bun test plugins/dev-process-toolkit/tests/gate-check-setup-permissions-shape.test.ts` GREEN on a fresh /setup run

Phase B — Tier 2 themed bundles (FR4–FR9, parallel; FR4 introduces probe #58 used as cross-skill drift sentinel):

- [ ] FR4 (STE-318) — rewrite 12 surfaces (TDD architecture + resolver phrasing + deps-researcher probe claim); add probe #58 `cross_skill_contract_drift` + sibling test
      verify: `bun test plugins/dev-process-toolkit/tests/gate-check-cross-skill-contract-drift.test.ts` GREEN; `/gate-check` PASS with probe #58 enabled; `git grep -nE "three forked subagents|3-tier (ticket-binding|resolver)|deps_research_result_shape" plugins/dev-process-toolkit/{skills,docs,agents,README.md}` returns zero hits
- [ ] FR5 (STE-319) — 14 doc/spec edits (NFR-1 cap → 351 in 15 places; §-anchors → §3; Schema tallies; STE-296→STE-308 attribution; orphan `{#schema-J}` removed)
      verify: `git grep -nE "(300|350)[- ](line|line-)?(cap|budget|lines)" plugins/dev-process-toolkit/{docs,specs}` returns zero matches outside `archive/**`; `git grep -nE "technical-spec\\.md §(7\\.3|9|9\\.3)" plugins/dev-process-toolkit/{docs,specs}` returns zero matches outside `archive/**`
- [ ] FR6 (STE-320) — remove L10 disclaimer + 2 doc echoes; expand `closing_summary_capability_keys.ts` registry from 12 to ~18 keys; add bidirectional probe #44 check
      verify: `git grep -nE "Spec-compliance checks are \\*\\*not\\*\\* code-reviewer" plugins/dev-process-toolkit/{docs,agents}` returns zero hits; `bun test plugins/dev-process-toolkit/tests/gate-check-closing-summary-capability-keys.test.ts` GREEN with bidirectional invariant
- [ ] FR7 (STE-321) — drop `SUPPORTED_TRACKER_KEYS`; add `readAdapterName` helper + cross-check validator; rewrite Schema Q + gate-check L26 for mode-conditional `tracker:`; extend probe #13
      verify: `bun test plugins/dev-process-toolkit/tests/{tracker-config,gate-check-identity-mode-conditional}*.test.ts` GREEN; synthetic `gitlab` adapter fixture PASS in `tests/tracker-config-custom-adapter.test.ts`; existing linear+jira workflows unaffected
- [ ] FR8 (STE-322) — `/implement` Phase 1 step rename (`0.d→0.g`); `/ship-milestone-reference.md` phantom-feature claim removed; `/spec-archive` 3→4 renumber; `/ship-milestone:89` "step 8" reference fix; LOW polish (test comment + AC label fixes)
      verify: alphabetic order check on `/implement` substep labels; `awk` on spec-archive numbering; `bun test plugins/dev-process-toolkit/tests/{skill-nfr-1-length,setup-tracker-config-write}.test.ts` GREEN unchanged
- [ ] FR9 (STE-323) — rewrite `parseBun` to anchor on `Ran N tests` summary line; add two new bun-multi-file fixtures
      verify: `bun test plugins/dev-process-toolkit/tests/test-count-parser.test.ts` GREEN; manual verification reports correct count on current `bun test 2>&1` output
- [ ] FR10 (STE-324) — 10 LOW polish edits closing all remaining 2026-05-20 review items: expand branch_proposal ALLOWED_TYPES, bumpYaml per-kind path-shape validator, NON_COMMIT_PRODUCING_SKILLS allowlist, probe #49 canonical-path move, snake-case probe-name docs, /spec-review fork tag, disable-model-invocation symmetry, CLAUDE.md.template Workflows update, specs/notes/archive convention, TDD test comment cleanup
      verify: `bun test plugins/dev-process-toolkit/adapters/_shared/src/branch_proposal.test.ts plugins/dev-process-toolkit/adapters/_shared/src/release_config.test.ts plugins/dev-process-toolkit/tests/gate-check-tracker-local-reconciliation-drift.test.ts` GREEN (path corrected during M84 audit: branch_proposal + release_config tests live at `adapters/_shared/src/`, not `tests/`); `grep -rnE "(bun-zero-match-placeholder|orchestrator \\+ 3 forked)" plugins/dev-process-toolkit/examples/ plugins/dev-process-toolkit/tests/ plugins/dev-process-toolkit/adapters/` returns zero hits

Phase C — Close-out (after all FRs land):

- [ ] Run `/gate-check` → must report GATE PASSED including probe #57 + #58
- [ ] Run `/spec-review M84` → must report all 10 FRs traced to implementation + test
- [ ] Archive M84 + 10 FRs via `/ship-milestone` → bumps to v2.29.0

## Out of Scope (deferred to later milestones)

M84 closes 30/30 problem areas from the 2026-05-20 review with FR10 (STE-324) rolling in the final 10 LOW polish items. No deferred items remain from that source. Future drift will surface via the byte-checkable layer (probe #57 `public_surface_count_drift` + probe #58 `cross_skill_contract_drift`) plus the next dated review.

## Dependency Order

Phase A (FR1–FR3) shipped as commit `d3516db` on branch `chore/m84-public-surface-drift-cleanup`. Phase B (FR4–FR9) parallel — pick any order. Phase C (close-out) sequential after Phase B completes.

**Phase B internal coordination notes:**

- **STE-319 (FR5)** and **STE-316 (FR2)** share docs (`docs/setup-tracker-mode.md`, `docs/spec-review-tracker-mode.md`). STE-319 explicitly scopes to §-anchors only and lets STE-316 own the DD-12.x refs (per STE-319 § Notes carve-out).
- **STE-320 (FR6)** and **STE-322 (FR8)** are fully orthogonal. STE-320 touches `agents/code-reviewer.md` + capability registry .ts; STE-322 touches skill SKILL.md files + test files.
- **STE-321 (FR7)** touches `adapters/_shared/src/tracker_config.ts` + `skills/gate-check/SKILL.md:26` + `specs/technical-spec.md:275-276`. STE-322 also touches `skills/gate-check/SKILL.md` (probe #44 expansion via FR6, but FR6 owns that surface). Coordinate same-day merges so the L26 edit lands without conflict.
- **STE-318 (FR4)** adds probe #58. STE-319 (FR5) cap-citation backfill might also touch `skills/gate-check/SKILL.md` L214 (already handled by FR1 STE-315). Verify line-numbers haven't shifted before each edit.
- **Line-shift risk: STE-318 / STE-320 ↔ STE-319** (added 2026-05-20 audit pass). STE-318 rewrites prose at `docs/skill-anatomy.md:84,86` + `docs/adaptation-guide.md:81`; STE-320 rewrites adjacent prose at `docs/skill-anatomy.md:152` + `docs/adaptation-guide.md:113`. STE-319's cap-citation table cites line numbers in both files. Whichever of STE-318/STE-320 lands first WILL shift the line numbers STE-319 targets. Mitigation: edit-by-content not by line, OR ship STE-319 last and re-resolve line numbers at edit time.
- **STE-322 step renumber** shifts line numbers across many files (`docs/implement-reference.md`, `docs/implement-tracker-mode.md`, `adapters/jira.md`, `adapters/linear.md`, `skills/gate-check/SKILL.md`, plus two test files). Ship after FR4–FR7 or re-resolve line numbers at edit time.
- **STE-323 (FR9)** is fully isolated — single source file + single test file. Safe to ship first or last.

## Origin

`/dev-process-toolkit:spec-write /tmp/dpt-review-findings-2026-05-20.md` invoked 2026-05-20 against `main` @ `2875661` (v2.28.1). The findings file aggregates 41 verified survivors across 30 problem areas (A1–A30) from a clean-context review pass.

**Phase A** — initial M84 charter selected the three HIGH-severity user-hostile-drift items (A1 + A4 + A8) as the most urgent cohort. Shipped as commit `d3516db` on `chore/m84-public-surface-drift-cleanup`.

**Phase B** — `/dev-process-toolkit:brainstorm` invoked 2026-05-20 on the same findings file extended M84 to cover HIGH + MED items. Six themed bundles (FR4–FR9) approved via the Socratic clarification loop: bundling = themed (over per-area), granularity = 6 FRs (over 5 or 14), with the two embedded design questions (A11 `tracker:` shape, A12 `tracker_config` allowlist) resolved upfront in the brainstorm session before drafting.

Spec-research seeds fired per FR — STE-318 surfaced STE-138 + STE-296 + STE-308; STE-319 surfaced STE-315 + STE-316 + STE-305; STE-320 surfaced HG95V0 + HG95TM + STE-238 + STE-308; STE-321 surfaced STE-302 + STE-303 + STE-316; STE-322 surfaced STE-161 + STE-73 + STE-167; STE-323 surfaced STE-73.

## Release Target

v2.29.0 (provisional, minor). Codename TBD; decide at `/ship-milestone` time.

## Authoritative spec

`specs/plan/M84.md` (this file).
