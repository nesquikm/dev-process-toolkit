---
milestone: M122
status: archived
archived_at: 2026-08-15T09:57:38Z
kickoff_branch: feat/m122-ship-ready-nudge
frozen_at: null
migration: none
shipped_in: null
---

# Implementation Plan

## M122 — Ship-Ready Milestone Nudge {#M122}

**Goal:** Close the pre-archive encouragement gap for `/ship-milestone`. Probe #63 (`plan_ship_coherence`) turns the gate red only after a milestone's plan is archived; a milestone whose FRs are all done but whose plan was never closed lingers silently. M122 adds a warning-severity gate probe naming every ship-ready active plan, and generalizes the `/implement` end-of-loop ship offer (STE-75) to FR-form runs that archive the last remaining FR of a milestone.

**Prerequisites:** None. First milestone of the 2026-08-15 four-milestone program (dogfood order: M122 nudge → `/deliver` orchestrator → best-practice sources → `/setup` template source); the later milestones do not depend on M122's code, only on its ship.

**Release target:** v2.62.0

**Migration:** `none` — no consumer-artifact convention changes. The probe reads existing frontmatter shapes; the `/implement` offer is new prose over an existing ceremony. Nothing an existing downstream project would need `/upgrade` to repair.

### FRs

| FR | Title | Status |
|---|---|---|
| [STE-462](../frs/archive/STE-462.md) | Ship-ready milestone nudge: active-plan gate probe + /implement close offer | active |

### Tasks

1. Predicate + probe module (`active_plan_ship_ready.ts`) with its test file — AC-STE-462.1..4, .9.
   verify: `bun test tests/gate-check-active-plan-ship-ready.test.ts` green; mutation checks red as specified.
2. Probe registration in the gate-check SKILL numbered list + every probe-count-pinned meta-test site — AC-STE-462.5.
   verify: `bun test` (full gate root) green with the new count.
3. `/implement` FR-form close offer prose + meta-test pins (offer prompt, decline line, negative assertion, NFR-1 caps) — AC-STE-462.6..8.
   verify: `bun test` meta-tests green; grep pins hit the exact prompt strings.

### Dependency graph

Single-FR milestone; tasks 1 → 2 → 3 in order (2 and 3 both depend on 1; 3 references the module path registered in 2).
