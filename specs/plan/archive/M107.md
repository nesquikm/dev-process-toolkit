---
milestone: M107
status: archived
archived_at: 2026-07-16T16:07:10Z
kickoff_branch: fix/m107-docs-drift-sweep
frozen_at: null
shipped_in: v2.48.1
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M107: Docs-drift sweep {#M107}

**Goal:** align shipped docs with the current branch-automation contract and zero
out the standing probe-#37 advisories. Three stale phrasings (template trunk-OK
comment still naming retired types; two docs describing `{type}` as LLM-inferred;
the technical spec's "scope-aware default" note) plus five tree-leaf advisory rows
in `specs/technical-spec.md` (three genuinely stale, two illustrative examples).
Prose-only — scanner exemption rejected at spec time. Origin: M106 ceremony
advisories (/spec-review drift hints + /gate-check probe #37), 2026-07-16.

**Prerequisites:** v2.48.0 shipped (main @ PR #42). Load-bearing context: STE-228
(trunk-OK narrowed to `["ci"]`), STE-381 (deterministic `branchTypeFor`), STE-388
(single canonical seeded default `{type}/m{N}-{slug}`), STE-295 (probe #37
fence-only scope).

**Release target:** v2.48.1 (patch — pure-Fixed milestone). ⚠️ M101 (branch
`feat/ste-375-jira-milestone-as-epic`) is unshipped and in flight; it must rebase
past whatever ships first. `/implement` Phase 1 + `/ship-milestone` re-validate
against the live `plugin.json`, so this is a soft prediction.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-390 | Docs-drift sweep: retire stale branch-automation phrasing, clear tree-leaf advisories | linear:`STE-390` |

**Build order:** single FR.

**Tasks:**

_STE-390 — docs-drift sweep:_
- [x] Fix `templates/CLAUDE.md.template` trunk-OK comment to name `ci` only — STE-390
  verify: meta-test pins corrected line + retired-phrase absence GREEN
- [x] Sweep "LLM-inferred `{type}`" phrasing from `docs/setup-tracker-mode.md` + `docs/patterns.md` to the deterministic `branchTypeFor` contract — STE-390
  verify: meta-test asserts absence + presence in both files GREEN
- [x] Rewrite `specs/technical-spec.md` Schema L `branch_template:` bullet to the single canonical seeded default — STE-390
  verify: meta-test pins the corrected bullet GREEN
- [x] Fix the three stale tree leaves + reword the two illustrative example fences in `specs/technical-spec.md` — STE-390
  verify: dogfood meta-test — probe #37 returns zero violations on this repo — GREEN
- [x] Author the doc-conformance meta-test carrying all pins + the dogfood assertion — STE-390
  verify: `bun test tests/` GREEN with the new file counted
- [x] Calibration sweep as needed (no skills/ edits expected; line caps untouched) — STE-390
  verify: full `bun test` from `plugins/dev-process-toolkit` GREEN

**Tests:**
- Doc-conformance meta-test: per-site retired-phrase absence + corrected-phrase presence pins; probe-#37 dogfood zero-violations assertion (STE-390)

**Acceptance Criteria:**
- [x] Template trunk-OK comment names `ci` only; retired phrasing gone
- [x] Both docs describe `{type}` as deterministic via `branchTypeFor`; `{slug}` untouched
- [x] Technical-spec Schema L note names the single canonical seeded default
- [x] All five probe-#37 advisories cleared; dogfood meta-test pins zero violations
- [x] Full `bun test` gate + `/dev-process-toolkit:gate-check` GREEN

**Gate:** `bun test` (from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN

**Out of scope:**
- Scanner exemption logic in `scan_cross_cutting_spec_stale_file_refs.ts` — rejected at spec time; prose edits only.
- Probe #37 severity changes.
- M101 content (rebases independently).

<!-- token-stats:begin -->

## Token Stats

| scope | model | input | output | cache-read | cache-creation |
| --- | --- | ---: | ---: | ---: | ---: |
| dev-process-toolkit:gate-check | claude-fable-5 | 4539 | 113266 | 11387521 | 348462 |
| dev-process-toolkit:implement | claude-fable-5 | 74 | 64524 | 2946425 | 362557 |
| dev-process-toolkit:ship-milestone | claude-fable-5 | 52 | 47924 | 7977368 | 95674 |
| dev-process-toolkit:tdd | claude-fable-5 | 99 | 63769 | 6727146 | 135066 |
| (main-loop) | claude-fable-5 | 72 | 39240 | 10020292 | 85209 |
| total |  | 4836 | 328723 | 39058752 | 1026968 |

<!-- token-stats:end -->
