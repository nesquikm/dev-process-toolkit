---
milestone: M11
status: complete
kickoff_branch: null
frozen_at: null
revision: 1
---

# M11: Single File

## M11: Single File {#M11}

**Goal:** Patch the one-clarifying-question-at-a-time violation observed at the tail of the v1.13.0 session. Tighten `/brainstorm` and `/spec-write` wording so Claude doesn't batch questions at phase transitions or inside `/spec-write`'s per-section blocks. Ship as v1.14.0.

**Prerequisites:** M10 shipped (no file conflicts — M11 touches `brainstorm/SKILL.md` and `spec-write/SKILL.md`, neither of which M10 changed).

**Tasks:**

- [x] Task 1 — Add new Rules bullet + `### Rationalization Prevention` subsection to `skills/brainstorm/SKILL.md`. Adopt the canonical one-at-a-time sentence as the Rules bullet text.
  verify: grep finds the canonical sentence verbatim; grep finds `### Rationalization Prevention` heading; table has 3–6 rows covering "independent", "efficiency", "responsive user" rationalizations (AC-28.1, AC-28.2); `wc -l skills/brainstorm/SKILL.md` < 300 (AC-28.6)
- [x] Task 2 — Reshape the four per-section question blocks in `skills/spec-write/SKILL.md` (`#### requirements.md`, `#### technical-spec.md`, `#### testing-spec.md`, `#### plan.md`) from bulleted simultaneous questions to ordered-waiting prose ("Ask {Q1}. Wait for the answer. Then ask {Q2}.")
  verify: read each block; bullet-list simultaneous questions replaced; question set unchanged (AC-28.3, AC-28.7)
- [x] Task 3 — Add the canonical one-at-a-time sentence to `skills/spec-write/SKILL.md` Rules section, byte-identical to the one in brainstorm
  verify: `diff` on both extracted lines returns zero differences (AC-28.4, AC-28.5); `wc -l skills/spec-write/SKILL.md` < 300 (AC-28.6)
- [x] Task 4 — Bump version to 1.14.0 in `plugin.json` and `marketplace.json`; append CHANGELOG `## [1.14.0] — YYYY-MM-DD — "Single File"` section with FR-28 cross-reference and a short motivation paragraph referencing the v1.13.0-session observation
  verify: both JSON files read 1.14.0; CHANGELOG entry exists with FR-28 reference
- [x] Task 5 — Tier 1 static verification: run all FR-28 grep checks from `testing-spec.md` § Tier 1
  verify: every Tier 1 row for FR-28 reports the expected match
- [x] Task 6 — Tier 2 behavioral dogfood: synthetic `/brainstorm` session on a toy feature with 2 latent scope-lock questions at the spec-write handoff point; confirm the tightened skill instructs one-at-a-time behavior (read skill body, not runtime execution)
  verify: skill body now contains guidance that would prevent the v1.13.0-session violation

**Acceptance Criteria (milestone-level):**
- All FR-28 ACs pass Tier 1 static verification
- Canonical sentence is byte-identical across both skill files
- Both skills remain under 300 lines
- Release trio bumped to 1.14.0 together (plugin.json, marketplace.json, CHANGELOG.md)

**Gate:** `/gate-check` passes; release-trio in sync; PR opened for human review and merge.
