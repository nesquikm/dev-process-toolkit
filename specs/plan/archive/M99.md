---
milestone: M99
status: archived
archived_at: 2026-07-06T12:28:44Z
kickoff_branch: feat/m99-post-merge-ship-ceremony
frozen_at: null
shipped_in: v2.40.0
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M99: post-merge ship ceremony — shipped_in stamp + ship-debt beacon {#M99}

**Goal:** when an implementation PR merges to main before `/ship-milestone` + archival run, the leftover ceremony is unsupported folklore, nothing detects the debt (every version signal stales coherently — probe #9b passes), and `/pr` is milestone-blind; M98 sat merged-but-unshipped for two days. Make "shipped" a deterministic file property (`shipped_in` frontmatter stamped by the release commit), make its absence on archived plans a gate ERROR with an explicit visible `ship_state: parked` opt-out, and surface/collapse the ceremony at every touchpoint (`/pr` soft pre-flight, bare-`/ship-milestone` debt offer, opt-in `/pr` chain, documented recipe). Early merges stay fully legitimate — nothing hard-blocks.

**Prerequisites:** v2.39.0 shipped (main @ PR #31). Load-bearing existing machinery: STE-210 archive fallback (already carries the post-merge ship when archival merged too), STE-228 off-trunk branch-gate no-op, STE-75 opt-in chain mechanics, STE-92 one-shot-backfill precedent. No new external dependencies.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-368 | /ship-milestone shipped_in stamp + legacy archived-plan backfill | linear:`STE-368` |
| STE-369 | gate-check plan_ship_coherence probe + /spec-archive --parked | linear:`STE-369` |
| STE-370 | post-merge ceremony surfacing: /pr pre-flight + ship-debt offer + /pr chain + recipe | linear:`STE-370` |

**Dependency order:** STE-368 → STE-369 → STE-370. The probe (369) reads the stamp semantics and needs the backfill in the same milestone so it starts green (AC-STE-92.7 precedent); the surfacing FR (370) reuses 369's unstamped-unparked predicate and 368's absence signal. 368 and 369 touch disjoint files; 370 shares `ship-milestone/SKILL.md` with 368 — landing last avoids edit-conflict churn.

**Tasks:**

- [x] STE-368 — Stamp helper `plan_ship_stamp.ts` + ship-milestone SKILL.md stamp step (pre-flight #2 set, step-6 preview, step-7 write) + one-shot legacy backfill (reviewed mapping diff, script deleted after commit). — STE-368
  verify: helper unit tests (write / preserve / idempotent / double-ship refusal) + SKILL.md meta-test green; every `specs/plan/archive/M*.md` stamped or parked post-commit.
- [x] STE-369 — Probe `plan_ship_coherence` (ERROR; stamped→CHANGELOG cross-check, parked→NOTES, neither→FAILED with recipe remedy) + gate-check SKILL.md row + `/spec-archive --parked` + both exit hints. — STE-369
  verify: `tests/gate-check-plan-ship-coherence.test.ts` positive + three negatives green; `/gate-check` on this repo green including the new probe.
- [x] STE-370 — /pr soft pre-flight (`[m]/[s]/[a]` + `Follow-up:` PR-body line) + bare-/ship-milestone debt offer + opt-in `Open ceremony PR via /pr now?` chain + `## Post-merge ceremony` recipe in docs/ship-milestone-reference.md. — STE-370
  verify: meta-tests pin all five exact-format strings; NFR-1 line caps green on pr + ship-milestone SKILL.mds.

**Tests:**
- Unit — `plan_ship_stamp` helper behaviors (STE-368); `plan_ship_coherence` decision matrix over fixture archives (STE-369).
- Meta — SKILL.md prose pins: stamp step + widened expected set (368), probe row + `--parked` + exit hints (369), the five prompt/hint strings + docs heading + soft-prompt negative assertion (370).
- Runtime (ships `[~]` pending next live occurrence) — one real post-merge ceremony driven end-to-end via debt offer → ship → chained /pr (AC-STE-370.5 runtime leg).

**Acceptance Criteria:**
- [x] AC-STE-368.1–.5 (release-commit stamp both paths; expected-set + preview; helper semantics; backfill resolution order; same-commit compliance).
- [x] AC-STE-369.1–.5 (stamp coherence; unshipped-debt FAILED; parked NOTES; --parked flag + hints; probe row + tests).
- [x] AC-STE-370.1–.5 (soft /pr pre-flight; Follow-up line; debt offer; opt-in /pr chain; recipe docs + meta-tests) — runtime leg of .5 `[~]`.

**Gate:** `bun test` GREEN (full gate from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN on this repo (now including `plan_ship_coherence`).

**Release target:** v2.40.0 (provisional, minor). M92 (`chore/ste-344`) and M97 (`feat/m97`) remain unshipped on sibling branches and will carry `ship_state: parked` if archived before shipping; the final version resolves at `/ship-milestone` per the release-collision guard.
