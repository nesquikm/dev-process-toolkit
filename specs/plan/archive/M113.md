---
milestone: M113
status: archived
archived_at: 2026-07-26T00:00:36Z
kickoff_branch: fix/m113-milestone-name-emit-canonicalization
frozen_at: null
migration: none
---

# Implementation Plan

## M113 — Milestone-name emit canonicalization {#M113}

**Goal:** Close the colon-vs-em-dash milestone-name divergence a fresh Linear consumer chain hits (2026-07-23 conformance run, Linear leg: 71/72, probe #26 the sole red). Extend STE-335 (parser-side normalization) to the emit side: `/spec-write` emits em-dash milestone headings by default, the milestone-attachment prose mandates the em-dash canonical name, and the create-side derivation is pinned to the same normalizer probe #26 consumes. The parser stays tolerant of the colon form so legacy plan files keep working.

**Prerequisites:** none — every fix site is a live plugin surface (`skills/spec-write/SKILL.md`, `templates/spec-templates/plan.md.template`, `adapters/_shared/src/plan_file_single_milestone.ts`) plus meta-tests. STE-335's `parsePlanHeading` already exists and is unchanged.

**Release target:** TBD (single-FR milestone; re-bump if a sibling branch ships first — M101/M112 remain unshipped on their branches).

**Migration:** `none` — this milestone changes plugin surfaces only (SKILL prose, one template heading, one probe regex, meta-tests); no consumer-artifact convention change, so no registry entry.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-415 | /spec-write emits em-dash canonical milestone names to match probe #26 | linear:`STE-415` |

**Tasks:**

- [x] Amend `/spec-write` SKILL § 0b milestone-attachment prose: em-dash canonical requirement + colon-form named forbidden-at-emit + `parsePlanHeading` / `plan_heading.ts` cross-reference as single source of truth — STE-415
  - verify: new grep-shape meta-test asserts the em-dash clause + colon-forbidden phrase + `parsePlanHeading` cross-ref all present in `skills/spec-write/SKILL.md`
- [x] Rewrite the SKILL "Stable anchor IDs" exemplar and `templates/spec-templates/plan.md.template` milestone heading from `## M<N>:` to `## M<N> —` (retain the `{#M<N>}` anchor) — STE-415
  - verify: meta-test asserts no `## M<N>:` colon emit site in the SKILL exemplar or template, and the `{#M<N>}` anchor is present in both
- [x] Widen probe #32 `MILESTONE_HEADING_RE` to `/^## M\d+ *(—|:)/gm` + separator-neutral reason wording; update the gate-check SKILL probe #32 description and `tests/plan-template-shape.test.ts` AC-STE-197.1 regex to the em-dash form — STE-415
  - verify: `bun test tests/gate-check-plan-file-single-milestone.test.ts tests/plan-template-shape.test.ts` green; an em-dash multi-milestone fixture counts >1 headings
- [x] Add the shared-normalizer unit test: a colon-form plan heading through `planFileHeadingToMilestoneName` byte-equals `M2 — Greeting core` and byte-equals probe #26's canonical derivation for the same file — STE-415
  - verify: `bun test` for the new unit-test file green (create-side and probe-#26-side agree)
- [x] Full gate green — STE-415
  - verify: `bun test` from `plugins/dev-process-toolkit` green (~4950+ tests, incl. the STE-335 parser matrix unchanged)
