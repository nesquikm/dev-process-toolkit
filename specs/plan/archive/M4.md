---
milestone: M4
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M4: TDD + Implement

**Goal:** The heavy milestone — shallow test detection, SPEC_DEVIATION extraction, spec breakout, worktree recovery. These are the "build-time enforcement" features.
**Prerequisites:** M3 (drift detection patterns from gate-check inform Phase 4 report; Schema A/B established)

**Files to edit:**
- `skills/tdd/SKILL.md` — FR-8
- `skills/implement/SKILL.md` — FR-1 (Phase 4 ref), FR-5, FR-6, FR-8, FR-12

**Tasks:**
1. Add `### Assertion Quality` subsection to `tdd/SKILL.md` RED phase: Schema D anti-pattern list (3 items, exact order), guidance to assert on output/behavior (AC-8.1, AC-8.2)
2. Add `Assertion quality check` to `implement/SKILL.md` Phase 3 Stage A after AC check: same Schema D anti-pattern list (exact same items, same order as tdd) (AC-8.3, AC-8.4)
3. Add `### Spec Deviation Summary` subsection to `implement/SKILL.md` Phase 4 before step 13: Schema C table template with 4 columns, 4 classification types, bold Yes for ambiguous provisional (AC-5.1 through AC-5.4)
4. Add `### Spec Breakout` subsection to `implement/SKILL.md` Phase 2 after step 9: threshold of 3 `contradicts`/`infeasible` deviations with `<!-- ADAPT -->` marker, stop-and-report instruction, literal "valid output, not a failure" phrasing (AC-6.1 through AC-6.5)
5. Add `### Partial Failure Recovery` subsection to `implement/SKILL.md` Pre-flight worktree section: list completed vs. failed, 3 recovery options (cherry-pick, continue, discard), exact git commands for each (AC-12.1 through AC-12.4)
6. Add drift summary reference to `implement/SKILL.md` Phase 4 report bullet list (AC-1.5)
7. Verify `implement/SKILL.md` total line count is under 300 (NFR-1)

**Acceptance Criteria:**
- [x] AC-8.1: `### Assertion Quality` in tdd RED phase
- [x] AC-8.2: 3+ concrete anti-patterns listed
- [x] AC-8.3: `Assertion quality check` in implement Phase 3 Stage A
- [x] AC-8.4: Anti-pattern list identical in tdd and implement (same items, same order)
- [x] AC-5.1: `### Spec Deviation Summary` in Phase 4
- [x] AC-5.2: Schema C table with 4 columns
- [x] AC-5.3: 4 classification types matching Phase 2 step 9
- [x] AC-5.4: Rule: Classification = ambiguous → Needs Confirmation = `**Yes**`
- [x] AC-6.1: `### Spec Breakout` in Phase 2 after step 9
- [x] AC-6.2: Threshold of 3 for contradicts/infeasible
- [x] AC-6.3: `<!-- ADAPT -->` marker on threshold
- [x] AC-6.4: Stop-and-report instruction
- [x] AC-6.5: Literal `Spec Breakout is a valid output, not a failure`
- [x] AC-12.1: `### Partial Failure Recovery` in Pre-flight
- [x] AC-12.2: 3 recovery options listed
- [x] AC-12.3: Exact git commands for each option
- [x] AC-12.4: Completed vs. failed work listed before options
- [x] AC-1.5: Phase 4 report has literal item `Drift findings (if specs/ exists and gate-check was run)`
- [x] NFR-1: implement/SKILL.md under 300 lines

**Gate:** Tier 1 static verification (line count critical: budget ~273, max 300) + Tier 2 before/after for FR-8 (shallow test), FR-6 (spec breakout). Cross-check Schema D identical in both files (AC-8.4).
