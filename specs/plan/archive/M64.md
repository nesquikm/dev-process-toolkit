---
milestone: M64
status: archived
archived_at: 2026-05-06T14:00:39Z
kickoff_branch: null
frozen_at: null
shipped_in: v2.15.0
---

# M64 — Phase 2.X runtime fixtures for M58/M60/M61/M63 cohort {#M64}

**Release target:** v2.15.0 (minor — additive Phase 2.X extension to the project-local `/smoke-test` skill; no plugin surface change. Bundles standalone — no other M64 candidates pending. Note: the original draft targeted v2.14.0, but that version shipped same-day for M63 — `/implement` Phase 4 deviation shifted M64 to v2.15.0.)
**Codename:** TBD at `/ship-milestone` time.

**Status:** drafted 2026-05-06 from `/brainstorm` session same day; user approved scope = 4 user-proposed groups + addition A (branch-gate inverse-fixture). M62 `/report-issue` runtime testing explicitly excluded; `/spec-archive` + `/ship-milestone` branch-gate coverage explicitly deferred.

## Why this milestone exists

M58 (TDD orchestrator), M60 (`--no-tech` flag), M61 (universal branch gate), and M63 (spec-research subagent) all shipped runtime contracts that bun-mocked unit tests cannot exercise under real `claude -p` against a fresh test project. Three of those four (M58, M61, M63) explicitly carry deferred `[~]` archival-evidence ACs, and one (M60 / AC-STE-227.11) deferred its smoke fixture entirely. M64 closes those deferred items by adding four new fixture groups to `/smoke-test` Phase 2.X and a fifth sub-fixture pair (marker-present / marker-absent) for the M61 branch gate, validated by a single post-implementation `/conformance-loop` run.

After M64 ships:

- `/smoke-test` Phase 2.X carries seven fixture groups (1–3 from M56, 4–7 new), with the canonical `STE-<sut> runtime regression: <fixture-name>` diagnostic shape preserved across all.
- Group 4 drives `--no-tech` end-to-end (4-step refusal/clear/completion/archive pass) on Linear + Jira.
- Group 5 (sub-fixtures 5a + 5b) pins both directions of the M61 branch-gate marker contract for `/spec-write` only (per-skill expansion to `/spec-archive` + `/ship-milestone` deferred).
- Group 6 asserts the spec-research subagent fired inside the canonical-chain `/spec-write` child (lenient — any of three audit rows).
- Group 7 asserts the TDD orchestrator's three forked subagents fired inside the canonical-chain `/implement` child.
- A single `/conformance-loop` run on this repo, post-implementation, captures real `claude -p` stdout demonstrating the new fixtures fire (`[~]` deferred per `feedback_smoke_post_ship_retroactive` if the M64 archival window doesn't carry one).

## FR list (1 FR)

| FR      | Title                                                                                  | Tracker          | Phase                            | Severity / Recurrence                          |
|---------|----------------------------------------------------------------------------------------|------------------|----------------------------------|------------------------------------------------|
| STE-231 | feat(skills/smoke-test): Phase 2.X runtime fixtures for M58/M60/M61/M63 cohort         | linear:`STE-231` | project-local skill (smoke-test) | new — single FR, additive, project-local       |

## M64: Phase 2.X runtime fixtures {#M64-fr-block}

**Goal:** Add four new Phase 2.X fixture groups (4 = `--no-tech`, 5 = branch-gate 5a/5b, 6 = spec-research, 7 = TDD orchestrator) to `.claude/skills/smoke-test/SKILL.md`, preserving the canonical diagnostic + assertion shape from M56 (STE-220 / STE-221 / STE-222), and validate via one post-implementation `/conformance-loop` run.

**Tasks (in dependency order):**

1. Author Phase 2.X group 4 (`--no-tech` end-to-end) in `.claude/skills/smoke-test/SKILL.md` after group 3, with sub-fixtures 4a (Linear) + 4b (Jira), each driving the 4-step pass via four marker-bearing heredoc-on-stdin spawns. Diagnostic shape `STE-227 runtime regression: <fixture-name>`.
   verify: `grep -c '#### Fixture group 4 — STE-227' .claude/skills/smoke-test/SKILL.md` returns 1; `grep -c 'STE-227 runtime regression' .claude/skills/smoke-test/SKILL.md` returns ≥ 2 (one per sub-fixture diagnostic block)
2. Author Phase 2.X group 5 (branch-gate marker contract) with sub-fixtures 5a (marker present → assert `branch_gate_default_applied` row + `git branch --show-current` matches the proposed name) + 5b (marker absent → assert gate halts, no branch created — mirror STE-226 fixture 1b). Both Linear + Jira. Diagnostic `STE-228 runtime regression: <fixture-name>`.
   verify: `grep -c '#### Fixture group 5 — STE-228' .claude/skills/smoke-test/SKILL.md` returns 1; `grep -c 'STE-228 runtime regression' .claude/skills/smoke-test/SKILL.md` returns ≥ 2
3. Author Phase 2.X group 6 (spec-research subagent) — assertion against the existing `/tmp/dpt-smoke-<tracker>-spec-write.log` (no new spawn). Lenient: assert at least one of `spec_research_invoked` / `spec_research_no_matches` / `spec_research_shape_violation` rows present. Both Linear + Jira. Diagnostic `STE-230 runtime regression: spec-research-no-audit-row`.
   verify: `grep -c '#### Fixture group 6 — STE-230' .claude/skills/smoke-test/SKILL.md` returns 1; `grep -cE 'spec_research_(invoked|no_matches|shape_violation)' .claude/skills/smoke-test/SKILL.md` returns ≥ 3
4. Author Phase 2.X group 7 (TDD orchestrator forks) — assertion against the existing `/tmp/dpt-smoke-<tracker>-implement.log` (no new spawn). Assert all three `tdd-result` fenced blocks present in order (test-writer → implementer → refactorer). Both Linear + Jira. Diagnostic `STE-225 runtime regression: tdd-result-blocks-incomplete`.
   verify: `grep -c '#### Fixture group 7 — STE-225' .claude/skills/smoke-test/SKILL.md` returns 1; `grep -c 'STE-225 runtime regression' .claude/skills/smoke-test/SKILL.md` returns ≥ 1
5. Extend the Phase 2.X summary line — append per-group PASS/FAIL rendering for groups 4–7; preserve the existing `M56 runtime checks: PASS` line for groups 1–3 unchanged.
   verify: `grep -cE 'STE-(227|228|230|225) runtime check:' .claude/skills/smoke-test/SKILL.md` returns 4
6. Run `/conformance-loop --max-iterations 1 --jira-project DST` post-implementation as archival evidence; capture real `claude -p` stdout for at least one fixture per new group. May ship `[~]` deferred per memory `feedback_smoke_post_ship_retroactive`.
   verify: `/tmp/dpt-conformance-loop-<date>-iter-1.md` shows `STE-227 runtime check: PASS`, `STE-228 runtime check: PASS`, `STE-230 runtime check: PASS`, `STE-225 runtime check: PASS`
7. CHANGELOG, plugin manifest, marketplace.json, README "Latest:" line, `specs/requirements.md` "Latest shipped release:" line bumped per `/ship-milestone` Release Files block at v2.15.0.
   verify: `/gate-check` probe #9b (root spec hygiene — release marker line) passes; `/gate-check` overall green.

**Acceptance Criteria:**

- [x] AC-STE-231.1 — fixture group 4 (STE-227 `--no-tech` end-to-end) authored with sub-fixtures 4a Linear + 4b Jira.
- [x] AC-STE-231.2 — fixture group 5 (STE-228 branch-gate marker contract) authored with sub-fixtures 5a (marker present) + 5b (marker absent, prose-only); coverage-gap note for `/spec-archive` + `/ship-milestone` deferral inline.
- [x] AC-STE-231.3 — fixture group 6 (STE-230 spec-research subagent runtime) authored; lenient OR over three audit rows.
- [x] AC-STE-231.4 — fixture group 7 (STE-225 TDD orchestrator forks runtime) authored; ≥3 `tdd-result` fenced-block assertion.
- [x] AC-STE-231.5 — diagnostic-shape invariant `STE-<sut> runtime regression: <fixture-name>` preserved across all 4 groups (sut ∈ {227, 228, 230, 225}, never 231).
- [x] AC-STE-231.6 — Phase 2.X summary line section extended; M56 line preserved unchanged; per-group footers contribute the 4 new runtime-check lines.
- [x] AC-STE-231.7 — capability-key map untouched (no new keys in `/spec-write` § 7).
- [x] AC-STE-231.8 — no new `/gate-check` probe added.
- [~] AC-STE-231.9 — live `/conformance-loop --max-iterations 1` archival evidence. Deferred per memory `feedback_smoke_post_ship_retroactive`; the FR (line 35) explicitly permits this defer.
- [x] AC-STE-231.10 — out-of-scope assertions (`/report-issue` excluded; `/spec-archive` + `/ship-milestone` per-skill expansion deferred) documented as a coverage-gap note inside group 5's section.
- [~] AC-STE-231.11 — CHANGELOG, plugin manifest, marketplace.json, README "Latest:" line, `specs/requirements.md` "Latest shipped release:" line bumped per `/ship-milestone` Release Files block at v2.15.0. Deferred to `/ship-milestone` (its domain, not `/implement`'s); spec deviation logged (original target v2.14.0 collided with M63's same-day release; bumped to v2.15.0).

**Gate:** `bun test && /dev-process-toolkit:gate-check`

## Success criteria

- All 11 ACs on STE-231 pass.
- The four new fixture groups fire under a live `/conformance-loop` run with the expected `STE-<sut> runtime check: PASS` lines.
- No new `/gate-check` probes added (Phase 2.X lives in project-local SKILL.md; gate-check stays plugin-internal).
- CHANGELOG, plugin manifest, marketplace.json, README "Latest:" line bumped per `/ship-milestone` Release Files block.
- `specs/requirements.md` "Latest shipped release:" line is `v2.15.0` post-ship (gate-check probe #9b).

## Cross-cutting impact

- `.claude/skills/smoke-test/SKILL.md` — four new Phase 2.X group sections (4 / 5 / 6 / 7) inserted after group 3; Phase 2.X summary-line rendering extended.
- No plugin-internal changes (no `plugins/dev-process-toolkit/` files touched).
- No `requirements.md` / `technical-spec.md` / `testing-spec.md` changes (project-local skill extension; not a cross-cutting architecture change).
- `specs/requirements.md` — "Latest shipped release:" bumped post-ship.

## Notes

**Provenance.** Drafted 2026-05-06 from `/brainstorm` session same day. User approved scope = 4 user-proposed groups + addition A (branch-gate marker-absent inverse fixture); rejected addition B (per-skill branch-gate expansion to `/spec-archive` + `/ship-milestone`); excluded M62 `/report-issue` runtime testing.

**Memory composition.**

- `feedback_smoke_post_ship_retroactive` — applies to plan task 6 (live archival evidence). May ship `[~]` deferred.
- `feedback_check_milestones_first` — M64 selected after scan: M1–M63 archived; CHANGELOG up to v2.13.0 / M63.
- `project_no_users_yet` — vacuous (project-local skill, no plugin surface).
- `feedback_branch_isolation` + `feedback_implement_pacing` — vacuous for the SKILL.md edit work.

**Spec-research seed.** STE-220 / STE-221 / STE-222 are the canonical Phase 2.X precedents (M56 cohort). Reusable patterns lifted: `STE-<sut> runtime regression: <fixture-name>` diagnostic shape, behavioral-assertion-only rule, `/tmp/dpt-smoke-<tracker>-<skill>.log` capture pattern, deferred `[~]` archival evidence.

**Out-of-scope assertions documented in SKILL.md.** Group 5's section MUST carry an inline coverage-gap note: "Per-skill expansion to `/spec-archive` + `/ship-milestone` deferred — both are explicitly NOT in the canonical chain (would corrupt real data); the canonical chain transitively exercises the gate for `/setup` (Phase 1 bootstrap), `/spec-write` (Phase 2 step 2), `/implement` (Phase 2 step 3) — three of five commit-producing skills." This makes the deferred coverage discoverable to future operators reading the SKILL.md.
