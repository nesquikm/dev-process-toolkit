---
milestone: M3
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M3: Gate-Check + Code-Reviewer

**Goal:** Add drift detection, security scanning, structured output to gate-check; add spec compliance to code-reviewer. These form the "review layer" of the toolkit.
**Prerequisites:** None (Schema A is defined in NFR-4, not created by another milestone; CI docs in M1 are independent)

**Files to edit:**
- `skills/gate-check/SKILL.md` — FR-1, FR-2, FR-3
- `agents/code-reviewer.md` — FR-11

**Tasks:**
1. Add optional security audit step to `gate-check/SKILL.md` Commands section after step 4: list `npm audit`, `pip-audit`, `cargo audit`, `flutter pub audit` as examples, mark as optional (AC-2.1)
2. Add security-specific `<!-- ADAPT -->` examples to Code Review section: at least 2 examples (AC-2.2)
3. Update Security row in Code Review rubric to reference audit tools (AC-2.3)
4. Add `## Drift Check` section after `## Code Review`: conditional on `specs/` existing, AC traceability table using Schema A format with columns `AC ID`, `Status`, `Location`, literal instruction that drift does NOT cause GATE FAILED (AC-1.1 through AC-1.4)
5. Update `## Verdict` section: add drift findings to NOTES path (AC-1.4 continued)
6. Add `## Structured Output` section before `## Rules`: JSON summary guidance with example schema `{"step": "...", "status": "pass|fail", "summary": "..."}` (AC-3.1, AC-3.2)
7. Add `5. **Spec compliance**` section to `agents/code-reviewer.md`: conditional on `specs/` existing, cross-reference ACs, report gaps in unified format (AC-11.1 through AC-11.3)

**Acceptance Criteria:**
- [x] AC-2.1: Commands section has optional security audit step with stack examples
- [x] AC-2.2: Code Review has security-specific ADAPT examples
- [x] AC-2.3: Security rubric row references audit tools
- [x] AC-1.1: `## Drift Check` section exists after `## Code Review`
- [x] AC-1.2: Conditional on `specs/` existing, skip silently otherwise
- [x] AC-1.3: Table with columns `AC ID`, `Status`, `Location`
- [x] AC-1.4: Literal `Drift findings do NOT cause GATE FAILED` + NOTES path
- [x] AC-3.1: `## Structured Output` section exists
- [x] AC-3.2: JSON schema example with `step`, `status`, `summary` fields
- [x] AC-11.1: `5. **Spec compliance**` section in code-reviewer
- [x] AC-11.2: Conditional on `specs/` existing
- [x] AC-11.3: Spec gaps reported in unified findings format

**Gate:** Tier 1 static verification + Tier 2 before/after for FR-1 (with/without specs/). Line counts: gate-check ~132, code-reviewer ~48. Cross-check Schema A consistency with spec-review (M2).
