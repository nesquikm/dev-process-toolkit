---
milestone: M2
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M2: Light-Touch Skills

**Goal:** Enhance 3 small, isolated skills. Each change is self-contained.
**Prerequisites:** None (risk scan categories are self-contained; Schema A is defined in NFR-4, not created by another milestone)

**Files to edit:**
- `skills/visual-check/SKILL.md` — FR-9
- `skills/spec-write/SKILL.md` — FR-10
- `skills/spec-review/SKILL.md` — FR-7

**Tasks:**
1. Add `### 0. Check MCP availability` to `visual-check/SKILL.md` before step 1: detection method (attempt `list_ducks` or check config), error message with literal `mcp-rubber-duck is not configured` and setup link, `### Manual Verification Checklist` with 5+ items (layout, responsiveness, accessibility, console errors, visual regressions), pass/fail summary using Schema E format (AC-9.1 through AC-9.5)
2. Replace risk scan content in `spec-write/SKILL.md` step 6 with structured version: table with columns `Category`, `Risk`, `Severity`, `Mitigation`; exactly 6 categories; 3 severity levels; high-severity blocking instruction (AC-10.1 through AC-10.4)
3. Add traceability map step to `spec-review/SKILL.md` between step 2 (scan) and step 3 (report): use Schema A format (`AC-X.Y → file:line`), `(not found)` marker, `potential drift` label per Schema B (AC-7.1 through AC-7.4)

**Acceptance Criteria:**
- [x] AC-9.1: `### 0. Check MCP availability` exists before step 1
- [x] AC-9.2: Contains concrete detection method
- [x] AC-9.3: Contains literal `mcp-rubber-duck is not configured` + setup link
- [x] AC-9.4: `### Manual Verification Checklist` with 5+ items
- [x] AC-9.5: Fallback uses Schema E checkmark format
- [x] AC-10.1: Risk scan table with 4 columns: Category, Risk, Severity, Mitigation
- [x] AC-10.2: Exactly 6 categories listed
- [x] AC-10.3: 3 severity levels: high, medium, low
- [x] AC-10.4: Literal high-severity blocking instruction present
- [x] AC-7.1: Traceability map step exists between scan and report
- [x] AC-7.2: Uses Schema A format `AC-X.Y → file:line`
- [x] AC-7.3: Uses literal `(not found)` for missing implementations
- [x] AC-7.4: Uses literal `potential drift` label

**Gate:** Tier 1 static verification + Tier 2 before/after for FR-9 (MCP absent), FR-7 (partial implementation). Line counts: visual-check ~98, spec-write ~159, spec-review ~53.
