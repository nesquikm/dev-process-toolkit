---
milestone: M9
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M9: Dead Branches

**Target release:** v1.12.0 "Dead Branches"

**Depends on:** M8 (shipped) — M9 rewrites `implement/SKILL.md` Phase 3 Stage B. M8 added the post-archive drift check subsection to `implement/SKILL.md` Phase 4; that content must be stable before Phase 3 is rewritten and the total file length is re-measured against NFR-1. See FR-22 for full acceptance criteria.

**Goal.** Wire `code-reviewer` subagent into `/implement` Phase 3 Stage B via explicit `Agent`-tool invocation; delete unused `test-writer` agent; consolidate review rubric to a single canonical home in `code-reviewer.md`; drop `implement/SKILL.md` below 240 lines to buffer NFR-1.

**Task order** (strictly serialized — most tasks touch `implement/SKILL.md` or `code-reviewer.md`):

1. **Move stack-specific checklist** — Extract stack-specific review examples from `implement/SKILL.md:171-176` into `code-reviewer.md` (or `docs/implement-reference.md`). Resolves AC-22.3.

2. **Strip spec compliance from code-reviewer** — Delete `code-reviewer.md:34-40` (the Spec Compliance section). Resolves AC-22.5.

3. **Rewrite Stage B as delegation** — Rewrite `implement/SKILL.md` Phase 3 Stage B (~lines 162-179) to invoke `code-reviewer` via explicit `Agent`-tool call. Include exact prompt template, return shape expectation, and integration logic for the Stage B pass/fail decision. Resolves AC-22.2 and contributes to AC-22.9.

4. **Update gate-check to reference canonical rubric** — In `gate-check/SKILL.md` Code Review section (~lines 33-53), replace the inline rubric with a pointer to `code-reviewer.md`. Keep review execution inline (gate verdict must return in one turn). Resolves AC-22.4.

5. **Align simplify wording** — Review `simplify/SKILL.md` rubric against `code-reviewer.md`; fix any contradictory phrasing. No delegation conversion. Resolves AC-22.6.

6. **Delete test-writer** — Delete `plugins/dev-process-toolkit/agents/test-writer.md`. Resolves AC-22.1.

7. **Update adaptation-guide** — Rewrite `docs/adaptation-guide.md` Step 6 (~lines 109-121). Resolves AC-22.7.

8. **Update skill-anatomy example** — Add concrete `Agent`-tool invocation example to `docs/skill-anatomy.md` Subagent Execution section, using `/implement` Phase 3 Stage B as reference. Label `context: fork` example as unexercised alternative. Resolves AC-22.8.

9. **Verify NFR-1 safety margin** — Run `wc -l plugins/dev-process-toolkit/skills/implement/SKILL.md`. Must return under 240. Resolves AC-22.9.

10. **Version bump + CHANGELOG** — Bump `plugin.json` and `marketplace.json` to 1.12.0. Add `CHANGELOG.md` `## [1.12.0] — YYYY-MM-DD — "Dead Branches"` entry with `### Added` / `### Changed` / `### Removed` subsections cross-referencing FR-22. Resolves AC-22.10.

11. **Dogfood validation** — Run `/dev-process-toolkit:implement` against a tiny sample change in a scratch project to confirm the `Agent`-tool invocation of `code-reviewer` actually returns usable findings and Stage B correctly integrates them. If the invocation fails, return to task 3 and refine the prompt template. Must pass before committing the version bump from task 10.

**Gate commands** (Markdown-only plugin — no compile/test runtime):

- `wc -l plugins/dev-process-toolkit/skills/implement/SKILL.md` — must be under 240
- Grep for `test-writer` across `plugins/` — zero matches in skills, agents, or docs (CHANGELOG is exempt)
- Version strings must match across `plugin.json`, `marketplace.json`, and `CHANGELOG.md`
- Dogfood run from task 11 must return a valid `code-reviewer` response
