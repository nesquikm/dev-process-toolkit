---
status: archived
archived_at: 2026-05-21T14:22:36Z
---

# M85 — /conformance-loop spawn snippet fix {#M85}

Single-FR milestone closing the Phase A spawn snippet shape regression in `.claude/skills/conformance-loop/SKILL.md`. The broken bare-arg form (`claude -p /smoke-test --tracker linear ...`) shipped since STE-224 (M57, commit `a33d935`, 2026-05-05) and was caught by a live `/conformance-loop --jira-project DST` run on 2026-05-21 — both Phase A children fast-failed with `error: unknown option '--tracker'` before any tracker writes. Fix: wrap slash command + skill args in a single quoted positional (matches STE-185 canonical form + Phase B `/implement` line 298).

Scope deliberately narrow per operator selection at /spec-write time: spec fix only, no new test, no new gate-check probe.

## FRs

| FR id   | Title                                                                                          | Status |
|---------|------------------------------------------------------------------------------------------------|--------|
| STE-325 | fix /conformance-loop Phase A spawn snippet — wrap slash-cmd+args in single quoted positional   | active |

## Release Target

v2.30.0 (provisional, minor). Codename TBD; decide at `/ship-milestone`.

## Dependency Order

Single FR; no ordering required.

## Out of Scope (deferred)

- Real-CLI smoke probe for spawn-shape regressions — the existing `conformance-loop-dry-run.test.ts` mocks the subprocess and cannot catch argv-shape bugs. Deferred to a future FR.
- /gate-check static probe for bare-arg `claude -p /smoke-test --flag` form in SKILL.md bash blocks. Deferred to a future FR.
