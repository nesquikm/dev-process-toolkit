---
milestone: M94
status: archived
archived_at: 2026-07-02T11:55:59Z
kickoff_branch: null
frozen_at: null
shipped_in: v2.35.0
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M94: conformance-loop allow-list fix {#M94}

**Goal:** `/conformance-loop` false-greens because the tracked `.claude/settings.json` `permissions.allow` omits the `claude -p` child-spawn pattern — under headless auto-mode the nested `claude -p /setup` spawn falls to the auto-mode safety classifier, which denies it → 0-byte grandchild → empty findings reported as green (confirmed empirically 2026-07-02: probe + live-session both classifier-denied; remedy per the classifier itself is "add a Bash permission rule"). Regression from STE-252 (bypassPermissions → tracked allow-list, which never enumerated the one command the whole skill runs on). Fix: authorize the spawn in every governing allow-list (STE-350), fence the omission so it can't recur (STE-351), and detect the symptom class at runtime (STE-352). No scheduler — the loop runs **on demand, headless or interactive, on the operator's subscription**.

**Prerequisites:** Builds on STE-252 (the tracked `permissions.allow` allow-list + Phase 0 pre-flight (f)), the `/gate-check` structural-probe harness, and Phase 8's `parseStreamJsonTranscript` (`adapters/_shared/src/socratic_first_turn.ts`). No new external dependencies.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-350 | Allow-list the `claude -p` child-spawn so nested spawns aren't classifier-denied | linear:`STE-350` |
| STE-351 | Pre-flight asserts the allow-list contains the spawn pattern + `ANTHROPIC_API_KEY` guard | linear:`STE-351` |
| STE-352 | stream-json capture + non-empty-spawn assertion to detect the 0-byte-grandchild false-green | linear:`STE-352` |

**Dependency order:** STE-350 first — it lands the canonical spawn-pattern literal and the export-vs-prefix convention. Then STE-351 (fence: pre-flight + probe assert that literal is present) and STE-352 (detector: runtime non-empty/denial assertion) proceed in parallel; both consume STE-350's literal but not each other.

**Tasks:**

- [x] STE-350 — Resolve the env-prefix match (recommended: export `CLAUDE_CONFIG_DIR` once per spawn block, bare `claude -p …`), add the `claude` spawn pattern to `.claude/settings.json`, the `/smoke-test` Phase 1 scaffold heredoc, and `templates/settings.json` (if it carries the canonical list). — STE-350
  verify: meta-test asserts `.claude/settings.json` `permissions.allow` contains the pattern and it matches a fixture of the exact spawn command; scaffold allow-list == canonical list.
- [x] STE-350 — Update the `/conformance-loop` Phase A + `/smoke-test` Phase 2 spawn blocks to the chosen convention so the pattern actually matches at runtime. — STE-350
  verify: grep/AST meta-test asserts spawn blocks export `CLAUDE_CONFIG_DIR` and invoke a bare `claude -p` (no inline env prefix on the `claude` token).
- [x] STE-351 — Strengthen Phase 0 pre-flight (f) to a contains-check for the spawn pattern (NFR-10 refusal on miss) + mirror in the delegated `/smoke-test` pre-flight; log `spawn_pattern_allow_present` on the hit path. — STE-351
  verify: `bun test` unit — refuse (NFR-10 shape) on a fixture allow-list without the pattern; pass + emit token when present.
- [x] STE-351 — Add the `/gate-check` structural probe (fail GATE if the pattern is absent from `.claude/settings.json` or the scaffold snippet) + the `ANTHROPIC_API_KEY` / `ANTHROPIC_AUTH_TOKEN` refuse-if-set Phase 0 guard. — STE-351
  verify: probe test fails without pattern / passes with it; env-set → refuse, unset → proceed (both vars).
- [x] STE-352 — Switch canonical-chain child capture to `--output-format stream-json` and parse via `parseStreamJsonTranscript` so per-probe rows + `tdd-result` fences are visible. — STE-352
  verify: `bun test` — `parseStreamJsonTranscript` over a captured stream-json fixture yields the expected rows/fences.
- [x] STE-352 — Add the non-empty/`permission_denials`-for-`claude` assertion (0-byte or denied spawn ⇒ high-severity finding) + a Phase 2.X regression fixture reproducing a nested spawn under the allow-list. — STE-352
  verify: `bun test` — 0-byte log ⇒ finding; `permission_denials` for `claude` ⇒ finding; healthy log ⇒ pass.

**Tests:**
- Meta-tests under `tests/` — allow-list membership + scaffold equality + spawn-block convention (STE-350); pre-flight contains-check + gate-check probe (STE-351).
- Unit — `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` guard (STE-351); `parseStreamJsonTranscript` fixture + non-empty/denial assertion (STE-352).
- Runtime (smoke-validated; may ship `[~]` pending a live run) — headless nested spawn completes non-empty under the patched allow-list (STE-350.3 / STE-352.3).

**Acceptance Criteria:**
- [x] AC-STE-350.1–.3 (toolkit allow-list carries the pattern; scaffold matches; spawn-form/pattern consistent).
- [x] AC-STE-351.1–.4 (pre-flight contains-check; gate-check probe; `ANTHROPIC_API_KEY` guard; hit-path token).
- [x] AC-STE-352.1–.3 (stream-json capture; non-empty/denial assertion; Phase 2.X regression fixture).

**Gate:** `bun test` GREEN (full gate from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN on this repo.

**Release target:** v2.35.0 (provisional, minor — FR1 fixes the loop, FR2/FR3 add pre-flight + probe + capture behavior). M92 (`chore/ste-344`) and M93 (`feat/m93`) are unshipped on sibling branches; the final version resolves at `/ship-milestone` per the release-collision guard. Codename "Authorized" TBD at ship.
