---
milestone: M115
status: archived
archived_at: 2026-07-26T08:41:45Z
kickoff_branch: feat/m115-tracker-less-milestone-ulid-ids
frozen_at: null
migration: none
shipped_in: v2.56.0
---

# Implementation Plan

## M115 — Tracker-less milestone identity minting {#M115}

**Goal:** Close the cross-branch milestone-number allocation race in `mode: none` by construction. `/spec-write` stops picking a sequential `M<N>` in tracker-less mode and instead mints an opaque `M_<short-ULID>` from `Provider.mintId()`, bypassing `nextFreeMilestoneNumber` exactly as the Jira Epic-first path does. Reverses STE-377's deliberate tracker-less carve-out on the grounds that its premise — no key to claim from — is false. Linear and Jira allocation stay byte-unchanged; existing `M<N>` plans coexist with no migration.

**Prerequisites:** none. The union milestone-token grammar (STE-376) already accepts `M_<key>`, `mintId()` / `mintUniqueId` already exist and are offline-safe, and `/ship-milestone` already treats `M_<key>` as a valid milestone token. This milestone adds a producer, not a grammar.

**Release target:** TBD (single-FR milestone; M112/M113/M114 are also unshipped and may land first — re-bump at ship time).

**Migration:** `none` — existing `M<N>` plan files keep resolving, archiving and shipping unchanged, so no consumer artifact needs rewriting and no registry entry is required. The change is forward-only: it governs milestones created after adoption.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-417 | Mint opaque ULID-derived milestone ids in tracker-less mode | linear:`STE-417` |

**Tasks:**

- [x] Add `milestoneIdFromUlid(ulid)` to `adapters/_shared/src/milestone_token.ts` beside `milestoneIdFromEpicKey` — returns `M_${ulid.slice(23, 29)}`, throws on `ULID_REGEX` miss or malformed derived id — STE-417
  - verify: `bun test` for the new unit cases green (charset, round-trip through `parseMilestoneToken`, both throw paths)
- [x] Route milestone minting through `mintUniqueId({ exists })` with a synchronous predicate checking `specs/plan/M_<tail>.md` and `specs/plan/archive/M_<tail>.md` — STE-417
  - verify: seeded-ULID test (`DPT_TEST_ULID_SEED`) forces a first-mint collision; second mint is used and the pre-existing plan file is byte-unchanged
- [x] Add the `mode: none` minted branch to `/spec-write` SKILL § plan.md milestone-allocation guard; narrow the "Linear and `mode: none` keep the sequential five-way scan" sentence to Linear only — STE-417
  - verify: grep-shape meta-test asserts the minted branch prose + `nextFreeMilestoneNumber` bypass clause + Linear/Jira-unchanged clause present, and the old both-modes sentence absent
- [x] Add mode-conditional plan-frontmatter `id:` — document in `templates/spec-templates/plan.md.template`, enforce with a NEW sibling probe (#73) walking `specs/plan/**` bidirectionally; do NOT widen `identity_mode_conditional.ts` (its FR-only scope-isolation boundary is deliberate) — STE-417
  - verify: probe unit + gate-check test green in both directions (mode-none plan missing `id:` fails; tracker-mode plan carrying `id:` fails); probe registered in `skills/gate-check/SKILL.md` and in the `docs/layout-reference.md` enforcing-probe list
- [x] Re-scope ONLY the AC-STE-377.4 block in `adapters/_shared/src/epic_first_allocation.test.ts` to Linear-only + assert the minted path does NOT call `nextFreeMilestoneNumber`; leave `next_free_milestone_number.test.ts` (AC-STE-376.3 coexistence) untouched; document tracker-less milestone identity in `docs/layout-reference.md` — STE-417
  - verify: re-scoped regression test green; new assertion proves the allocator is uncalled on the minted path; `next_free_milestone_number.test.ts` unchanged in the diff
- [x] Full gate green — STE-417
  - verify: `bun test` from `plugins/dev-process-toolkit` green (~5163+ tests, incl. the STE-376 union-matcher consumer audit unchanged)
