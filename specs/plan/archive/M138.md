---
milestone: M138
status: archived
archived_at: 2026-09-02T14:34:52Z
kickoff_branch: null
frozen_at: null
migration: none
shipped_in: v2.75.1
---

# Implementation Plan

## M138 — Bootstrap Identity {#M138}

**Goal:** A tracker-less project's bootstrap plan carries the identity its own mode uses, from the moment `/setup` writes it.

**Prerequisites:** None.

**Release target:** v2.75.1 (provisional, patch — both FRs are Fixed).

Observed 2026-09-02: `/setup` with no arguments on a fresh Flutter project produced `specs/plan/M1.md`. Step 8 writes that filename in every mode, and `mode: none` names its plans `M_<short-ULID>`. Probe #73 exempts `kind: scaffolding`, so nothing reds; the mismatch waits for `/spec-write` to consume it.

STE-481 already rejected "do not scaffold under `mode: none`" — the scaffold's pre-filled paths, gate commands and live `<scaffolding>` FR row are read downstream by `plan_only_archival` and `tracker_local_reconciliation_drift`. Minting at `/setup` keeps every one of those and fixes the name, so that rejection stands unchallenged.

Repairing existing bootstrapped projects is out of scope by operator decision (2026-09-02).

### FRs

| FR | Title | Tracker |
|----|-------|---------|
| STE-537 | /setup mints the mode:none bootstrap scaffold identity | linear:`STE-537` |
| STE-538 | Adopt the scaffold's minted id instead of minting a second one | linear:`STE-538` |

### Tasks

- [x] STE-537 — mint the identity in `/setup` step 8 on the tracker-less branch only
  verify: a mode-none run writes `specs/plan/M_<tail>.md`; linear and jira runs still write `M1.md`
- [x] STE-537 — record the minted `id:` and `kind: scaffolding` in the scaffold's frontmatter
  verify: `parseFrontmatter` reads both keys back off the written file
- [x] STE-537 — reword the STE-189 class-(5) inventory sentence to name both shapes
  verify: a test greps the sentence and fails on an unconditional `specs/plan/M1.md` claim
- [x] STE-537 — prove the scaffold passes probe #73 without its exemption
  verify: the probe runs with `scaffolding` removed from the exempt list and reports zero violations
- [x] STE-538 — add `adoptOrMintMilestoneId` beside the minter and route the `none` branch through it
  verify: an eligible scaffold yields its own recorded id; no scaffold mints fresh
- [x] STE-538 — make consumption a frontmatter edit when the identity was adopted
  verify: `from` equals `to`, the body is byte-identical, and `id:` is unchanged
- [x] STE-538 — pin the single mint as a call count
  verify: a double records exactly one `mintMilestoneId` call across `/setup` then `/spec-write`
- [x] STE-538 — test STE-481's two recorded-but-untested refusals
  verify: the multi-scaffold refusal and the no-frontmatter path each assert their own message
- [x] STE-538 — resolve the `findScaffoldPlan` / probe #73 filename-filter asymmetry
  verify: a scaffold whose name is off the canonical shape is seen by both, or the gap is recorded

### Gate

`cd plugins/dev-process-toolkit && bun test`

### Dependency graph

STE-537 precedes STE-538: adoption has nothing to adopt until `/setup` records an identity on the scaffold. Both land before M139 touches the same dispatcher.

### Follow-ups carried into M138

None.
