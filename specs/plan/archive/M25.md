---
milestone: M25
status: archived
archived_at: 2026-04-24T21:20:14Z
kickoff_branch: null
frozen_at: null
---

# M25 — Stale Refs {#M25}

**Release target:** v1.27.0. Codename TBD.
**Status:** drafted 2026-04-25 from PR #4 ship-readiness audit (`/tmp/pr4-audit-2026-04-24.md`, iterations 1–5).
**Origin:** PR #4 audit surfaced 2 true merge blockers + ~30 cleanup items; validated ~99% accurate via 4 parallel agents on 2026-04-24. M25 and M26 split the cleanup. M25 is the fast mechanical pass (visible-staleness fixes a cold reviewer notices first); M26 "Dead Migration" is the deletion-heavy follow-up.

## Why this milestone exists

Six visible-staleness items:

1. `CLAUDE.md:15` enumerates "12 slash commands" — actual is 14 (`/docs` + `/ship-milestone` omitted).
2. `docs/skill-anatomy.md:179` pins "As of v1.22.0" and says "0 of 12 skills" — both stale.
3. `docs/adaptation-guide.md:111` pins "As of v1.12.0" — 14 releases stale.
4. `specs/plan/archive/M24.md` carries stale metadata (`Release target: v1.25.0`, `Codename TBD`, `status: active`) despite M24 shipping as v1.26.0 "Symmetry".
5. README Quickstart/Workflow sections don't advertise `/docs` + `/ship-milestone`.
6. 18 of 24 archived plan files carry non-canonical `status:` (4 `active`, 12 `complete`, 2 `draft`) instead of `archived`. `/implement` Phase 4 prose doesn't flip plan status on close, so future milestones re-drift.

"No external users yet" frame lets us sweep without backwards-compat ceremony. Deletion of dead migration code (audit D1–D7) is M26's scope, not M25's.

## FR list (5 FRs, `status: active`)

| FR | Title | Tracker | Phase |
|----|-------|---------|-------|
| `STE-88` | CLAUDE.md skill enumeration fix | linear:`STE-88` | A |
| `STE-89` | Stale version pin sweep in docs/ | linear:`STE-89` | A |
| `STE-90` | M24.md archived-plan metadata refresh | linear:`STE-90` | A |
| `STE-91` | README workflow advertisement for /docs + /ship-milestone | linear:`STE-91` | A |
| `STE-92` | Archive plan-status normalization + probe #16 | linear:`STE-92` | A |

Ship as one PR with five commits.

## Implementation order

**Phase A — `STE-88` → `STE-92`:**

All five FRs independent. Suggested commit order:

1. `STE-88`: `CLAUDE.md:15` — drop skill-name enumeration, match README's "14 skills (slash commands)" phrasing. Pure prose edit.
2. `STE-89`: Rewrite `docs/skill-anatomy.md:179` and `docs/adaptation-guide.md:111` to final-state prose with no version pin or count.
3. `STE-90`: Flip `specs/plan/archive/M24.md` — `status: archived`, set `archived_at`, "Release target: v1.26.0", "Codename: Symmetry", "Status: shipped 2026-04-24".
4. `STE-91`: Add `/docs` + `/ship-milestone` to README Quickstart table + Workflow section.
5. `STE-92`: Flip all 18 non-`archived` plan-file statuses in `specs/plan/archive/*.md` to `status: archived`; set `archived_at` from `git log -1 --format=%cI <file>`. Add plan-status flip line to `skills/implement/SKILL.md:236` Phase 4 atomic-commit prose. Add `/gate-check` probe #16 ("archive plan-status invariant") + `tests/gate-check-archive-plan-status.test.ts`.

- **Ships as PR #1.** `chore(m25): stale refs cleanup (STE-88, STE-89, STE-90, STE-91, STE-92)`.

**Phase B — Release:**

`/ship-milestone M25` → v1.27.0, codename TBD.

## Gate checks

- After Phase A: `/implement M25` regression passes; probe #16 fires correctly on positive + negative fixtures; all existing 15 probes continue passing byte-identically; `bun test` green (expect +N tests from probe #16).
- After Phase B: `/ship-milestone M25` runs clean. Dogfood — probe #16 would have caught the M20/M21/M23/M24 drift at `/gate-check` time.

## Dependencies

No prep chore; M24 shipped as v1.26.0 (2026-04-24). No other dependencies — all five FRs are independent prose/metadata edits.

## Risk scan

| Category | Risk | Severity | Mitigation |
|----------|------|----------|------------|
| Silent-leap prevention | Probe #16 fires retroactively on any pre-M25 archived plan carrying non-`archived` status | low | `STE-92` backfills all 18 plans in the same commit that adds the probe — probe starts green. |
| Vocabulary ambiguity | Some plans carry `status: complete` with possibly-meaningful ship semantics | low | Every `specs/plan/archive/*.md` represents a shipped milestone — there is no "removed but unshipped" case. `archived` is the canonical value; rename is semantically safe. |
| Phase 4 prose change | Adding plan-status flip to `/implement` Phase 4 could destabilize the atomic-commit contract | low | Change is additive — one line after the FR status-flip loop. Phase 4 test (`tests/implement-phase4-close.test.ts`) updated in the same FR. |
| Timestamp backfill | Setting `archived_at` on 18 plans retroactively = fabricating timestamps | low | Use `git log -1 --format=%cI <file>` per file — that's when the archive move actually happened. Honest rather than invented. |

No high-severity risks.

## Cross-spec additions

- **`requirements.md`:** no new NFRs.
- **`technical-spec.md`:** no interface changes.
- **`testing-spec.md`:** new test file `gate-check-archive-plan-status.test.ts`; no new fixture directories.

## Deferred decisions

1. **Codename.** Decide at ship time.
2. **M26 sequencing.** M25 ships before M26 "Dead Migration" begins. If batched into one PR later, plan files merge trivially.
