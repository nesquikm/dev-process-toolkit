---
milestone: M1
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M1: Templates & Docs

**Goal:** Golden path workflows, enhanced spec templates, CI starter configs, and adaptation guide updates. Zero risk to existing skill behavior — only templates, docs, and example files.
**Prerequisites:** None

**Files to edit:**
- `templates/CLAUDE.md.template` — FR-13 (workflows section)
- `templates/spec-templates/requirements.md.template` — FR-14 (security, NFRs, negative ACs)
- `templates/spec-templates/technical-spec.md.template` — FR-14 (ADR section)
- `templates/spec-templates/plan.md.template` — FR-14 (ADAPT markers)
- `templates/spec-templates/testing-spec.md.template` — FR-14 (ADAPT markers)
- `docs/adaptation-guide.md` — FR-3 (CI/CD Parity section)

**Files to create:**
- `examples/typescript-node/.github/workflows/gate-check.yml` — FR-3
- `examples/python/.github/workflows/gate-check.yml` — FR-3
- `examples/flutter-dart/.github/workflows/gate-check.yml` — FR-3

**Tasks:**
1. Add `## Workflows` section to `CLAUDE.md.template` after `## Key Commands` with 3 named paths: Bugfix (`/debug → /implement → /gate-check → /pr`), Feature (`/brainstorm → /spec-write → /implement → /spec-review → /gate-check → /pr`), Refactor (`/implement → /simplify → /gate-check → /pr`) — verify all skill names exist in `skills/` (AC-13.1, AC-13.2, AC-13.3)
2. Add `## Security / Abuse Cases` section to `requirements.md.template` with 2 example entries showing attacker motivation + mitigation (AC-14.1)
3. Add measurable constraint examples to NFR section in `requirements.md.template`: include units like `p95 latency < 200ms`, `availability >= 99.9%` (AC-14.2)
4. Add 2+ negative acceptance criteria examples using `must NOT...` pattern to `requirements.md.template` (AC-14.3)
5. Add `## Architecture Decision Records` section to `technical-spec.md.template` with table: `Decision`, `Options Considered`, `Choice`, `Rationale` (AC-14.4)
6. Enhance `<!-- ADAPT -->` markers across all 4 spec templates with 2+ stack-specific examples each (AC-14.5)
7. Add `## CI/CD Parity` section to `docs/adaptation-guide.md` explaining how to sync gate-check with CI (AC-3.3)
8. Create `.github/workflows/gate-check.yml` in each of `examples/typescript-node/`, `examples/python/`, `examples/flutter-dart/` with working GitHub Actions configs (AC-3.4, AC-3.5, AC-3.6)

**Acceptance Criteria:**
- [x] AC-13.1: CLAUDE.md.template has `## Workflows` after `## Key Commands`
- [x] AC-13.2: 3 named paths with arrow notation
- [x] AC-13.3: All skill names in paths exist in skills/
- [x] AC-14.1: requirements.md.template has `## Security / Abuse Cases` with 2+ examples
- [x] AC-14.2: NFR section has measurable constraints with units
- [x] AC-14.3: 2+ negative ACs with `must NOT...` pattern
- [x] AC-14.4: technical-spec.md.template has `## Architecture Decision Records` with table
- [x] AC-14.5: All 4 templates have ADAPT markers with 2+ stack examples
- [x] AC-3.3: Adaptation guide has `## CI/CD Parity` section
- [x] AC-3.4: `examples/typescript-node/.github/workflows/gate-check.yml` exists
- [x] AC-3.5: `examples/python/.github/workflows/gate-check.yml` exists
- [x] AC-3.6: `examples/flutter-dart/.github/workflows/gate-check.yml` exists

**Gate:** Tier 1 static verification — Markdown syntax, internal references, ADAPT marker count, line counts
