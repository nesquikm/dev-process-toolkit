---
milestone: M_0c14d0
status: archived
archived_at: 2026-09-17T11:18:49Z
kickoff_branch: null
frozen_at: null
migration: none
shipped_in: v2.85.0
---

# Implementation Plan

## M_0c14d0 — Design References That Get Read {#M_0c14d0}

**Goal:** The design images an author pastes during design reach the person — or skill — building the feature. Phase 4b" hands the resolved check skill the references bound to the FR in scope, each with its caption, through one shared renderer; and the archive stops claiming this shipped in M93.

**Prerequisites:** Ships SECOND, after M_862e04 (v2.84.1) — that milestone's STE-600 is what turns the gate green. Independent of M_f1f569 in content; both ship serially, never in parallel.

**Release target:** v2.85.0 — a minor bump, because the single FR is Added. Verify with `inferBump` against the FR frontmatter and CHANGELOG rather than trusting this line.

### FRs

| FR | Title | Tracker |
|----|-------|---------|
| STE-596 | Design references reach the project's verification step | linear:`STE-596` |

**Tasks:**

- [x] STE-596 — Widen `DesignReferenceRow` with the caption, reusing the sibling row's existing caption parsing rather than a second reader. — STE-596
  verify: one `toEqual` on a whole row asserts the caption, and the assertion fails on the pre-change bytes.
- [x] STE-596 — Add the shared renderer turning FR-scoped rows into the canonical block, unresolved paths rendered as skipped. — STE-596
  verify: renderer unit test covers a resolving row, an unresolved row, and the empty list.
- [x] STE-596 — Filter rows by the FR's own spec path and thread the rendered block into the Phase 4b″ check-skill invocation. — STE-596
  verify: an FR citing nothing invokes the skill byte-identically to today; an FR citing two images passes both.
- [x] STE-596 — Render the same block on the `manual` reminder path. — STE-596
  verify: a manual-mode run names the references in its reminder.
- [x] STE-596 — Register the capability token pair and its plain-language map entry. — STE-596
  verify: `closing_summary_capability_keys` carries both keys and the section 7 map renders each.
- [x] STE-596 — Correct the archived M93 lineage claim and amend the STE-505 argument surface. — STE-596
  verify: the archive states what M93 shipped, what this ships, and what remains the check skill's job.

### Milestone-level acceptance

| Check | Command | Expected |
|-------|---------|----------|
| Suite | `cd plugins/dev-process-toolkit && bun test` | 0 fail; skips match main's by identity |
| Gate | `/dev-process-toolkit:gate-check` | 0 FAIL |
| One parser only | grep the consumer surface for a second design-references heading parser | zero hits, with a control proving the grep can hit |
| Vacuous path unchanged | an FR citing no references | check-skill invocation byte-identical to pre-change |

### Risks

| Risk | One side | The other side |
|------|----------|----------------|
| Widening the row crosses STE-542's deliberate frozen line | The caption is the half that tells a check skill what it is looking at | The FR justifies the crossing in Notes rather than calling it a repair |
| Field-by-field assertions stay green against an added field | This is the documented way a widening slips past its own tests | AC.4 pins one `toEqual` on the whole row |
| This repo runs `verify_mode: manual`, so the auto-run path is not exercised here | The capability would look shipped while its main path is untested locally | AC.6 makes the manual render a first-class requirement, and the auto path is covered by unit tests |
| Probe counts shift when a new probe or token lands | A token addition has historically moved pinned counts across many test files | Register the tokens in one commit and re-run the full suite before the next task |

<!-- token-stats:begin -->

## Token Stats

| scope | model | input | output | cache-read | cache-creation |
| --- | --- | ---: | ---: | ---: | ---: |
| dev-process-toolkit:gate-check | claude-opus-5 | 196 | 147816 | 31841515 | 2424205 |
| dev-process-toolkit:implement | claude-opus-5 | 184 | 38519 | 12713271 | 238320 |
| dev-process-toolkit:pr | claude-opus-5 | 6 | 1524 | 1333578 | 10260 |
| dev-process-toolkit:ship-milestone | claude-opus-5 | 46 | 22422 | 9728201 | 62269 |
| dev-process-toolkit:spec-review | claude-opus-5 | 8 | 2469 | 1801617 | 11627 |
| dev-process-toolkit:tdd | claude-opus-5 | 14 | 6193 | 1271991 | 21552 |
| (main-loop) | claude-opus-5 | 500 | 174709 | 74178981 | 373731 |
| total |  | 954 | 393652 | 132869154 | 3141964 |

<!-- token-stats:end -->
