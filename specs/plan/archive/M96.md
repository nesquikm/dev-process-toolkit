---
milestone: M96
status: archived
archived_at: 2026-07-03T10:28:51Z
kickoff_branch: chore/m96-driver-wait-discipline
frozen_at: null
shipped_in: v2.37.0
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M96: smoke-driver wait discipline {#M96}

**Goal:** the 2026-07-02 iter-2 conformance run proved every M95 mechanism fires (scoped cleanup, detached spawns + pidfiles, trust seeding, detectors) but the loop still cannot run green: both drivers fire-and-exit at the WAIT step (F1), the Phase 0.5 wipe self-reports PASS without executing so stale logs can false-pass completeness checks (F2), orphaned grandchildren have nondeterministic fates (F3), and the first healthy grandchild run surfaced two plugin-side defects — the tdd pre-commit hook blocks /setup's own placeholder deliverable (F4) and the Jira status-vocabulary fetch is company-managed-only (F5). Fix all six findings (F6 rides as a doc note in STE-358) so a `/conformance-loop` iteration completes with both legs green.

**Prerequisites:** M95 (v2.36.0 "Integrity") — the detached-spawn + pidfile mechanics, `assertChainIntegrity`, and trust seeding this milestone builds on. No new external dependencies.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-357 | Enforce the wait discipline — bounded multi-iteration polls, no turn-end with live pidfiles | linear:`STE-357` |
| STE-358 | Verified Phase 0.5 wipe + freshness-gated chain-completeness checks | linear:`STE-358` |
| STE-359 | Orphan adoption for surviving grandchildren | linear:`STE-359` |
| STE-360 | Exempt the /setup Bun zero-match placeholder from pre-commit-tdd-orchestrator | linear:`STE-360` |
| STE-361 | adapters/jira.md — transitions API as primary status-vocabulary path for team-managed projects | linear:`STE-361` |

**Dependency order:** STE-357 → STE-358 → STE-359 sequential (all three edit the two driver SKILL.mds; STE-359's adoption block reuses STE-357's bounded-poll fence). STE-360 and STE-361 are independent plugin-side FRs and may land in any order after the driver trio.

**Tasks:**

- [x] STE-357 — Upgrade both poll fences to bounded multi-iteration loops, add the NOT-license red-flag prose, add the final-message pidfile self-check. — STE-357
  verify: meta-tests pin the multi-iteration shape + warning + self-check in both SKILL.mds.
- [x] STE-358 — Widen the Phase 0.5 wipe + post-rm ls verification; add `runStart` freshness gating to `assertChainIntegrity` + wire both prose sites; add the F6 no-child-side-merge sentence. — STE-358
  verify: `bun test` unit — stale/fresh fixtures; meta-tests for the wipe fence + both run-start prose sites + step 6 sentence.
- [x] STE-359 — Add the Phase A orphan-adoption block + nondeterminism residual-risk prose in both SKILL.mds. — STE-359
  verify: meta-tests pin the adoption block + both prose sites.
- [x] STE-360 — Exempt the marker-bearing placeholder in pre-commit-tdd-orchestrator.ts + document in /setup. — STE-360
  verify: `bun test` unit — placeholder-only/no-marker/mixed/deletion cases; doc-conformance for /setup prose.
- [x] STE-361 — Rewrite adapters/jira.md status-fetch to the two-path probe order + skill-prose sweep. — STE-361
  verify: jira-md doc-conformance pins the two-path section; meta-test for skill-prose references.

**Tests:**
- Meta-tests under `tests/` — poll-fence shape + NOT-license prose + self-check (STE-357); wipe verification + run-start prose (STE-358); adoption block + residual-risk prose (STE-359); /setup exemption prose (STE-360); jira-md two-path section (STE-361).
- Unit — `assertChainIntegrity` freshness fixtures (STE-358); pre-commit-tdd-orchestrator exemption cases (STE-360).
- Runtime (smoke-validated; ships `[~]` pending a live run) — a `/conformance-loop` iteration completes with both legs green: full six-skill chains, zero truncation findings, zero stale-capture findings.

**Acceptance Criteria:**
- [x] AC-STE-357.1–.3 (multi-iteration polls; NOT-license prose; final-message self-check — runtime half `[~]`).
- [x] AC-STE-358.1–.3 (verified wipe; freshness gating; F6 doc note).
- [x] AC-STE-359.1–.3 (adoption block; residual-risk prose; runtime `[~]`).
- [x] AC-STE-360.1–.2 (hook exemption + tests; /setup prose).
- [x] AC-STE-361.1–.2 (jira.md two-path section; skill-prose sweep).

**Gate:** `bun test` GREEN (full gate from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN on this repo.

**Release target:** v2.37.0 (provisional, minor). M92 (`chore/ste-344`) and M93 (`feat/m93`) remain unshipped on sibling branches; the final version resolves at `/ship-milestone` per the release-collision guard.
