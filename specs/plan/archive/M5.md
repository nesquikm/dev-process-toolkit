---
milestone: M5
status: complete
kickoff_branch: null
frozen_at: null
revision: 1
---

# M5: Setup Doctor + Version Bump

**Goal:** Add doctor validation to setup, render golden paths in setup report, bump version, and verify everything works together.
**Prerequisites:** M1-M4 (all content changes must be complete)

**Files to edit:**
- `skills/setup/SKILL.md` — FR-4, FR-13
- `.claude-plugin/plugin.json` — version bump
- `docs/patterns.md` — update if new patterns introduced
- `docs/sdd-methodology.md` — update if methodology changes

**Tasks:**
1. Add `### 1b. Doctor Validation` to `setup/SKILL.md` after step 1: checklist table with columns `Check`, `How`, `Remediation`; checks for tools, gate commands, CLAUDE.md, settings.json; conditional skip for new projects; pass/fail reporting (AC-4.1 through AC-4.5)
2. Add workflow paths to `setup/SKILL.md` step 11 report: same 3 paths as CLAUDE.md template (Bugfix, Feature, Refactor); verify skill names match (AC-13.4, AC-13.3)
3. Bump version in `.claude-plugin/plugin.json` from `1.7.0` to `1.8.0`
4. Review `docs/patterns.md` and `docs/sdd-methodology.md` — update if new patterns (drift detection, spec breakout, doctor validation) warrant documentation
5. Run full Tier 3 regression verification
6. Verify NFR-4 cross-skill schemas: diff Schema D lists in tdd vs. implement, verify Schema A format in gate-check vs. spec-review, verify Schema F verdict strings in gate-check vs. structured output

**Acceptance Criteria:**
- [x] AC-4.1: `### 1b. Doctor Validation` exists after step 1
- [x] AC-4.2: Checklist table with columns `Check`, `How`, `Remediation`
- [x] AC-4.3: Checks for tools, gate commands, CLAUDE.md, settings.json
- [x] AC-4.4: Literal pass/fail reporting instruction
- [x] AC-4.5: Conditional skip for new projects
- [x] AC-13.4: Setup report contains same named paths (Bugfix, Feature, Refactor) in same order with same skill sequences
- [x] AC-13.3: Workflow paths reference only existing skills (re-verified)
- [x] Plugin version is 1.8.0
- [x] Tier 3 regression passes (setup, implement, gate-check, tdd, code-reviewer)
- [x] No skill file exceeds 300 lines (NFR-1 final check)

**Gate:** Full Tier 1 + Tier 2 + Tier 3 verification. All 61 ACs pass. All line counts verified. NFR-4 cross-skill schema verification: diff Schema D anti-pattern lists in tdd vs. implement (AC-8.4), verify Schema A format in gate-check vs. spec-review, verify Schema F verdict strings in gate-check verdict vs. structured output.
