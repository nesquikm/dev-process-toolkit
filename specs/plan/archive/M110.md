---
milestone: M110
status: archived
archived_at: 2026-07-20T07:05:21Z
kickoff_branch: fix/m110-conformance-hardening
frozen_at: null
migration: none
shipped_in: v2.52.0
---

# Implementation Plan

## M110: Conformance hardening — Crucible {#M110}

**Goal:** Fix the five high-severity shipped-plugin defects the 2026-07-19 `/conformance-loop` run (Linear + Jira, capture-only) surfaced. Two harden the canonical chain's behaviour under `claude -p` (spec-write's milestone gate silently no-ops; report-issue's publish gate fails open), two repair the smoke harness's own detectors (a clean-exit/zero-work false-green no detector caught; the Phase-8 arbiter that can neither pass nor fail correctly), and one fixes a live Linear-adapter regression (an upstream HTML-shape change broke AC-fence stripping, uncaught because the unit suite pinned the old shape).

**Prerequisites:** none — every fix site already ships. STE-401 + STE-402 reuse the STE-226 marker contract + `requireOrRefuse` (STE-232); STE-399 + STE-400 extend the STE-237 / STE-355 smoke-capture helpers; STE-398 is a self-contained regex + coverage fix.

**Release target:** v2.51.0 for STE-398..402; **v2.52.0** for the STE-404 follow-up (F4 from the 2026-07-20 re-run, folded into the reopened milestone). M101 "Lineage" remains unshipped on a sibling branch and must rebase past this.

**Migration:** `none` — this milestone changes plugin surfaces (two adapter helpers, two SKILL bodies, two new gate probes, one capability token), not the artifacts the toolkit manages inside consumer projects. No consumer tree needs rewriting.

**FR list** (build order: A → B → C independent; D → E share the marker/refusal contract):

| FR | Title | Tracker |
|----|-------|---------|
| STE-398 | Widen AC_LINEAR_XML_RE to tolerate Linear's href attribute + href coverage | linear:`STE-398` |
| STE-399 | socratic_first_turn: reject vacuous ok-asked + path-scope scaffolding | linear:`STE-399` |
| STE-400 | smoke-child-capture: liveness detector for clean-exit/zero-work runs | linear:`STE-400` |
| STE-401 | spec-write: route the milestone-allocation gate through the marker/refusal contract | linear:`STE-401` |
| STE-402 | report-issue: gate the gist publish on the marker, not prose | linear:`STE-402` |
| STE-404 | Close the first-turn tracker-create gap (F4 follow-up from the 2026-07-20 re-run) | linear:`STE-404` |

**Tasks:**

- [x] Widen `AC_LINEAR_XML_RE` in `adapters/linear/src/format_description.ts` from `<issue id="[^"]*">` to an any-attribute form (`<issue\s[^>]*>`) that still requires the `AC-` prefix + `.<N>` suffix scope — STE-398
  - verify: `bun test adapters/linear/src/format_description.test.ts` — strip recovers `AC-STE-203.1:` from an href-carrying wrapper
- [x] Add href-carrying fixtures: strip path (id+href, href+id, extra attrs), bare-reference negative case (href present, no `AC-` prefix ⇒ wrapper kept), round-trip regression — STE-398
  - verify: `bun test adapters/linear/src/format_description.test.ts` — all new cases green; existing cases unchanged
- [x] Add `"vacuous"` to `FirstTurnOutcome`; return it from `assertFirstTurnShape` when the fall-through (no ask/refusal/scaffold) is reached instead of `ok-asked` — STE-399
  - verify: `bun test adapters/_shared/src/socratic_first_turn.test.ts` — a no-ask/no-refusal/no-scaffold transcript returns `vacuous`
- [x] Thread an optional `projectRoot` into `assertFirstTurnShape`; a scaffolding tool_use is a violation only when its `path` resolves inside `projectRoot` (path absent ⇒ treated as in-scope, preserving the no-projectRoot contract) — STE-399
  - verify: `bun test` — scaffold write outside `projectRoot` does not throw; write inside still throws
- [x] Populate each `tool_use` entry's `path` from tool_input in `parseStreamJsonTranscript`; extend `TranscriptEntry` with an optional `path` — STE-399
  - verify: `bun test adapters/_shared/src/socratic_first_turn_stream.test.ts` — a Write event's `file_path` lands on the entry
- [x] `socratic_first_turn_assert` CLI: accept optional `<projectRoot>` arg, print a `vacuous` verdict line and exit non-zero for the vacuous outcome (distinct from `ok-*`/`violation`) — STE-399
  - verify: `bun test` — CLI-level assertion of the vacuous exit code + verdict string
- [x] Add `checkSkillCompletionSignal(ndjson, child, marker)` to `smoke_child_capture.ts`: extract assistant text via `extractAssistantText`, return one high finding when `marker` is absent, `[]` when present — STE-400
  - verify: `bun test tests/smoke-child-capture*.test.ts` (or the colocated `.test.ts`) — marker-present ⇒ [], marker-absent ⇒ 1 finding
- [x] Prove assistant-text scoping: a capture where `marker` appears only in a `user`/tool_result event still returns the finding (F3-class robustness) — STE-400
  - verify: `bun test` — user-event-only marker ⇒ finding; empty capture ⇒ finding
- [x] Route spec-write's milestone-allocation gate through `requireOrRefuse(..., defaultValue: <recommendation>)`; document the marker-present/default-apply + non-tty-refusal branches at the gate site in `skills/spec-write/SKILL.md` — STE-401
  - verify: `bun test tests/gate-check-spec-write-milestone-gate-routed.test.ts` — probe passes on the edited SKILL
- [x] Register `milestone_allocation_default_applied` in `CANONICAL_CAPABILITY_KEYS` + add rendered prose to the /spec-write § 7 static map — STE-401
  - verify: `bun test tests/gate-check-closing-summary-capability-keys.test.ts` — new key resolves; existing keys unchanged
- [x] Add probe #70 `spec_write_milestone_gate_routed` (severity error) + register in `skills/gate-check/SKILL.md`; bump probe-count pins — STE-401
  - verify: `bun test tests/gate-check-spec-write-milestone-gate-routed.test.ts` + the public-surface-count-drift suite green
- [x] Route report-issue's publish gate through the runtime byte-grep + three-branch `requireOrRefuse` contract; add the non-tty-refusal branch + NOT-a-trigger anchor in `skills/report-issue/SKILL.md` — STE-402
  - verify: `bun test tests/gate-check-report-issue-publish-gate-marker.test.ts` — probe passes on the edited SKILL
- [x] Register `report_issue_publish_refused` in `CANONICAL_CAPABILITY_KEYS` + § 7 static map; add probe #71 `report_issue_publish_gate_marker` (error) + register in gate-check SKILL; bump probe-count pins — STE-402
  - verify: `bun test` — new probe + key resolve; public-surface-count-drift green
- [x] Add `TRACKER_CREATE_TOOLS` to `socratic_first_turn.ts`; `assertFirstTurnShape` throws on a tracker-create tool_use before the first ask/refusal (path-agnostic) — STE-404
  - verify: `bun test adapters/_shared/src/socratic_first_turn.test.ts` — createJiraIssue/save_issue before ask throws; after ask ok
- [x] Harden `skills/spec-write/SKILL.md` first-action + Socratic contract: name tracker-create tools forbidden-before-first-ask + the autonomous-reminder escalation (F4) + F2 refusal-only clarification — STE-404
  - verify: `bun test tests/gate-check-spec-write-first-turn-tracker-create-clause.test.ts` — probe passes on the edited SKILL
- [x] Add probe #72 `spec_write_first_turn_tracker_create_clause` (error) + register in gate-check SKILL; bump probe-count pins 71→72 — STE-404
  - verify: `bun test` — new probe green; public-surface-count-drift + calibration tests reflect 72

**Acceptance criteria:** every AC in STE-398..402 passes; full `bun test` from `plugins/dev-process-toolkit` green with zero failures and zero errors; no existing probe regresses; the two new probes (#70, #71) each ship with their `tests/gate-check-<slug>.test.ts` (positive + negative fixture).

**Gate commands:**

```bash
cd plugins/dev-process-toolkit && bun test
```

**Dependency graph:**

```
STE-398 (linear href regex) ── independent
STE-399 (socratic arbiter)  ── independent
STE-400 (liveness detector) ── independent

STE-401 (spec-write gate) ──┐ share the STE-226 marker + requireOrRefuse contract;
STE-402 (report-issue gate)─┘ build D before E so the routing pattern is settled once
```

## Notes

All five FRs originate from the 2026-07-19 `/conformance-loop` run (aggregated report `/tmp/dpt-conformance-loop-2026-07-19-iter-1.md`, 22 raw → 18 deduped findings). This milestone takes only the five HIGH findings that are **shipped-plugin** defects (they ride a plugin reinstall). The project-local smoke-harness findings (F3 capability-key NDJSON grep, F8 group-7 tdd-result, F11/F12/F13/F14/F15/F17/F18) live in `.claude/skills/` and are handled outside this milestone — they do not ride the plugin update.

STE-401 (F1) is the headline: it is why the canonical chain currently needs the smoke driver to pre-bake the milestone answer. STE-402 (F5) is the security-relevant one: an authorization gate on an irreversible external side effect (a public gist) failed open, saved only by the harness classifier.
