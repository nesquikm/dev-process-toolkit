---
milestone: M10
status: complete
kickoff_branch: null
frozen_at: null
revision: 1
---

# M10: Second Look

## M10: Second Look {#M10}

**Goal:** Borrow three patterns from the `superpowers` plugin plus one local tightening, all aimed at hardening the implement → review loop. Headline change is splitting `/implement` Phase 3 Stage B into two sequential `code-reviewer` passes (spec-compliance + code-quality, fail-fast on Pass 1). Supporting changes: rationalization-prevention table in `/gate-check`, new `docs/parallel-execution.md` linked from a new `## Parallelization` subsection in `/implement`, and tightened `plan.md.template` task decomposition.

**Prerequisites:** M9 shipped (Stage B already delegates to `code-reviewer` via `Agent` tool — M10 splits that single invocation into two passes).

**Tasks:**

- [ ] Task 1 — Split Stage B in `implement/SKILL.md` into Pass 1 (Spec Compliance) and Pass 2 (Code Quality); add fail-fast rule and skipped-Pass-2 reporting line
  verify: `wc -l skills/implement/SKILL.md` < 300; grep finds both `### Pass 1: Spec Compliance` and `### Pass 2: Code Quality`; grep finds the literal fail-fast and "Skipped" strings (AC-23.1, AC-23.3, AC-23.4, AC-23.5, AC-23.7, AC-23.8)
- [ ] Task 2 — Add `### Pass-Specific Return Contracts` subsection to `agents/code-reviewer.md` documenting the two prompt shapes
  verify: grep finds the new subsection; both pass shapes use OK / CONCERN line format (AC-23.6)
- [ ] Task 3 — Add `### Rationalization Prevention` table to `gate-check/SKILL.md` Red Flags section
  verify: grep finds the heading inside the existing `## Red Flags` section; table has the 6 AC-24.2 rows in order; `wc -l` < 300 (AC-24.1, AC-24.2, AC-24.4); verdict strings unchanged (AC-24.3)
- [ ] Task 4 — Create `plugins/dev-process-toolkit/docs/parallel-execution.md` with the three required sections, both external links, and the advisory disclaimer
  verify: file exists; exactly 3 top-level sections in order; both URLs present; `wc -l` ≤ 200 (AC-25.1, AC-25.2, AC-25.3, AC-25.4, AC-25.5, AC-25.7, AC-25.8)
- [ ] Task 5 — Add `## Parallelization` subsection to `implement/SKILL.md` placed immediately before Phase 3, with the literal pointer line
  verify: grep finds the heading; literal pointer line present; heading sits between Phase 2 and Phase 3 (AC-25.6); `wc -l` still < 300
- [ ] Task 6 — Update `templates/spec-templates/plan.md.template` with 2-line task shape, `### Task Sizing` section, and anti-pattern callout
  verify: read template; grep finds `### Task Sizing` and the literal `Each task should be ≈ one commit's worth of work`; example tasks render as 2-line entries with `verify:` (AC-26.1, AC-26.2, AC-26.3)
- [ ] Task 7 — Update `spec-write/SKILL.md` to reference the new Task Sizing guidance
  verify: grep finds `Task Sizing` reference in the `plan.md` step or `## Rules` section (AC-26.4)
- [ ] Task 8 — Bump version to 1.13.0 in `plugin.json` and `marketplace.json`; add CHANGELOG entry
  verify: both JSON files read 1.13.0; CHANGELOG has `## [1.13.0] — YYYY-MM-DD — "Second Look"` section with FR-23..26 cross-references
- [ ] Task 9 — Update project `CLAUDE.md` skill count if changed (no new skills in M10, so skill count stays at current value); add `/implement` two-pass note and `/gate-check` rationalization-table mention if relevant
  verify: grep — no stale skill-count strings; new behavior surfaced in CLAUDE.md if user-visible
- [ ] Task 10 — Tier 1 static verification: run all FR-23..26 grep checks from `testing-spec.md` § Tier 1
  verify: every Tier 1 row for FR-23..26 reports the expected match
- [ ] Task 11 — Tier 2 behavioral validation: run the FR-23..26 before/after scenarios on a reference fixture
  verify: each scenario's "After" outcome observed; capture output for spec-archive
- [ ] Task 12 — Tier 3 dogfood: run `/implement` on M10 itself end-to-end through the new two-pass Stage B
  verify: Pass 1 + Pass 2 both fire on a clean change set; Pass 2 skipped on a synthetic spec-drift change; final OVERALL verdict is correct in both cases
- [ ] Task 13 — Drop `disable-model-invocation: true` from `skills/implement/SKILL.md` and `skills/pr/SKILL.md` frontmatter; leave `skills/setup/SKILL.md` unchanged
  verify: grep first 10 lines — `implement` and `pr` show no `disable-model-invocation` line; `setup` still has `disable-model-invocation: true` (AC-27.1, AC-27.2, AC-27.3); `git grep "disable-model-invocation" plugins/dev-process-toolkit/skills/` returns only `setup/SKILL.md` (AC-27.6)
- [ ] Task 14 — Update `docs/skill-anatomy.md` Best Practices #2 (narrow recommendation to bootstrap skills) and update the inline comment on the `disable-model-invocation` row of the frontmatter-fields table
  verify: grep Best Practices section — no longer mentions "commit" or "PR" as triggers for the flag (AC-27.4); table row comment reflects the narrower rule
- [ ] Task 15 — Update `docs/parallel-execution.md` Agent-Teams section to document direct `Skill`-tool invocation of `/implement` from subagents; remove any workaround language about reading `SKILL.md` body manually
  verify: read Agent-Teams section — states `/implement` is invocable via Skill tool; no mention of reading the file body (AC-27.5); `wc -l docs/parallel-execution.md` still ≤ 200
- [ ] Task 16 — Update CHANGELOG `## [1.13.0]` entry to include FR-27 under `### Changed` (drop flag from `/implement` and `/pr`, keep on `/setup`) — amends the Task 8 CHANGELOG entry
  verify: CHANGELOG contains FR-27 cross-reference in the v1.13.0 section

**Acceptance Criteria (milestone-level):**
- All FR-23..26 ACs pass Tier 1 static verification
- All FR-23..26 ACs pass Tier 2 behavioral validation on the reference fixtures
- M10 dogfood (Task 12) completes successfully — `/implement` runs end-to-end through the new two-pass Stage B on M10's own change set
- `wc -l skills/implement/SKILL.md` < 300 (NFR-1)
- `wc -l skills/gate-check/SKILL.md` < 300 (NFR-1)
- `wc -l docs/parallel-execution.md` ≤ 200 (AC-25.7)
- All three release-checklist files updated together (plugin.json, marketplace.json, CHANGELOG.md) per project CLAUDE.md release rule

**Gate:** `/gate-check` passes; M10 dogfood (Task 12) completes; release-checklist trio updated.
