---
milestone: M8
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M8: Residue Scan

**Goal:** Add a post-archive drift check to `/spec-archive` and `/implement` Phase 4 that flags stale content — both orphan identifier references (Pass A grep) and scope-limiting semantic framing (Pass B Claude scan) — as an advisory report. Never auto-rewrites narrative. Addresses the Flutter dogfood case where archiving M1-M4 left `requirements.md` calling the project a "layered documentation set" with M5 code work in flight.
**Prerequisites:** M7 (archival mechanism must exist before drift checks can run after it — M8 is strictly additive and depends on the `## Post-Archive Drift Check` hook point existing in `/spec-archive` and `#### Post-Archive Drift Check` in `/implement` Phase 4's Milestone Archival subsection)

**Files to create:**
- (none — all changes are additions to existing files)

**Files to edit:**
- `plugins/dev-process-toolkit/skills/spec-archive/SKILL.md` — FR-21 (new `## Post-Archive Drift Check` section, ~40 lines)
- `plugins/dev-process-toolkit/skills/implement/SKILL.md` — FR-21 (new `#### Post-Archive Drift Check` subsection in Phase 4 Milestone Archival, ~5 lines pointing to spec-archive)
- `plugins/dev-process-toolkit/docs/patterns.md` — FR-21 (new `### Pattern: Post-Archive Drift Check` section, ~35 lines)
- `plugins/dev-process-toolkit/.claude-plugin/plugin.json` — version bump 1.10.0 → 1.11.0
- `.claude-plugin/marketplace.json` — version bump 1.10.0 → 1.11.0
- `CHANGELOG.md` — new `## [1.11.0] — YYYY-MM-DD — "Residue Scan"` section summarizing FR-21 under `### Added`
- `README.md` — optional: mention drift check under existing `/spec-archive` feature bullet
- `CLAUDE.md` (repo root) — no skill count change (no new skill); optional mention of drift check in Release Checklist section
- `specs/technical-spec.md` — Section 1 directory structure annotation updates for line delta (~+5 lines on implement/SKILL.md, ~+40 on spec-archive/SKILL.md, ~+35 on docs/patterns.md)

**Tasks (dependency order):**
1. **Draft `spec-archive` Post-Archive Drift Check section** (FR-21 foundation) — add `## Post-Archive Drift Check` to `skills/spec-archive/SKILL.md` after the file-modification step and before the final report. Content: (a) one-sentence purpose, (b) Pass A instructions (grep live spec files for `M{N}`, `FR-{N}`, `AC-{N}.` excluding `^> archived:` lines), (c) Pass B instructions (read each live spec with a brief containing archived IDs + archive title/goal, flag narrative whose framing assumes archived scope is the whole project, name the canary pattern), (d) Schema I table render, (e) literal `technical-spec.md` advisory-only rule (AC-21.9), (f) empty-report path with literal `No drift detected` (AC-21.10), (g) 3-choice UX block (inline / save to `specs/drift-{YYYY-MM-DD}.md` / acknowledge) (AC-21.11). Verify heading structure matches AC-21.1. Target: ~40 lines.
2. **Draft `implement` Phase 4 pointer subsection** (FR-21) — add `#### Post-Archive Drift Check` to `skills/implement/SKILL.md` Phase 4's `### Milestone Archival` subsection, placed after the archive move step and before the final Phase 4 report. Content: ~5 lines — one-sentence purpose, pointer to `skills/spec-archive/SKILL.md` § Post-Archive Drift Check, and the context-specific brief template (just-archived ID list + one-paragraph archive file title/goal excerpt for Pass B). DO NOT inline the two-pass logic — keep it DRY by pointing to spec-archive. Verify placement matches AC-21.2. After this task, run `wc -l skills/implement/SKILL.md` and confirm the file is still within the NFR-1 envelope (post-M7 mitigation accounted for).
3. **Docs: `### Pattern: Post-Archive Drift Check`** (FR-21) — add new section to `docs/patterns.md` after `### Pattern: Archival Lifecycle`. Content: (a) the two-pass rationale (determinism + judgment), (b) the Flutter canary example verbatim with the "documentation-only deliverable" framing, (c) why Pass B is load-bearing despite false-positive potential, (d) Pass B prompt construction tips (title+goal only, not full body), (e) accuracy-first tradeoff explanation from the brainstorm. Verify section exists per AC-21.12.
4. **Version bump** — update `plugin.json` and `marketplace.json` from `1.10.0` to `1.11.0` (per release checklist in CLAUDE.md).
5. **CHANGELOG.md entry** — add new `## [1.11.0] — 2026-04-10 — "Residue Scan"` section at top of `CHANGELOG.md`, below the intro, with an `### Added` subsection summarizing FR-21 in 2-3 bullets cross-referencing the FR ID. Reference the Flutter dogfood canary as motivation.
6. **Optional: README and CLAUDE.md touch-ups** — if the `/spec-archive` bullet in README mentions features, add "post-archive drift check" phrasing. No skill count change (no new skill). Skip silently if not relevant.
7. **Manual verification on canary fixture** — craft the Flutter canary fixture (per testing-spec.md §5) with M1-M4 archived and narrative framing "layered documentation set" still in Overview. Run `/spec-archive` on a new archival operation. Verify Pass B flags Overview, Out-of-Scope, Security sections as `medium` severity and produces a Schema I table. Verify user can choose all 3 UX options. This is the load-bearing validation task — FR-21 exists because of this exact scenario.
8. **Tier 1 static verification** — grep all AC-21 literal strings, verify Schema I column count in technical-spec.md, verify heading placement in both spec-archive and implement skills.
9. **Tier 2 behavioral verification** — run the 7 Tier 2 scenarios from testing-spec.md (canary, orphan-token, spotless, implement Phase 4, ADR, save-for-later, acknowledge).
10. **Tier 3 regression** — run `/spec-archive` and `/implement` Phase 4 on a clean fixture; verify archival mechanism from M7 still works end-to-end.

**Acceptance Criteria:**
- [x] AC-21.1: `skills/spec-archive/SKILL.md` has `## Post-Archive Drift Check` after file modifications, before final report
- [x] AC-21.2: `skills/implement/SKILL.md` Phase 4 has `#### Post-Archive Drift Check` after archive move, before Phase 4 report
- [x] AC-21.3: Two passes documented (Pass A → Pass B) with unified report
- [x] AC-21.4: Pass A grep targets `M{N}`, `FR-{N}`, `AC-{N}.` excluding `^> archived:` lines
- [x] AC-21.5: Pass B reads live specs with (a) archived ID list, (b) archive title/goal excerpt, (c) scope-framing instruction
- [x] AC-21.6: Pass B names the canary pattern with the Flutter example
- [x] AC-21.7: Schema I defined in technical-spec.md with exactly 5 columns and 2 severity values (`high`, `medium`)
- [x] AC-21.8: Drift check is advisory-only; Pass A edits require explicit user approval
- [x] AC-21.9: Literal `technical-spec.md uses Superseded-by markers, not archival — Pass B flags for this file are advisory only, never push for removal` present
- [x] AC-21.10: Empty report emits literal `No drift detected` and continues without prompting
- [x] AC-21.11: Non-empty report offers exactly 3 user choices; never blocks archival
- [x] AC-21.12: `docs/patterns.md` has `### Pattern: Post-Archive Drift Check`
- [x] Plugin version is 1.11.0
- [x] Marketplace version is 1.11.0
- [x] CHANGELOG.md has `## [1.11.0] — YYYY-MM-DD — "Residue Scan"` entry cross-referencing FR-21
- [x] NFR-1: `implement/SKILL.md` line count still within NFR-1 envelope after task 2 (baseline 272 + ~5 from FR-21 = ~277; well under 300 — final: 276)
- [ ] Canary fixture manual validation succeeds (Pass B flags the three "documentation-only" sections) — **deferred: requires user-driven manual run against a Flutter fixture; cannot be executed from this repo alone**

**Gate:** Tier 1 static verification (all FR-21 literal strings, Schema I column count, heading placement, line-count recheck on implement/SKILL.md) + Tier 2 behavioral verification (7 scenarios from testing-spec.md §2.Tier 2) + Tier 3 regression (existing `/spec-archive` and `/implement` Phase 4 archival still works on a clean fixture). The canary fixture manual validation in task 7 is the proof-of-work that FR-21 solves the observed Flutter problem.

**Critical path note:** `implement/SKILL.md` entered M8 at **272 lines** (M7 overflow mitigation extracted ~55 lines to `docs/implement-reference.md`, so the file has ~28 lines of headroom under the 300-line NFR-1 cap). M8 adds only ~5 lines by pointing to spec-archive rather than inlining the two-pass logic, landing around 277 lines. Recheck `wc -l` as a gate during task 2; no proactive extraction needed.
