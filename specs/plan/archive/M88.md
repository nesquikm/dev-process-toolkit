---
milestone: M88
status: archived
archived_at: 2026-06-02T11:21:17Z
kickoff_branch: null
frozen_at: null
---

# Implementation Plan

## Milestone Order

## M88: Kotlin stack + bun prerequisite {#M88}

**Goal:** Promote Kotlin to a first-class stack (generic Gradle/JVM) at parity
with Flutter/Python/TypeScript — `examples/kotlin/`, `/setup` auto-detection, a
`stacks.kotlin` permissions key, a detekt scaffold-verify branch, and a new
`kotlin-detekt-configured` gate probe — and document **bun** as a universal,
stack-independent prerequisite in `README.md`.
**Prerequisites:** None

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-336 | Kotlin first-class stack — generic Gradle/JVM (examples + /setup detection + permissions + detekt gate) | linear:`STE-336` |
| STE-337 | Document bun as a universal prerequisite in README | linear:`STE-337` |

**Tasks:**

_FR-A — STE-336 (Kotlin first-class stack):_
- [x] Add `examples/kotlin/gate-commands.md` (gate set `./gradlew compileKotlin → detekt → test → build`, JUnit5/MockK TDD patterns, `./gradlew`-wrapper conventions, Settings Example block) + `examples/kotlin/release.yml` (gradle.properties version regex + CHANGELOG + optional README regex)
  verify: `ls examples/kotlin` shows exactly `gate-commands.md release.yml`; `git grep -nE 'Bash\(\./gradlew [^)]*\*\)' examples/kotlin` returns zero
- [x] Add `stacks.kotlin` row to `templates/permissions.json` with five explicit subcommands (`compileKotlin`, `detekt`, `test`, `build`, `--version`)
  verify: `bun test` merge_settings — `canonicalAllowList(template,"kotlin")` returns the Kotlin allowlist (≠ generic); `git grep -nE 'Bash\(\./gradlew [^)]*\*\)' templates/` returns zero
- [x] Wire `/setup` Kotlin detection: SKILL.md:53 marker probe (`build.gradle.kts`/`settings.gradle.kts`), :117 example match, :65 doctor `./gradlew --version` prefix
  verify: manual read of SKILL.md diff; Kotlin markers present at each anchor
- [x] Add `/setup` step 2b Kotlin scaffold bullet + step 2c detekt scaffold-verify branch (doc in `docs/setup-reference.md § Kotlin detekt scaffold-verify branch`)
  verify: SKILL.md step 2b shows "Kotlin (Gradle/JVM)" bullet; setup-reference.md carries the detekt branch section
- [x] Add `/gate-check` probe `kotlin-detekt-configured` at `adapters/_shared/src/kotlin_detekt_configured.ts` (detect Gradle Kotlin + detekt-declared → require detekt plugin; vacuous otherwise)
  verify: `bun test tests/gate-check-kotlin-detekt-configured.test.ts` green for all four fixtures
- [x] Update `docs/adaptation-guide.md` (add dedicated Kotlin row; narrow combined `Java/Kotlin` row to `Java`) + `README.md` (auto-detects line + Examples Provided For)
  verify: adaptation-guide shows separate Kotlin (detekt) + Java (spotless) rows; README lists Kotlin

_FR-B — STE-337 (bun universal prerequisite):_
- [x] Add `## Prerequisites` section to `README.md` (bun required for the toolkit's `_shared` helpers/adapters regardless of consumer stack; install pointer + `bun --version`)
  verify: `grep -n '## Prerequisites' README.md` hits; section names bun + bun.sh
- [x] Reconcile the tracker-mode bun mention (`/setup` step 7b + `docs/setup-tracker-mode.md`) to point at the universal Prerequisites section
  verify: no surviving "bun … only … tracker" framing (grep); mentions cross-reference the README section

**Tests:**
- `tests/gate-check-kotlin-detekt-configured.test.ts` — four `mkdtempSync` fixtures (detekt-declared-plugin-missing → fail; plugin-present → pass; detekt-not-a-gate → vacuous; non-Kotlin → vacuous)
- `merge_settings` unit test — `canonicalAllowList(template,"kotlin")` ≠ generic fallback
- FR-B is docs-only — verification via the AC-STE-337.2 grep + manual review

**Acceptance Criteria:**
- [x] AC-STE-336.1 — AC-STE-336.10 all pass (see `specs/frs/archive/STE-336.md`)
- [x] AC-STE-337.1 — AC-STE-337.3 all pass (see `specs/frs/archive/STE-337.md`)

**Gate:** `bun test && /gate-check`
