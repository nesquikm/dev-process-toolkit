---
milestone: M7
status: archived
archived_at: 2026-04-24T16:42:24+04:00
kickoff_branch: null
frozen_at: null
revision: 1
---

# M7: Bounded Context

**Goal:** Add milestone-based auto-archival to `/implement`, the `/spec-archive` escape-hatch skill, stable anchor IDs in templates, the `specs/archive/` directory convention with rolling index, and all supporting documentation + README + project CLAUDE.md updates. Retroactively compact the shipped v1.8/v1.9 content (M1–M6 and FR-1 through FR-15) as a dogfooding validation task.
**Prerequisites:** M6 (all v1.9.0 content must be final before archival is introduced — no point archiving content that's still being edited)

**Files to create:**
- `plugins/dev-process-toolkit/skills/spec-archive/SKILL.md` — FR-17 (entire skill)
- `plugins/dev-process-toolkit/templates/spec-templates/archive-index.md.template` — FR-19 (new template file)

**Files to edit:**
- `plugins/dev-process-toolkit/skills/implement/SKILL.md` — FR-16 (Milestone Archival subsection), FR-19 (Phase 1 no-read instruction)
- `plugins/dev-process-toolkit/skills/setup/SKILL.md` — FR-18 (doctor anchor check), FR-19 (scaffold archive/ + index.md)
- `plugins/dev-process-toolkit/skills/spec-write/SKILL.md` — FR-18 (anchor ID instruction + warning)
- `plugins/dev-process-toolkit/skills/spec-review/SKILL.md` — FR-19 (Optional: Consult Archive Index step)
- `plugins/dev-process-toolkit/skills/gate-check/SKILL.md` — FR-19 (drift never reads archive)
- `plugins/dev-process-toolkit/templates/spec-templates/plan.md.template` — FR-18 (milestone `{#M{N}}` anchor)
- `plugins/dev-process-toolkit/templates/spec-templates/requirements.md.template` — FR-18 (FR `{#FR-{N}}` anchor)
- `plugins/dev-process-toolkit/docs/patterns.md` — FR-18 (Stable Anchor IDs section), FR-20 (Archival Lifecycle pattern)
- `plugins/dev-process-toolkit/docs/sdd-methodology.md` — FR-20 (compactable specs paragraph)
- `plugins/dev-process-toolkit/docs/adaptation-guide.md` — FR-20 (Customizing Archival section)
- `README.md` (repo root) — FR-20 (feature bullet + v1.10.0 release notes)
- `CLAUDE.md` (repo root) — FR-20 (skill count + /spec-archive in commands)
- `plugins/dev-process-toolkit/.claude-plugin/plugin.json` — version bump 1.9.0 → 1.10.0
- `.claude-plugin/marketplace.json` — version bump 1.9.0 → 1.10.0 (per release checklist in repo CLAUDE.md)

**Tasks (dependency order):**
1. **Anchor ID templates first** (FR-18 foundation) — update `plan.md.template` and `requirements.md.template` with `{#M{N}}` / `{#FR-{N}}` anchor syntax (AC-18.1, AC-18.2). Verify `## M1: {title} {#M1}` renders correctly in a Markdown viewer.
2. **Archive index template** (FR-19 foundation) — create `templates/spec-templates/archive-index.md.template` with header row `| Milestone | Title | Archived | Archive File |` and one-line purpose description (AC-19.3).
3. **Setup scaffolding** (FR-19) — update `setup/SKILL.md` step 2 to create `specs/archive/` directory + initial `specs/archive/index.md` from the template (AC-19.1, AC-19.2).
4. **Setup doctor anchor check** (FR-18) — extend doctor validation (from FR-4) to verify every `## M{N}:` and `### FR-{N}:` heading has a matching `{#...}` anchor; report misses under `GATE PASSED WITH NOTES` (AC-18.5).
5. **Spec-write anchor instruction** (FR-18) — add plain-text instruction in `spec-write/SKILL.md` that generated/edited headings must carry anchors; warning on missing anchors (AC-18.4).
6. **New `/spec-archive` skill** (FR-17) — create `skills/spec-archive/SKILL.md` with full content: frontmatter, purpose, user-selected-only rule, `## Present Diff for Approval` step, approval gate, `### Reopening an Archived Milestone` subsection, `### Archiving from technical-spec.md` warning subsection, Schema G/H references, index append step (AC-17.1 through AC-17.9). Target size: ~120 lines.
7. **Implement Milestone Archival** (FR-16) — add `### Milestone Archival` subsection to `implement/SKILL.md` Phase 4 after `### Spec Deviation Summary`: conditional on human approval, archive path format, move-not-copy rule, traceability matrix lookup, FR collapse rule, Schema H pointer, index append, literal instructions from AC-16.7 and AC-16.8 (AC-16.1 through AC-16.10).
8. **Implement no-read rule** (FR-19) — add literal instruction in Phase 1 of `implement/SKILL.md`: `Do not read specs/archive/ during implementation — archived milestones are historical context only` (AC-19.6).
9. **Gate-check no-read rule** (FR-19) — add literal instruction in `gate-check/SKILL.md` Drift Check subsection: `Never read specs/archive/ — only live spec files count for drift detection` (AC-19.5).
10. **Spec-review archive lookup** (FR-19) — add `### Optional: Consult Archive Index` step in `spec-review/SKILL.md` between scan and findings; only reads `specs/archive/index.md` if user query references a milestone ID not in live plan.md (AC-19.4).
11. **Patterns doc — Stable Anchor IDs** (FR-18) — add `### Stable Anchor IDs` section to `docs/patterns.md` with rationale and anchor format table (AC-18.6).
12. **Patterns doc — Archival Lifecycle** (FR-20) — add `### Pattern: Archival Lifecycle` section to `docs/patterns.md` explaining trigger, scope, non-scope, rationale (AC-20.1).
13. **Methodology doc** (FR-20) — add paragraph to `docs/sdd-methodology.md` noting specs are compactable and archival is automatic at `/implement` Phase 4 (AC-20.2).
14. **Adaptation guide** (FR-20) — add `## Customizing Archival` section explaining opt-out (delete `specs/archive/`) and manual archival via `/spec-archive` (AC-20.3).
15. **README update + CHANGELOG.md** (FR-20) — add feature bullet mentioning `/spec-archive` + `specs/archive/` (AC-20.4); create `CHANGELOG.md` at repo root in Keep a Changelog format with a `## [1.10.0]` entry summarizing FR-16..19 in 2-4 bullets (AC-20.5); README links to CHANGELOG instead of embedding release notes. Also update `CLAUDE.md` (repo root) Release Checklist to mandate CHANGELOG.md updates on every version bump (AC-20.10).
16. **Project CLAUDE.md update** (FR-20) — update skill count (`11 skills` → `12 skills`), list `/spec-archive` in key commands (AC-20.6, AC-20.7).
17. **Skill-anatomy conditional update** (FR-20) — if `docs/skill-anatomy.md` has skill-list examples, add `/spec-archive` (AC-20.8).
18. **Version bump** — update `plugin.json` and `marketplace.json` from `1.9.0` to `1.10.0` (per release checklist).
19. **Dogfood: retroactive compaction (validation task)** — use the newly-built `/spec-archive` skill to compact this repo's own M1–M6 content from `plan.md` and the corresponding shipped FRs (FR-1 through FR-15) from `requirements.md`. Each archive operation is human-reviewed via the diff step. This is the final validation that the feature works end-to-end. Expected outcome: `specs/plan.md` and `specs/requirements.md` shrink substantially; `specs/archive/` gains 6 milestone archive files; `specs/archive/index.md` gains 6 rows.

**Acceptance Criteria:**
- [x] AC-16.1: `### Milestone Archival` subsection exists in Phase 4 after `### Spec Deviation Summary`
- [x] AC-16.2: Archival runs only after human approves Phase 4 report
- [x] AC-16.3: Archive path format `specs/archive/M{N}-{slug}.md` documented
- [x] AC-16.4: Move-not-copy instruction for milestone block
- [x] AC-16.5: Traceability matrix lookup for AC migration
- [x] AC-16.6: FR collapse rule documented
- [x] AC-16.7: Literal `Archival is skipped if specs/archive/ does not exist`
- [x] AC-16.8: Literal `technical-spec.md is never auto-archived`
- [x] AC-16.9: Schema H pointer in plan.md at removal location
- [x] AC-16.10: Archive index append instruction
- [x] AC-17.1: `skills/spec-archive/SKILL.md` exists with valid frontmatter
- [x] AC-17.2: User-selected-only rule in description
- [x] AC-17.3: `## Present Diff for Approval` step
- [x] AC-17.4: Approval gate before file modifications
- [x] AC-17.5: Uses Schema G archive file format
- [x] AC-17.6: Uses Schema H pointer format
- [x] AC-17.7: `### Reopening an Archived Milestone` subsection with revision naming
- [x] AC-17.8: `### Archiving from technical-spec.md` warning subsection
- [x] AC-17.9: Index append step
- [x] AC-18.1: `plan.md.template` has `{#M{N}}` anchor literal
- [x] AC-18.2: `requirements.md.template` has `{#FR-{N}}` anchor literal
- [x] AC-18.3: AC line format unchanged (existing convention)
- [x] AC-18.4: `spec-write/SKILL.md` has anchor instruction + warning
- [x] AC-18.5: Setup doctor checks anchors, reports under NOTES
- [x] AC-18.6: `docs/patterns.md` has `### Stable Anchor IDs` section
- [x] AC-19.1: `setup/SKILL.md` creates `specs/archive/` directory
- [x] AC-19.2: `setup/SKILL.md` creates `specs/archive/index.md` with header row
- [x] AC-19.3: `archive-index.md.template` exists
- [x] AC-19.4: `spec-review/SKILL.md` has `### Optional: Consult Archive Index`
- [x] AC-19.5: Literal `Never read specs/archive/ — only live spec files count for drift detection`
- [x] AC-19.6: Literal `Do not read specs/archive/ during implementation — archived milestones are historical context only`
- [x] AC-19.7: Append-only archive rule documented
- [x] AC-20.1: `docs/patterns.md` has `### Pattern: Archival Lifecycle` section
- [x] AC-20.2: `docs/sdd-methodology.md` notes compactable specs
- [x] AC-20.3: `docs/adaptation-guide.md` has `## Customizing Archival` section
- [x] AC-20.4: `README.md` feature bullet mentions `/spec-archive`
- [x] AC-20.5: `CHANGELOG.md` exists in Keep a Changelog format with v1.10.0 entry; README references it instead of embedding release notes
- [x] AC-20.6: Project `CLAUDE.md` skill count updated
- [x] AC-20.7: Project `CLAUDE.md` lists `/spec-archive` in commands
- [x] AC-20.8: `docs/skill-anatomy.md` updated if skill lists exist (conditional)
- [x] AC-20.9: Every doc/README/CHANGELOG addition cross-references FR-16..19
- [x] AC-20.10: `CLAUDE.md` Release Checklist lists 3 mandatory files (plugin.json, marketplace.json, CHANGELOG.md) with anti-README-rot warning
- [x] NFR-1: No skill file exceeds 300 lines (recheck `implement/SKILL.md` after additions — budget concern)
- [x] NFR-5: Post-archival `plan.md` and `requirements.md` line counts lower than pre-archival (validated in dogfood task 19)
- [x] Plugin version is 1.10.0
- [x] Marketplace version is 1.10.0
- [x] All 6 shipped v1.8/v1.9 milestones compacted into `specs/archive/` via dogfood task

**Gate:** Tier 1 static verification (all literal strings, all file/section presence, anchor ID grep in templates, line count on implement/SKILL.md) + Tier 2 behavioral verification (FR-16 end-to-end in a reference project, FR-17 diff approval flow, FR-18 doctor anchor check, FR-19 setup scaffolding, FR-20 doc presence) + Tier 3 regression (setup/implement/gate-check/spec-archive core workflow on a fresh project). The dogfood task in step 19 is both the final validation and the proof-of-work for NFR-5.

**Critical path concern:** `implement/SKILL.md` is currently at ~273 lines (after M1-M6). Adding FR-16's `### Milestone Archival` subsection (~35 lines) + FR-19's no-read rule (~2 lines) pushes it to ~310 lines — **over the 300-line NFR-1 cap**. Mitigation: per NFR-1 overflow rule, extract Phase 3 hardening examples (or the Phase 4 archival detail) to `docs/implement-reference.md` with a one-line pointer in the skill file. Decide which content moves during task 7 (Implement Milestone Archival), not at the end.
