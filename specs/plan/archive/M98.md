---
milestone: M98
status: archived
archived_at: 2026-07-06T07:47:47Z
kickoff_branch: feat/m93-verification-skills
frozen_at: null
shipped_in: v2.39.0
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M98: smoke/conformance headless-run hardening {#M98}

**Goal:** the 2026-07-04 live `/conformance-loop --jira-project DST` run (capture-only) aborted both iterations at smoke/conformance **infrastructure** blockers (0 plugin-under-test findings). Fix all three so an autonomous `/conformance-loop` iteration can reach the plugin-under-test surface: F3 (run-blocker) — the `/smoke-test` Phase 2 driver fire-and-exits under `-p`, spawning `/setup` via `run_in_background` and yielding the turn, and it recurred **despite** M96/STE-357's prose guardrails; F2 — glob fences false-PASS under zsh `nomatch`; F1 (worked around live) — the STE-356 trust-seed write is denied by the harness self-modification classifier under `-p`.

**Prerequisites:** M95 (v2.36.0) + M96 (v2.37.0) — the detached-spawn/pidfile/bounded-poll mechanics and the STE-356 trust seeding this milestone hardens. No new external dependencies. All three FRs edit the maintainer-only project-local skills `.claude/skills/{smoke-test,conformance-loop}/SKILL.md` (+ one new meta-test each); no plugin-shipped skill changes.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-365 | smoke-test Phase 2 `-p`-context probe + `run_in_background` prohibition + drift meta-test (F3) | linear:`STE-365` |
| STE-366 | zsh-safe glob fences across smoke-test + conformance-loop (F2) | linear:`STE-366` |
| STE-367 | workspace-trust operator precondition — assert + refuse, no self-write (F1) | linear:`STE-367` |

**Dependency order:** all three edit the same two SKILL.md files but at disjoint sites (Phase 2 spawn vs glob fences vs Phase 1 step 6b / pre-flights), so they are logically independent. Land **STE-365 first** (the priority run-blocker), then STE-366, then STE-367; sequencing avoids edit-conflict churn on the shared files rather than reflecting a true dependency.

**Tasks:**

- [ ] STE-365 — Add the Phase-2-entry `SMOKE-CTX:` context probe + co-located ⛔ FORBIDDEN-`run_in_background` callout at the smoke-test spawn site; mirror the callout at conformance-loop's Phase A spawn; add the drift meta-test over both SKILL.mds. — STE-365
  verify: `tests/m98-ste-365-fire-and-exit-guard.test.ts` pins the probe + headless warning + FORBIDDEN callout + detached snippet in both skills; existing line-cap meta-tests stay green.
- [ ] STE-366 — Make every glob fence zsh-safe: Phase 0.5 `rm` → `bash -c`, verify `ls` → `find`; `for … in <glob>` pidfile fences (both skills) → `nullglob` guard; teardown/orphan globs likewise. — STE-366
  verify: `tests/m98-ste-366-zsh-glob-fences.test.ts` pins guard tokens at each fence + negative assertion no unguarded verify-`ls`/`for`-glob remains; optional `zsh -c` behavioral test.
- [ ] STE-367 — Rewrite smoke-test step 6b from write to assert+refuse (jq remedy); remove Phase 5 trust-removal + backup; add conformance-loop pre-flight (h) asserting both test paths trusted; rename `workspace_trust_seeded` → `workspace_trust_present` repo-wide. — STE-367
  verify: `tests/m98-ste-367-workspace-trust-precondition.test.ts` pins write-removed/assert-present + pre-flight (h) + teardown-removal-gone + token rename.

**Tests:**
- Meta-tests under `tests/` — fire-and-exit guard (STE-365); zsh glob-fence guards (STE-366); trust-precondition shape + token rename (STE-367). All grep the repo-root-relative project-local SKILL.mds.
- Optional behavioral — extract the Phase 0.5 fence block and run under `zsh -c` (STE-366), skipped if no zsh in CI.
- Runtime (ships `[~]` pending a live run) — the next `/conformance-loop` run shows both `/smoke-test` legs running the full grandchild chain to completion with no fire-and-exit (AC-STE-365.5), zero self-modification denials with trust pre-seeded and an up-front refusal when absent (AC-STE-367.5).

**Acceptance Criteria:**
- [ ] AC-STE-365.1–.4 (context probe; co-located prohibition; drift meta-test; conformance-loop parity) — .5 runtime `[~]`.
- [ ] AC-STE-366.1–.4 (smoke + conformance fences zsh-safe; verify cannot false-PASS; drift meta-test).
- [ ] AC-STE-367.1–.4 (assert-not-write; pre-flight (h); refusal content + token; teardown no longer removes trust) — .5 runtime `[~]`.

**Gate:** `bun test` GREEN (full gate from `plugins/dev-process-toolkit`) + `/dev-process-toolkit:gate-check` GREEN on this repo.

**Release target:** v2.39.0 (provisional, minor). M92 (`chore/ste-344`) and M97 (`feat/m97`) remain unshipped on sibling branches; the final version resolves at `/ship-milestone` per the release-collision guard.
