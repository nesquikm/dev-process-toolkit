---
milestone: M87
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
---

# Implementation Plan

## Milestone Order

## M87: Plan-heading parser unification {#M87}

**Goal:** Fix the milestone-binding write + verify paths so they parse the
current-format `## M<N>: <title>` plan heading (not just the legacy H1+em-dash
form). Closes the drift that silently disabled M86/STE-329's milestone binding
(Linear `object` + Jira `label`) for every downstream consumer.
**Prerequisites:** None

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-335 | Unify plan-heading parser — fix milestone attach + probe #26 on `## M<N>:` plans | linear:`STE-335` |

**Tasks:**
- [ ] Add shared `parsePlanHeading(md)` parser in `adapters/_shared/src/plan_heading.ts` (H1/H2 × em-dash/colon × optional anchor; normalize to `M<N> — <title>`)
  verify: `bun test tests/plan-heading.test.ts` green for the full matrix + headingless → null
- [ ] Delegate `planFileHeadingToMilestoneName` (attach_project_milestone.ts) to `parsePlanHeading`; delete private `PLAN_HEADING_REGEX`; raise only on null
  verify: `bun test` — attach test on a `## M<N>:` plan returns `M<N> — …` (was RED), headingless still throws
- [ ] Delegate probe #26 `readPlanHeading` to `parsePlanHeading`; delete private `HEADING_RE`
  verify: `bun test` — probe #26 fires on a current-format plan for both label + object bindings (was vacuous)
- [ ] Repo-wide audit: no other live `^# (M\d+ — …)` regex copy remains
  verify: `grep -rn 'M\\d+ — ' adapters/_shared/src` returns no live heading regex

**Tests:**
- `tests/plan-heading.test.ts` — parser matrix + headingless + titles with `/` and `:`
- `attach_project_milestone` — current-format plan (RED pre-fix) + headingless (still throws)
- probe #26 — current-format fixture, both label + object bindings (RED pre-fix → GREEN)
- legacy `# M<N> — …` fixtures stay green

**Acceptance Criteria:**
- [ ] AC-STE-335.1 — AC-STE-335.7 all pass (see `specs/frs/STE-335.md`)

**Gate:** `bun test && bunx tsc --noEmit`
