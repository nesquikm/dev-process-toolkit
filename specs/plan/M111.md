---
milestone: M111
status: active
archived_at: null
kickoff_branch: fix/m111-monolith-signaling
frozen_at: null
migration: none
shipped_in: null
---

# Implementation Plan

## M111: Legacy-monolith signaling {#M111}

**Goal:** Close the two-facet gap the 2026-07-21 `/report-issue` gist (rh/stock_check_app) surfaced: a legacy monolithic-specs tree with a hand-written `CLAUDE.md` gets zero signal that the purpose-built `monolith-split` migration exists (`/upgrade` Step 0 and gate-check probe #69 both short-circuit before any detector runs), and `/setup`'s `.gitkeep` scaffold silently defeats the detector's absent-or-empty limb even on the supported bootstrap-then-upgrade path. The bootstrap-first ownership gate stays hard — no migration ever runs on an unmanaged tree; every fix is detection, signaling, or scaffold hygiene.

**Prerequisites:** none — every fix site ships in v2.53.0. STE-410/411/412 all reuse the STE-392 detector; STE-411 reuses STE-410's probe surface.

**Release target:** v2.54.0 (provisional; M101 "Jira milestone-as-Epic" remains unshipped on a sibling branch — whichever ships second re-bumps).

**Migration:** `none` — this milestone changes plugin surfaces only (one detector predicate, one probe branch, three SKILL bodies); no consumer-artifact convention changes, so no registry entry.

**FR list** (build order A → B → C → D):

| FR | Title | Tracker |
|----|-------|---------|
| STE-409 | Content-aware emptiness limb in the monolith-split detector | linear:`STE-409` |
| STE-410 | Probe #69: legacy-monolith advisory row on unmanaged trees | linear:`STE-410` |
| STE-411 | /upgrade Step 0: monolithic-layout hint on the never-bootstrapped path | linear:`STE-411` |
| STE-412 | /setup step 8: no specs/frs/ scaffold over a live monolithic requirements.md | linear:`STE-412` |

**Tasks:**

- [ ] Replace the emptiness-limb listing check in `monolith_split.ts` with the `hasRealFrContent` predicate (dot-entries skipped, recursive, `.md`-only evidence) + scaffold-aware evidence wording — STE-409
  - verify: `bun test adapters/_shared/src/migrations/monolith_split.test.ts` — `.gitkeep`-only and scaffold-shape fixtures ⇒ `applies: true`
- [ ] Add the six emptiness fixtures (scaffold shapes, real-content shapes, degrade path) + the evidence byte-pin — STE-409
  - verify: same suite green; existing cases unchanged
- [ ] Export `LEGACY_MONOLITH_HINT` + the unmanaged-tree monolith sniff branch in `runUpgradeStalenessProbe` (guarded, degrade-to-silence) — STE-410
  - verify: `bun test tests/gate-check-upgrade-staleness.test.ts` — unmanaged+monolith ⇒ 1 row + hint; unmanaged+clean ⇒ 0 rows; managed unchanged
- [ ] Amend the gate-check SKILL probe #69 entry (carve-out sentence + hint literal) + run the docs-pin sweep — STE-410
  - verify: `bun test tests/m109-ste-394-docs-pins.test.ts` + the count-drift suite green (probe count stays 72)
- [ ] Add the `import.meta.main` CLI to `upgrade_staleness.ts` + the /upgrade SKILL Step 0 invocation + conditional refusal lines — STE-411
  - verify: `bun test tests/m111-ste-411-upgrade-hint-pins.test.ts` — SKILL carries the byte-equal literal + invocation line; ownership sentence intact
- [ ] /setup SKILL § 8 monolith guard (skip `specs/frs/` scaffold + audit row + never-overwrite pin) + the STE-189 inventory carve-out — STE-412
  - verify: `bun test tests/m111-ste-412-setup-scaffold-guard.test.ts` — guard + inventory pins present
**Acceptance criteria:** every AC in STE-409..412 passes; full `bun test` from `plugins/dev-process-toolkit` green; no existing probe regresses; the numbered probe count stays 72 (no new probe — #69 is amended in place).

**Gate commands:**

```bash
cd plugins/dev-process-toolkit && bun test
```

**Dependency graph:**

```
STE-409 (detector content check)
   ├─→ STE-410 (probe #69 carve-out row + hint literal)
   │        └─→ STE-411 (/upgrade Step 0 hint — consumes the probe CLI)
   └─→ STE-412 (/setup step 8 guard — consumes detect; independent of B/C)
```

## Notes

Origin: `/report-issue` gist https://gist.github.com/nesquikm/ac2cc2437e686e407f687348ab704c46 (rh/stock_check_app, Flutter, plugin 2.53.0, medium severity, reproducible) → `/brainstorm` 2026-07-21 (Approach 2 of 3 approved — canonical sentinel; ownership gate kept hard) → this `/spec-write`.

Rejected at brainstorm: running migrations on unmanaged trees (violates the STE-391 ownership invariant); `/implement`-side hints (out of its lane; hint sprawl — the M107 docs-drift shape). The `/setup` step-6c `upgrade_available` registry walk already covers managed trees mid-bootstrap; M111 covers the pre-bootstrap silence (STE-410/411) and the post-scaffold silence (STE-409/412).
