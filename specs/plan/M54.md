---
milestone: M54
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
---

# M54 — Smoke #11 Follow-ups: spec-driven workflow hardening (Dart-lib smoke) {#M54}

**Release target:** v2.7.0 (minor — multiple skill-prose tightenings, one new helper, one template rewrite, one new `--code-only` flag for `/implement`, two new capability keys, two new `/gate-check` probes; no plugin-facing API breakage). Codename: TBD at `/ship-milestone` time.

**Status:** drafted 2026-05-04 from the 2026-05-04 Linear smoke run against v2.5.0 on `/Users/ns/workspace/st/dpt_test_lib` (pure Dart math library). Findings document: `/tmp/dpt-findings-2026-05-04.md`. 6 FRs grouped from 14 verified findings (1 false-positive subsumed: F3.7).

## Why this milestone exists

The 2026-05-04 smoke run produced 16 findings spanning `/setup`, `/spec-write`, `/spec-archive`, `/implement`, `/ship-milestone`, and `templates/`. Brainstorm verification (this conversation) overturned 1 sub-agent verdict, dropped 1 subsumed finding, and grouped the surviving 14 into 6 FRs by skill / file co-location:

1. **Plan-file output hygiene** (STE-197) — three compounding bugs in how `/setup` writes plan files (multi-milestone bundling, no frontmatter, leftover placeholders) cascade into `/spec-archive`/`/implement`/`/ship-milestone` failures downstream.
2. **Milestone-attach capability split** (STE-198) — `/spec-write` skips milestone attachment when the Linear project has zero milestones, even though the adapter could create one. Capability key conflates two distinct adapter states.
3. **Tracker availability probes** (STE-199) — both `/setup` (first-run, MCP not registered) and `/implement` (mid-session, MCP unloadable) have no graceful fallback. New `--code-only` flag + `<deferred>` binding mode + pre-flight probes.
4. **Scaffolding-milestone closure** (STE-200) — `/spec-archive` refuses on zero-FR milestones; `/implement` Phase 1/5 assume work to do; `/ship-milestone` would bump versions for placeholders. Add plan-only archival + slim closure path.
5. **`/ship-milestone` task-bullet pre-flight** (STE-201) — pre-flight walks FR rows but not `**Tasks:**` checkboxes, so a plan with unchecked task bullets passes pre-flight cleanly.
6. **Doc & workflow polish** (STE-202) — five mechanical fixes bundled: `/simplify` in the Feature workflow, singular `specs/plan.md` prose drift, M-resolver kind, permissions trim, `branch_template` trunk-OK allowlist.

After M54 ships:

- A fresh `/setup` against any project produces plan files that pass `/gate-check` cleanly (frontmatter present, single milestone per file, no leftover placeholders).
- Linear projects with zero milestones get auto-created milestones during `/spec-write`, not silently dropped attachments.
- `/setup` no longer crashes on a project where the operator hasn't yet authenticated the configured MCP; `/implement` can run in `--code-only` mode when the MCP is mid-session unavailable.
- Bootstrap milestones (M1 in fresh-project repos) close cleanly without manual `git mv` workarounds.
- `/ship-milestone` refuses to release a milestone whose plan still has unbacked task bullets (no false-positive ship).
- Operators following the canonical Feature workflow run `/simplify` before `/spec-review`, catching premature splits and template-name leftovers that previously required dedicated cleanup milestones.

## FR list (6 FRs)

| FR      | Title                                                                                          | Tracker          | Phase    | Severity |
|---------|------------------------------------------------------------------------------------------------|------------------|----------|----------|
| STE-197 | Plan-file hygiene: trimmed template + YAML frontmatter + bootstrap allocation rules            | linear:`STE-197` | template | high     |
| STE-198 | Milestone-attach: auto-create branch + capability key split (zero-milestone Linear projects)   | linear:`STE-198` | spec-write | high   |
| STE-199 | Tracker availability: first-run deferred binding + mid-session probe + `--code-only` fallback  | linear:`STE-199` | setup + implement | high |
| STE-200 | Scaffolding-milestone closure: `--plan-only` archival + slim Phase 1 + Phase 5 zero-FR skip    | linear:`STE-200` | spec-archive + implement | high |
| STE-201 | `/ship-milestone` pre-flight walks `**Tasks:**` checklist (not just FR rows)                   | linear:`STE-201` | ship-milestone | medium |
| STE-202 | Doc & workflow polish: `/simplify` in Feature row, prose drift, permissions trim, branch_template trunk-OK | linear:`STE-202` | docs + templates | low |

## M54: Smoke #11 follow-ups {#M54-fr-block}

**Goal:** Close 14 verified bugs from the 2026-05-04 Dart-lib smoke run + retroactively validate the M53-deferred AC-STE-195.5 (smoke-chain stream-idle).
**Prerequisites:** M53 shipped (v2.6.0 "Sensitivity") — its driver retry-with-rollback is the test fixture for AC-STE-195.5 retroactive validation.

**Tasks:**

- [ ] STE-197 — rewrite `templates/spec-templates/plan.md.template` (single-milestone scaffold + YAML frontmatter); update `/setup` step 8 to mark bootstrap milestone `kind: scaffolding`; add `/implement` Phase 4 frontmatter-prepend branch; add `/gate-check` probe `plan-file-single-milestone`.
  verify: `bun test test/templates/plan-template.test.ts` passes (template parses with single milestone + frontmatter); `/gate-check` flags multi-milestone fixture at ADVISORY.
- [ ] STE-198 — split `milestone_attach_unavailable` into two capability keys; update `attach_project_milestone.ts` helper with auto-create branch; update `/spec-write` § 0b step 4 prose; add Linear smoke fixture (zero-milestone project).
  verify: `bun test test/adapters/_shared/attach_project_milestone.test.ts` passes both capability paths; smoke fixture creates issue with auto-attached milestone.
- [ ] STE-199 — add `/setup` step 7b first-run-deferral branch + `--resume-tracker-binding` flag; add `/implement` Phase 1 pre-flight probe + `--code-only` flag; add two capability keys.
  verify: `bun test test/skills/setup/step-7b-deferred.test.ts` + `test/skills/implement/preflight-probe.test.ts` pass; `--code-only` integration test produces `tracker_skipped` rows.
- [ ] STE-200 — `/spec-archive` plan-only auto-detect + `--plan-only` flag; `/implement` Phase 1 slim-closure detection; Phase 5 silent-skip extension; capability key.
  verify: `bun test test/skills/spec-archive/plan-only.test.ts` + integration test (bootstrap repo → `/implement M1` slim-closes → no `/ship-milestone` prompt).
- [ ] STE-201 — `/ship-milestone` pre-flight walks `**Tasks:**` block + `[deferred]` marker support; `/gate-check` probe `plan-task-fr-coverage`.
  verify: `bun test test/skills/ship-milestone/preflight-tasks.test.ts` passes refusal + `[deferred]` exemption; smoke regression matches AC-STE-201.2 shape.
- [ ] STE-202 — six doc/template/config edits (workflow row, prose drift, M-resolver kind, permissions trim, branch_template allowlist, `/setup` 7d `docs_default_applied` capability row).
  verify: `grep -rn 'specs/plan.md' plugins/dev-process-toolkit/skills/` returns zero matches; manual review of CLAUDE.md.template Workflows block; autonomous `/setup` against a fresh repo emits `docs_default_applied` row in summary.
- [ ] **M53 retroactive: AC-STE-195.5 validation** — run a full smoke chain (`/setup → /spec-write → /implement → /gate-check → /spec-review → /simplify → /pr`) under conditions that reproduce a stream-idle timeout. Confirm M53's driver retry-with-rollback recovers cleanly. If no real stream-idle surfaces during M54, the validation can ship as `[~]` once more with explicit M55 carry-forward — but the canonical path is to validate now.
  verify: smoke driver produces a `stream_idle_recovered` audit row OR explicit `[~]` carry-forward note in `specs/plan/archive/M54.md` Notes section at archival time.
- [ ] **Lift per-FR smoke regression fixtures into `.claude/skills/smoke-test/SKILL.md` Phase 2 probes** — shared across future smoke runs (both Linear and Jira legs), not one-off per-FR test files. Covers the three regression-detection-critical FRs (STE-197 plan-file shape, STE-200 scaffolding-closure path, STE-201 ship-milestone task-bullet pre-flight); STE-198/199/202 stay as per-FR fixtures (smaller blast radius or environmental). All three probes are tracker-agnostic by design: STE-197 inspects local files only; STE-200 invokes `/spec-archive`/`/implement` skills that don't write to tracker for scaffolding milestones; STE-201 only reads tracker FR statuses. Implementer of each covered FR writes the per-FR fixture first, then this task lifts it into the shared driver.
  verify: `/smoke-test` Phase 2 output enumerates new probes for STE-197/200/201 conditions and runs them on **both** the Linear leg (fresh-project fixture, per existing pattern) and the Jira leg (persistent fixture project — `/smoke-test` cannot create new Jira projects per workspace permissions, so issue-level cleanup is the existing Jira-smoke responsibility). Replaying the 2026-05-04 `dpt_test_lib` fixture pre-fix catches all three on the Linear leg; equivalent pre-fix replay on the Jira leg catches them too; post-fix smoke is clean on both legs.

**Tests:**

- Unit tests for each AC across `attach_project_milestone.ts`, `resolve.ts`, plan-template parser, pre-flight probe (`/implement`, `/ship-milestone`).
- Integration tests against fresh-project fixtures (Dart, TypeScript, Python) confirming `/setup` → `/implement M1` → `/spec-archive M1` slim closure works without manual intervention.
- Smoke regression: rerun the 2026-05-04 fixture (`dpt_test_lib`-style Dart project with zero Linear milestones) and confirm all 14 finding symptoms are now absent.

**Acceptance Criteria:**

- [ ] All ACs in STE-197 through STE-202 (28 ACs total) satisfied per their respective FR files.
- [ ] `/gate-check` clean on the milestone branch — no advisory rows for any of the 14 findings' fixture conditions.
- [ ] M53 deferred AC-STE-195.5 retroactively validated OR explicitly carried to M55 with framing in M54 archival Notes.
- [ ] `/spec-review` against the milestone confirms no implementation deviates from FR specs.
- [ ] No new untracked findings surface during smoke regression — i.e., the 14 fixes don't introduce new bugs.

**Gate:** `bun test --bail` (all FR + adapter unit tests) + `/gate-check` (zero ADVISORY rows for fixture conditions) + manual smoke regression on `dpt_test_lib`-style Dart fixture.

## Notes

- **F3.7 (drift Pass A grep missing `<tracker-id>`)** is intentionally subsumed by STE-197. Once `/setup` no longer leaves `<tracker-id>` literals in committed plan files, the drift-grep gap is moot. If a future smoke surfaces leftover placeholders via a path other than `/setup`, F3.7 reopens as its own FR.
- **F3.10 (branch_template trunk-OK allowlist)** was suggested for deferral by the smoke-tester (P3 polish). Bundled into STE-202 because it's a one-line prose addition; deferring it just delays the polish without reducing review effort.
- The brainstorm verification of all 14 findings (with one sub-agent verdict overturned via direct read) is the source of truth for which findings are real. Any future re-grouping should re-verify before claiming a finding is a false-positive.
- Implementation order matters for STE-200: it depends on STE-197's `kind: scaffolding` frontmatter convention. Implement STE-197 first.
- All 6 FRs surface `filename_policy_override` capability rows during `/spec-write` per the unconditional rule.
