---
status: active
archived_at: null
kind: feature
---

# M70: Runtime emission contract closures (post-M69 follow-ups) {#M70}

Release target: TBD
Codename: TBD

## Summary

Close state-emission convergence + post-iter-1 contract drift surfaced after M69 shipped (v2.20.0 "Byte-Strict"). Three FRs cover (a) **STE-284** — state-emission via tracker ↔ local reconciliation (helper preflight + /gate-check probe); (b) **STE-294** — /spec-write auto-apply contract drift (STE-226 hardening, marker as sole trigger); (c) **STE-295** — post-iter-1 fixture-group bundling 5 ACs (hook carve-out for spec-only commits, Linear AC-token strip on read, branch-gate default-applied coverage, probe-#37 doc, severity-format normalization).

Rescoped on 2026-05-15: the original M70 was created 2026-05-11 by an earlier `/conformance-loop` iter-1 alongside STE-280/281/282 (Pattern-26 first-turn enforcement broadening, /implement `tdd-result` visibility, /spec-write spec-research token emission). Those three were cancelled on 2026-05-15 as not urgent enough to block release — they fix observability gaps where the underlying features work; the gate-check + smoke-probe coverage will catch any drift if it matters later. M70 now ships three focused FRs.

Triggered by: 2026-05-13 partial-scan trap surfaced manually during the `/brainstorm` → `/spec-write` flow for STE-283 / Honored Contracts (STE-284) + post-M69 `/conformance-loop` iter-1 (2026-05-14) findings F1/F3/F4/F5/F9/F11 + severity-format meta-bug (STE-294, STE-295).

## In Scope

- [ ] STE-284 — Tracker ↔ local FR/milestone reconciliation: helper preflight + /gate-check probe
  - verify: 8 ACs covering `nextFreeMilestoneNumber` tracker extension, `reconcileTrackerLocal` helper, `/spec-write` § 0.5 preamble, `/gate-check` probe, capability key map updates, STE-119 supersession framing, `Provider.listMilestones()` interface, performance budget; tests in `plugins/dev-process-toolkit/adapters/_shared/src/__tests__/`, `skills/{spec-write,gate-check}/__tests__/`, `adapters/{linear,jira}/__tests__/`.
- [ ] STE-294 — STE-226 hardening: /spec-write auto-apply must ignore autonomous-mode-reminder paraphrases
  - verify: 5 ACs covering `spec_write_alternate_trigger_scan.ts` forbidden-phrase array extension (probes #38–39), SKILL.md prose pass at § 0b step 4 + § 7a + ## Rules, regression fixture at `tests/fixtures/socratic-first-turn/regression/spec-write-marker-absent-reminder-present-2026-05-14.json`, runtime test in `skills/spec-write/__tests__/marker_detection_runtime.test.ts`, smoke fixture 1b byte-checkable assertion update (`.claude/skills/smoke-test/SKILL.md`), cross-tracker asymmetry root-cause documentation. Sourced from `/conformance-loop` iter-1 (2026-05-14) § F1; F7 + F10 folded as side-effects (F10 → separate `/setup`-side FR). Extends the STE-262 + STE-270 triple-layer defense pattern.
- [ ] STE-295 — Smoke fixture group: M70 post-iter-1 cohort follow-ups
  - verify: 5 ACs covering STE-290 pre-commit-tdd-orchestrator carve-out for spec-only commits (AC-1, ship first), STE-211 `stripLinearACFences` on `mcp__linear__get_issue` read path (AC-2), `branch_gate_default_applied` coverage via Phase 9 master-merge sub-fix (AC-3, folds F9), probe #37 fence-only scope documented in `/gate-check` (AC-4), Linear smoke findings template `**Severity:**` canonical normalization (AC-5). Sourced from `/conformance-loop` iter-1 (2026-05-14) § F3/F4/F5/F9/F11 + severity-format meta-bug. Pairs with STE-294 to cover iter-1 high+medium findings. Follows STE-231 / STE-286 fixture-group precedent.

## Out of Scope (deferred)

- Cross-branch awareness (STE-225-style branch-aware scanning) — documented limitation in STE-284; deferred follow-up FR.
- Bidirectional FR-description sync — STE-211's existing per-AC content drift flow is separate.
- Auto-resolution of milestone-mismatches — STE-284 prompts only, never silently resolves.

## Risks

- **Risk:** STE-284 supersedes STE-119's tracker-exclusion decision; cleanup of any other call sites that assumed mode-agnostic milestone allocation may surface during `/implement`. **Severity:** medium. **Mitigation:** grep for `nextFreeMilestoneNumber` call sites + verify each handles the optional `provider` param correctly during `/implement` Phase 2.
- **Risk:** AC-STE-295.1 (hook carve-out for spec-only commits) must ship FIRST among STE-295's ACs — it unblocks every subsequent spec commit in M70 (including this FR's own /implement commits) by removing the per-session `/tdd` + `/gate-check` ceremony. **Severity:** low. **Mitigation:** /implement's TDD chain naturally lands AC.1 first if walked in numeric order; flag explicitly in STE-295's body.

## Notes

- M70 is themed "Runtime emission contract closures" but the dominant work is state-emission reconciliation (STE-284) + post-iter-1 contract closures (STE-294, STE-295). Title preserved for stable anchor reference; theme statement broadened.
- The local plan file `specs/plan/M70.md` was created on 2026-05-13, 2 days after M70 was first created on Linear via `/conformance-loop` iter-1 (2026-05-11). This is the canonical partial-scan trap reproduction: the milestone existed on the tracker but had no local plan file until manual recovery. STE-284's reconciliation flow will prevent this in the future.
- **Rescoped 2026-05-15:** STE-280/281/282 cancelled on Linear (`statusType: canceled`). Rationale: medium-severity observability-only gaps where the underlying features work correctly; not urgent enough to block M70 release. Gate-check probes (#38–48 family) + smoke probes will catch any drift if it matters later. Decision documented here so future audits understand the cancellation isn't drift — it's a deliberate scope tightening.
- Brainstorm sessions: 2026-05-13 (STE-284 — Approach 3 hybrid: helper preflight + `/gate-check` probe; auto-import tracker→local silently; defense in depth). 2026-05-14 (STE-294 + STE-295 — Approach 2: standalone STE-226 carve-out FR + fixture-group FR bundling F3/F4/F5/F11/severity-format-meta).
