# M66: Socratic-loop hardening (universal AskUserQuestion) {#M66}

Release target: v2.17.0
Codename: TBD

## Summary

Close the autonomous-mode contract on the **per-conversation Socratic-loop** side. STE-232 (M65) closed the per-step data-gate side; STE-237 closes the symmetric loop-entry side. The recurrence class STE-232 missed: the model can skip a Socratic clarification loop entirely (under autonomous-mode reminder + verbose pre-baked args) before any gated step fires. Universal `AskUserQuestion` mandate (B-side) makes the loop structurally impossible to skip; `/smoke-test` Phase 8 + `/gate-check` probe (C-side) enforces source and behavior.

Triggered by the v2.16.0 magpie-incident gist (`https://gist.github.com/nesquikm/2904e50c7213b6aa392b998d4137f609`) — `/setup` scaffolded an entire plugin (plugin.json, .mcp.json, 7 SKILL.md files, README, CLAUDE.md) without asking a single clarifying question, despite the skill body's `requires-input:` annotations and Pattern-26 prose.

## FRs in scope

| Tracker ID | Title | ACs |
|---|---|---|
| STE-237 | Socratic-loop refusal protocol — universal AskUserQuestion mandate + first-turn contract | 7 |
| (others queued — TBD)               |                                                                                          |     |

## Dependencies

- STE-226 (M59, archived) — load-bearing for the auto-approve marker; contrasted in STE-237's protocol section as the per-step gate's default-apply analog.
- STE-232 (M65, archived) — load-bearing for the per-step refusal protocol; STE-237 is the symmetric loop-entry layer.
- STE-220 (archived) — cautionary precedent: prose-only carve-outs failed at runtime; STE-237's design must be byte-checkable, not prose-only.

## Gate commands

(populated when implementation starts)
