# M66: Auto-mode / refusal-protocol hardening — round 2 {#M66}

Release target: v2.17.0
Codename: TBD

## Summary

Round 2 of the autonomous-mode contract hardening that began with STE-226 (M59 marker) and STE-232 (M65 per-step refusal). Two recurrence vectors closed:

- **Socratic-loop side (STE-237)** — STE-232 closed the per-step data-gate side; STE-237 closes the symmetric loop-entry side. The recurrence class STE-232 missed: the model can skip a Socratic clarification loop entirely (under autonomous-mode reminder + verbose pre-baked args) before any gated step fires. Universal `AskUserQuestion` mandate (B-side) makes the loop structurally impossible to skip; `/smoke-test` Phase 8 + `/gate-check` probe (C-side) enforces source and behavior.
- **Capability-row emission side (STE-238)** — `/conformance-loop` iteration 1 (2026-05-07) flagged a systemic gap: SKILL.md prose for STE-226 / STE-228 / STE-230 specifies byte-checkable capability-key tokens (`spec_write_draft_default_applied`, `branch_gate_default_applied`, `spec_research_invoked`, etc.) that the runtime LLM emits as narrative prose, not literal tokens. Downstream `/gate-check` probes that grep for these tokens cannot verify the contract. Same STE-220 lesson re-applies; same milestone theme.

Both FRs share the same originating lesson (STE-220: prose-only enforcement drifts; byte-checkable structural verification is required) on different surfaces.

Triggered by:

- v2.16.0 magpie-incident gist (`https://gist.github.com/nesquikm/2904e50c7213b6aa392b998d4137f609`) for STE-237 — `/setup` scaffolded an entire plugin without asking a single clarifying question.
- `/conformance-loop` iteration 1 capture (`/tmp/dpt-conformance-loop-2026-05-07-iter-1.md`) for STE-238 — F1+F3+F4 systemic gap on `/spec-write` audit-row emission.

## FRs in scope

| Tracker ID | Title | ACs |
|---|---|---|
| STE-237 | Socratic-loop refusal protocol — universal AskUserQuestion mandate + first-turn contract | 7 |
| STE-238 | Capability-row instrumentation — emit literal STE-226/228/230 keys in closing summaries | 8 |

## Dependencies

- STE-226 (M59, archived) — load-bearing for the auto-approve marker; contrasted in STE-237's protocol section as the per-step gate's default-apply analog. STE-238 closes the capability-row emission gap left open by STE-226.
- STE-228 (M61, archived) — origin of `branch_gate_*` capability-key tokens; STE-238 closes their runtime-emission gap.
- STE-230 (M61, archived) — origin of `spec_research_*` capability-key tokens; STE-238 closes their runtime-emission gap.
- STE-231 (M61 or later, archived) — supplies the lenient-assertion smoke-test shape used by STE-238 AC.5 (Phase 9 fixtures).
- STE-232 (M65, archived) — load-bearing for the per-step refusal protocol; STE-237 is the symmetric loop-entry layer; STE-238 mirrors its `imputed:`-style audit-trail extension on the closing-summary surface.
- STE-220 (archived) — cautionary precedent: prose-only carve-outs failed at runtime; both M66 FRs' designs must be byte-checkable, not prose-only.

## Gate commands

(populated when implementation starts)
