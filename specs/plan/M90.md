---
milestone: M90
status: active
kickoff_branch: fix/ste-340-dart-analyzer-stale-retry
frozen_at: null
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M90: Dart-analyzer stale .dart_tool self-heal {#M90}

**Goal:** Stop the `dart-analyzer` signature-extraction strategy from silently degrading to `regex-fallback` when the bundled helper's `.dart_tool` is present-but-stale (its pinned `analyzer` version evicted from `~/.pub-cache`). On a non-zero `dart run` exit, refresh deps once (`dart pub get`) and retry before falling back. Single fix-class FR.

**Prerequisites:** None. Builds on STE-103 (dart-analyzer strategy + bundled helper).

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-340 | Self-heal stale .dart_tool — retry dart pub get before dart-analyzer fallback | linear:`STE-340` |

**Tasks:**

- [x] Extract `runDartPubGet` + `runDartExtractOnce` helpers from `extractViaDartAnalyzer` (`adapters/_shared/src/signature_extractor.ts`); add a `runFailed` discriminator separating a non-zero `dart run` exit from a parse/empty/not-array failure. — STE-340
  verify: `bun test signature_extractor_dart.test.ts` green (14 tests; existing fallthrough reasons unchanged).
- [x] Add the single retry: on `!ok && runFailed`, run `dart pub get` once and retry the extraction; on refresh failure keep the original run reason. — STE-340
  verify: new stale-`.dart_tool` test yields `strategy: dart-analyzer`; removing the retry makes it RED.
- [x] New test under `AC-STE-103.2 fallthrough branches` driving a stateful stub (`run` fails until `pub get` writes a `.resolved` marker). — STE-340
  verify: `bun test` full gate from `plugins/dev-process-toolkit` GREEN (3323 pass, 0 fail).

**Tests:**
- `signature_extractor_dart.test.ts` — new `stale .dart_tool` retry test (stateful stub: run fails first → pub get → retry succeeds → `dart-analyzer`); the five pre-existing AC-STE-103.2 fallthrough tests stay green.

**Acceptance Criteria:**
- [x] All AC-STE-340.1–.5 satisfied (single bounded retry, run-exit-only trigger, refresh-failure preserves original reason, first-use + fallthrough unchanged, tests green).

**Gate:** `bun test` GREEN + `/dev-process-toolkit:gate-check` GREEN on this repo.

**Release target:** v2.33.1 (patch — pure fix-class milestone, every FR `changelog_category: Fixed`). Codename TBD at `/ship-milestone` (working: "Refresh").
