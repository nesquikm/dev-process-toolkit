---
status: active
archived_at: null
kind: feature
---

# M67: Refusal-protocol round 3 {#M67}

Release target: v2.18.0
Codename: TBD

## Summary

Round 3 of the autonomous-mode contract hardening sequence (M58 STE-226 marker, M65 STE-232 per-step refusal, M66 STE-237 Socratic-loop entry / STE-238 capability-key emission). M67 closes the two surfaces `/conformance-loop` iter-1 (2026-05-08, post-M66-merge) caught:

- **F1 (Pattern 26 entry-side drift)** — cross-tracker confirmed: `/spec-write` violates on Linear (`Write@21`), `/setup` violates on Jira (`Write@17`). Phase 8 catches it; M67 strengthens skill-body prose + adds post-hoc commit-side detection.
- **F2 (marker-absent silent-commit path)** — `claude -p` non-tty stdin produces "dismissed" `AskUserQuestion` responses; model self-rationalizes "safe defaults" and lands silent commits (smoke captured `9b75b4b`). M67 routes the dismissed-non-tty path through `requireOrRefuse(...)` deterministically.

Detection-only scope; non-interactive only — interactive (tty) sessions are byte-identical to v2.17.0. M68 candidate: runtime PreToolUse harness hook for F1 enforcement IF M67 detection-only proves insufficient at the next `/conformance-loop` iteration.

Triggered by:

- `/conformance-loop` iter-1 2026-05-08 cross-tracker run (Linear + Jira both surfaced F1; Linear surfaced F2 silent commit).
- Reproducer fixture committed at `plugins/dev-process-toolkit/tests/fixtures/socratic-first-turn/spec-write-2026-05-07.json` (213 KB stream-json transcript, SHA `79be499`).

## In Scope

- [ ] STE-251 — Strengthen Pattern-26 first-turn + refusal contracts under non-interactive stdin
  - verify: `bun test plugins/dev-process-toolkit/adapters/_shared/src/{require_or_refuse,socratic_first_turn_post_hoc_drift,socratic_first_turn_replay}.test.ts` exits 0; re-run `/conformance-loop --jira-project DST --max-iterations 1` post-implement and confirm Phase 8 returns `ok-asked` / `ok-refused` for all 4 in-scope skills + zero silent commits land via `claude -p` marker-absent path.

## Out of Scope (deferred to M68 candidate)

- Runtime `PreToolUse` harness hook for F1 enforcement (would actively block `Write`/`Edit`/`NotebookEdit` before first `AskUserQuestion` under `-p`). Detection-only is the M67 contract; runtime enforcement is a candidate for M68 IF Phase 8 in the next `/conformance-loop` iteration still surfaces F1 violations.
- Interactive-session prompt redesign — operator-driven scope guarantee: M67 is non-interactive-only.
- Rewriting in-scope skill bodies beyond the lead-clause prepend (STE-237's existing `AskUserQuestion` discipline stays as-is).

## Risks

- **Risk:** detection-only scope means the model is still free to scaffold before asking on first-turn under `-p`; the only fence is post-hoc Phase 8 in smoke runs. **Severity:** medium. **Mitigation:** Phase 8 fires every `/conformance-loop` invocation (now de-budget-capped), so drift is caught before any release commit. M68 runtime hook stays in the design space if this proves insufficient.
- **Risk:** `process.stdin.isTTY` detection may be inconsistent across environments (Docker, CI, claude-st specifically). **Severity:** low. **Mitigation:** unit-test matrix covers `(tty, answered)`, `(tty, declined)`, `(non-tty, answered)`, `(non-tty, dismissed)` via `Object.defineProperty(process.stdin, 'isTTY', { value })`; integration via Phase 8 (already running `claude -p` in `/smoke-test`).

## Done When

- STE-251 fully implemented (5 ACs green); plugin tests pass; gate-check passes 45/45 (one new probe).
- Re-run `/conformance-loop --jira-project DST --max-iterations 1`: Phase 8 returns `ok-asked` / `ok-refused` for all 4 in-scope skills on both legs; no silent commits from marker-absent `/spec-write` non-tty paths.
- Manual interactive smoke pass (AC-STE-251.5): operator-witnessed note in the FR's Phase 4 report confirms byte-identical interactive behavior vs. v2.17.0.
