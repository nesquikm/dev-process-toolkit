---
milestone: M89
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
---

# Implementation Plan

## Milestone Order

<!-- Each milestone gets its own `specs/plan/M<N>.md` file; don't proceed to M<N+1> until M<N> gates pass. -->

## M89: Cross-branch & Jira milestone-collision hardening {#M89}

**Goal:** Eliminate `M<N>` milestone-number allocation collisions across parallel/unmerged git branches, worktrees, and clones by extending the union scan in `nextFreeMilestoneNumber` with two best-effort legs — a git-branch scan (FR-A) and Jira milestone-label enumeration (FR-B). Picks up the branch-aware scanning STE-284 explicitly deferred.

**Prerequisites:** None. FR-A and FR-B are independent (distinct code surfaces) and may ship in either order.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-338 | Cross-branch milestone-number scan in nextFreeMilestoneNumber | linear:`STE-338` |
| STE-339 | Jira listMilestones() via milestone-label enumeration | linear:`STE-339` |

**Tasks:**

_FR-A — STE-338 (git-branch scan leg):_
- [ ] Add `adapters/_shared/src/branch_milestone_scan.ts` — `scanBranchMilestones(repoRoot, opts?)` over `refs/heads` (+ `refs/remotes` by default), `ls-tree` for `specs/plan/M<N>.md` (+ archive), opt-in `fetch`, fail-soft to `[]`.
  verify: `bun test branch_milestone_scan.test.ts` green (temp-repo union of two branches + a remote-tracking ref; no-fetch default issues no network; non-repo dir → `[]`).
- [ ] Extend `nextFreeMilestoneNumber` with an injected `branchScanner` param and a `sources.branches` field; union into `next`.
  verify: `bun test next_free_milestone_number.test.ts` green (injected scanner unions; omitted scanner ⇒ `branches: []`, `next` unchanged from the four-way baseline).
- [ ] Wire fetch-policy resolution into `/spec-write` § plan.md allocation guard: `--no-fetch` > `--fetch` > `milestone_scan_fetch` config > default `false`; inject the scanner; add the `branches` diagnostic row.
  verify: SKILL.md § plan.md documents the precedence + branch-inclusive scan; the explicit-`M<N>` refusal path lists all five source breakdowns.
- [ ] Document `milestone_scan_fetch` (default `false`, mode-agnostic) in the CLAUDE.md template.
  verify: `grep milestone_scan_fetch plugins/dev-process-toolkit/templates/CLAUDE.md.template` hits with the default-`false` note.

_FR-B — STE-339 (Jira listMilestones enumeration):_
- [ ] Implement `listMilestones()` on the Jira `AdapterDriver` — JQL label-scan, client-side `^milestone-(M\d+)$` filter, dedupe, paginate, fail-soft to `[]`, return `{ name: "M<N>" }[]`.
  verify: `bun test` Jira `listMilestones` cases green (mixed labels → deduped+sorted M-tokens; failure → `[]`; two-page pagination).
- [ ] Document the label-scan in `adapters/jira.md`; mark STE-284 AC-STE-284.7 fulfilled and repoint STE-329's "does NOT implement listing" caveat here.
  verify: `grep -n listMilestones plugins/dev-process-toolkit/adapters/jira.md` shows the new section + supersede note.

**Tests:**
- `branch_milestone_scan.test.ts` (temp git repo: multi-branch + remote-tracking union, no-fetch default, fetch path, fail-soft).
- `next_free_milestone_number.test.ts` (branch-union via injected scanner; vacuous omitted scanner).
- Jira adapter unit: `listMilestones` label-scan (mixed labels, failure → `[]`, pagination); `tracker_provider` delegation now non-empty for Jira.

**Acceptance Criteria:**
- [ ] All AC-STE-338.1–.8 satisfied (branch-scan module, opt-in fetch, fail-soft, helper integration, fetch-policy precedence, diagnostic+refusal, config+docs, tests green).
- [ ] All AC-STE-339.1–.6 satisfied (label-scan listing, paginate-no-silent-cap, fail-soft, label scope, tests green, docs+supersede).

**Gate:** `bun test` GREEN + `/dev-process-toolkit:gate-check` GREEN on this repo.

**Release target:** v2.33.0 (provisional, minor). Codename TBD at `/ship-milestone` (working: "Census").
