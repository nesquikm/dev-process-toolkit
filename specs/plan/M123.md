---
milestone: M123
status: active
archived_at: null
kickoff_branch: feat/m123-deliver-orchestrator
frozen_at: null
migration: none
shipped_in: null
---

# Implementation Plan

## M123 — Deliver Pipeline Orchestrator {#M123}

**Goal:** Automate the pipeline the 2026-08-15 M122 run drove by hand: a single `/deliver` command that keeps the Socratic phases (`/brainstorm`, `/spec-write`) inline with the human and runs the mechanical chain (`/implement` → `/ship-milestone` → `/pr`) in one fresh, visible agent-toolkit worker per milestone, serially, with fenced stage hand-offs, relayed approval gates, and `merge_policy`-routed PR closure.

**Prerequisites:** M122 shipped (v2.62.0, merged). The agent-toolkit plugin is a runtime dependency of the spawned phase — its absence is a loud pre-flight halt, never a silent substitution.

**Release target:** v2.63.0

**Migration:** `none` — the `## Orchestration` block is optional with absence ≡ defaults; no existing consumer artifact changes shape.

### FRs

| FR | Title | Status |
|---|---|---|
| [STE-463](../frs/STE-463.md) | Orchestration config: ## Orchestration CLAUDE.md block + reader helper | active |
| [STE-464](../frs/STE-464.md) | /deliver pipeline orchestrator: inline Socratic phases + spawned visible ceremony chain | active |

### Tasks

1. Config reader module + template stub + tests — AC-STE-463.1..7.
   verify: `bun test orchestration_config.test.ts` green; mutation checks red as specified.
2. /deliver SKILL + reference doc + count-pin updates — AC-STE-464.1..4, .9.
   verify: `bun test` full root green with the new counts; NFR-1 caps pass.
3. Stage contract + gate relay + merge-policy routing prose with meta-test pins — AC-STE-464.5..7.
   verify: grep pins hit the exact banner/refusal/install literals.
4. Headless refusal + smoke fixture — AC-STE-464.8, .10.
   verify: fixture red under mutation, green live; `SMOKE_LEGS` byte-identical.

### Dependency graph

STE-463 → STE-464 (the skill consumes the config reader). Tasks 1 → 2 → 3 → 4.
