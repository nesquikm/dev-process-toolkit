---
milestone: M116
status: active
archived_at: null
kickoff_branch: null
frozen_at: null
migration: none
---

# Implementation Plan

## M116 — Headless Conformance Unblock {#M116}

**Goal:** Close the eight high-severity findings from the 2026-07-27 `/conformance-loop` run plus one high-severity identity-collision finding from a parallel read-only audit. The keystone is STE-418: `/spec-write` hard-refuses under `claude -p` because `AskUserQuestion` is unregistered there, which truncates the canonical chain at step 2 and makes `/implement` and `/spec-review` unreachable. Until it lands, every future headless smoke or conformance run fails identically and the toolkit has no end-to-end signal.

**Prerequisites:** none for STE-418, STE-420, STE-421, STE-422, STE-423, STE-424. STE-419 depends on STE-418 landing first — it adopts whatever sanctioned non-interactive mechanism STE-418 establishes, and fixing `/setup` before that mechanism exists would regress the chain further rather than improving it.

**Release target:** TBD — set at ship time. Latest shipped is v2.56.0 ("Mint"); M117 is also unshipped and may land first or alongside.

**Migration:** `none` — no consumer artifact convention changes. STE-424 explicitly rules out any identifier-format change, so existing minted ids, filenames, prefixes and tokens keep resolving unchanged.

**Provenance:** aggregate report at `/tmp/dpt-conformance-loop-2026-07-27-iter-1.md`; per-tracker findings at `/tmp/dpt-smoke-findings-2026-07-27-{linear,jira}.md`; identity audit at `/tmp/ulid-collision-research-2026-07-27.md`.

**FR list:**

| FR | Title | Tracker |
|----|-------|---------|
| STE-418 | Restore headless drivability of /spec-write under claude -p | linear:`STE-418` |
| STE-419 | Enforce the first-turn Socratic contract in /setup | linear:`STE-419` |
| STE-420 | Make driver abort verdicts machine-checkable when the exit code cannot carry them | linear:`STE-420` |
| STE-421 | Replace raw-log capability-key greps with assistant-scoped assertions | linear:`STE-421` |
| STE-422 | Pass projectRoot to the Phase 8 assert runner | linear:`STE-422` |
| STE-423 | Tracker-scope every smoke artifact path and pidfile glob | linear:`STE-423` |
| STE-424 | Detect mode-none short-ULID collisions across the archive boundary | linear:`STE-424` |

**Tasks:**

- [ ] Establish the sanctioned non-interactive answer path for `/spec-write` and gate it on the existing marker byte-grep — STE-418
  - verify: a non-tty child carrying the marker completes new-FR creation and returns a tracker id in a session with no ask tool; the same child without the marker still raises the canonical refusal
- [ ] Add the three non-trigger fixtures (autonomous-mode reminder, pre-baked args prose, standing-instruction paraphrase) and the refusal-marker-in-assistant-text assertion — STE-418
  - verify: each of the three fails to unlock the path; the refusal marker is present in the assistant-text projection, not merely in a tool_result payload
- [ ] Prove the chain completes: full smoke leg produces all six per-skill captures — STE-418
  - verify: `assertChainIntegrity` over the six captures returns zero findings; the two truncation rows for `/implement` and `/spec-review` are gone
- [ ] Route every `/setup` pre-baked gate site through the marker/refusal helper and add the first-turn scaffold-write guard — STE-419
  - verify: under non-tty with no marker, `assertFirstTurnShape` returns ask-or-refuse and the tree is byte-unchanged; with the marker, bootstrap completes
- [ ] Emit a per-tracker machine-readable verdict artifact from the final-message self-check — STE-420
  - verify: an aborting leg writes outcome plus trigger; a clean leg writes a pass outcome; reader unit cases cover malformed and absent
- [ ] Gate Phase A on the verdict artifact independently of the collected exit code — STE-420
  - verify: rc=0 plus an abort artifact aborts the iteration; rc=0 plus a pass artifact clears it; a missing artifact is treated as failure
- [ ] Restate or satisfy every SKILL clause asserting the abort must exit non-zero — STE-420
  - verify: no clause is left asserting a guarantee the runtime cannot provide; grep-shape meta-test pins the corrected wording
- [ ] Route Phase 2.X and Phase 9 capability assertions through the assistant-text projection and correct every token spelling — STE-421
  - verify: the pinned 2026-07-27 capture scores all five keys ABSENT; the stale-file-refs fixture asserts the underscored form
- [ ] Add the prose-versus-emission discrimination case and the positive-direction case — STE-421
  - verify: an emitted row still scores PASS; the same token appearing in explanatory prose after a refusal does not
- [ ] Pass the resolved test-project path as the assert runner's third argument and pin it — STE-422
  - verify: recorded brainstorm transcript scores ok-refused, recorded setup transcript still scores violation; meta-test asserts three arguments
- [ ] Tracker-scope the Phase 8 fixture path and all four pidfile glob sites, and correct the date-prevents-collision rationale — STE-423
  - verify: grep-shape meta-test finds no unscoped smoke artifact path or pidfile glob; the three abort clauses each carry the segment
- [ ] Add the tandem-isolation fixture and the null-glob guards — STE-423
  - verify: a simulated abort on one leg leaves a concurrently-live partner's pidfiles and processes untouched
- [ ] Add cross-file duplicate-basename detection to the plan walk and duplicate-tail detection to the FR walk, spanning active and archive together — STE-424
  - verify: an active plan and an archived plan deriving the same token fail the gate; the non-colliding case still passes
- [ ] Give FR minting a synchronous active-plus-archive existence predicate via the collision-guarded minter, and invert the archive-scan test — STE-424
  - verify: a seeded first-mint collision against an archived record retries to a distinct tail with the archived file byte-unchanged; the inverted test asserts the archive IS scanned
- [ ] Correct the two collision-guarantee prose over-claims — STE-424
  - verify: both sites state detection within one working tree rather than prevention by construction
- [ ] Full gate green — M116
  - verify: `bun test` from `plugins/dev-process-toolkit` green with no probe regressions; `/gate-check` clean
