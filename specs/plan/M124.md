---
milestone: M124
status: active
archived_at: null
kickoff_branch: feat/m124-best-practices
frozen_at: null
migration: none
shipped_in: null
---

# Implementation Plan

## M124 — Best-Practice Sources {#M124}

**Goal:** Give projects a curated, gate-checked catalog of house best-practice documents and make `/implement`'s review actually consult it. A parallel twin of the deps stack (manifest + module + management skill + seeding) plus a Phase 3 conformance lens that reads scope-matched docs directly — no new research fork, per the standing operator ruling — and a day-one hygiene probe per the twin-parity lesson.

**Prerequisites:** M123 shipped (v2.63.0, merged). This milestone is delivered via /deliver — its first production workload.

**Release target:** v2.64.0

**Migration:** `none` — the manifest is a new optional file; absence ≡ today's behavior. No existing consumer artifact changes shape.

### FRs

| FR | Title | Status |
|---|---|---|
| [STE-465](../frs/STE-465.md) | Best-practices manifest: specs/best-practices.yaml module + day-one hygiene probe | active |
| [STE-466](../frs/STE-466.md) | /best-practices management skill + /setup manifest seeding | active |
| [STE-467](../frs/STE-467.md) | /implement Phase 3 best-practices conformance lens | active |

### Tasks

1. Manifest module + shape refusal + tests — AC-STE-465.1..3, .5 (module half).
   verify: `bun test best_practices_manifest.test.ts` green; schema-guard mutations red.
2. Hygiene probe + registration + count-pin sweep — AC-STE-465.4, .5 (probe half).
   verify: `bun test` full root green with the new probe count.
3. /best-practices SKILL + count pins — AC-STE-466.1, .2, .4.
   verify: meta-test pins green; NFR-1 caps pass; probe #43 scopes the new SKILL cleanly.
4. /setup seeding step — AC-STE-466.3.
   verify: seeding meta-test green; /setup SKILL NFR-1 cap passes.
5. /implement lens prose + capability keys + pins — AC-STE-467.1..4.
   verify: token-registration tests green; lens grep pins hit.
6. Smoke fixture for the lens surface — AC-STE-467.5.
   verify: fixture red under mutation, green live; SMOKE_LEGS byte-identical.

### Dependency graph

STE-465 → STE-466 and STE-465 → STE-467; STE-466 before STE-467 (count pins land once, lens references the shipped skill). Tasks 1 → 2 → 3 → 4 → 5 → 6.
