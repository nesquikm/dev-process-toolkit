---
title: The pull-request gate finds the repository a request is opened from
milestone: M_85e846
status: archived
archived_at: 2026-09-20T18:40:05Z
tracker:
  linear: STE-615
created_at: 2026-09-18T13:32:37Z
changelog_category: Fixed
---

# The pull-request gate finds the repository a request is opened from

## Summary

The pull-request guard acts only when a command begins with the exact words gh pr create.
Change directory first, use a subshell, name the repository with a flag or an environment variable, or chain the request after a push, and the guard never looks.
It now finds pull-request creation through the same recogniser the commit guards use, works out which local checkout the request is opened from, and asks that checkout for its review evidence.

## Requirement

`templates/hooks/_lib/hooks/pre-pr-spec-review.ts:15-18` exits 0 unless the raw command matches `/^gh pr create\b/`. That line is byte-identical in 2.83.0, 2.85.0, 2.86.0 and HEAD. STE-597 recorded it as a known gap it left out of scope (`specs/frs/archive/STE-597.md`, Implementation notes). Measured on the installed 2.86.0 hook (skeptic repro `verify-hooks/drive.ts`, session rooted in FE, no spec-review in the transcript): the bare `gh pr create` is refused (P00). These all exit 0 before any evidence is looked at: `cd BE && gh pr create` (P01), `(cd BE; gh pr create)` (P02), `gh -R org/be pr create` (P03), `GH_REPO=org/be gh pr create` (P04), `git push -u origin b && gh pr create` (P05) and the newline-chained push-then-create (P07). Live traffic did exactly this. FE session `3ca0ab95` was refused on a bare FE `gh pr create` at 1-based line 983. At line 1016 it opened BE's PR #62 with `cd …/glacy-app-be && gh pr create`, with no refusal. FE session `80bbcf8c` (line 1071) did the same with no spec-review in the session at all. A rough spec-time regex scan of 85,948 distinct Bash commands found 140 that mention `gh … pr create`, and 64 that start with it. Some of the rest are heredoc or echo mentions, which is why the replay hand-classifies them. `skills/pr/SKILL.md:41` and `docs/honored-contracts.md:54` say the hook refuses "the `gh pr create` Bash call", which overstates what it does.

**Decision: recognition.** PR creation is read through the shared recogniser (STE-601, STE-613), so every wrapper, separator, nested-shell, substitution and directory row of its shape table applies unchanged. An invocation is PR creation when its argv0's final path segment is exactly `gh` and, after gh's flags (`-R`/`--repo` with a value, in either spelling, before `pr` or after `create`), its words are `pr create` or `pr new` (gh's built-in alias for create, measured on gh 2.95.0: its help lists `gh pr new` under ALIASES). `url=$(gh pr create …)` is creation, and so are `--dry-run` and `--web`, conservatively. `gh pr create --help` and `-h` are not creation: HEAD's anchored regex refuses them, measured during this review, and the new resolver must not. These are not PR creation, and each is pinned and listed in the hooks manual: `gh pr list`, `gh pr view`, `gh pr edit`, `gh pr merge` and `gh pr checkout`; `gh api …/pulls` with any method; `hub pull-request`; `git push -o merge_request.create`; `gh pr create` inside a script file; a user-defined `gh alias` (expanding it needs the user's gh config); and an opaque `bash -c "$X"`. A heredoc body that mentions `gh pr create` is not creation.

**Decision: the target.** A new module, `adapters/_shared/src/pr_target_repo.ts`, exports `resolvePrTargetFromPayload(payload)`. It returns the same `CommitTarget`-shaped verdict, with `isPr` in place of `isCommit`, `repoRoot`, `repoRoots` and `unresolved`. The local checkout is the checkout root of the directory the invocation runs in. A repository slug comes from `-R`/`--repo` (which takes precedence), or from `GH_REPO` set as a prefix assignment, as an `env` operand, or by an in-command `export`, or failing all of those from `GH_REPO` in the hook process's own environment, which the Bash tool's shell inherits from the same launch. When a slug is given, it must match a remote of that checkout. The remotes are read with `git remote -v` run in that checkout. Matching accepts https and ssh URL forms, an optional `.git` suffix and an optional `HOST/` prefix, and is case-insensitive.

- A slug that no remote matches is a known-foreign target, and the hook refuses it with exit 2 even when evidence is present. The refusal names the slug and the checkout, and the remedy names opening the request from that repository's own checkout. Evidence in one checkout cannot vouch for a request into another repository.
- An unexpanded slug, or a `git remote` that cannot be run, is unresolved. An unexpandable directory is unresolved too.

**Decision: the evidence.** The hook calls `requireSkillToolUse("dev-process-toolkit:spec-review", …)` with the target, and the rule from STE-614 applies without change. A toolkit-managed checkout needs a non-error Skill call plus a spec-review receipt in that checkout. An unmanaged checkout needs the transcript leg alone. Several checkouts need evidence in each. An unresolved target is evaluated against the session's own checkout and every candidate root the recogniser reports, and on success the hook exits 1 with a `Reminder:`, the operator-visible channel. The hook entry point carries no filesystem read of its own, which keeps the guard-family pin in `tests/hook-session-lib.test.ts` green. The /pr skill's own step 5 shape, a bare `gh pr create` in the session's own checkout, keeps working exactly as today once /spec-review has run for that checkout.

## Acceptance Criteria

- AC-STE-615.1: Recognition. `resolvePrTargetFromPayload` answers `isPr: true` for `gh pr create`, `/opt/homebrew/bin/gh pr create`, `gh -R o/r pr create`, `gh pr create --repo=o/r`, `cd <B> && gh pr create`, `(cd <B>; gh pr create)`, `git push -u origin b && gh pr create`, `git push -u origin b` newline `gh pr create`, `GH_REPO=o/r gh pr create`, `env GH_REPO=o/r gh pr create`, `command gh pr create`, `bash -lc 'gh pr create'`, `url=$(gh pr create --fill)`, `gh pr new`, `cd <B> && gh pr new` and `gh pr create --dry-run`. It answers `isPr: false` for `gh pr list`, `gh pr view 1`, `gh pr merge 1`, `gh api repos/o/r/pulls -X POST`, `hub pull-request`, `git push -o merge_request.create`, `bash make_pr.sh`, `bash -c "$X"`, `gh pr create --help`, `gh pr create -h` and `cat > f.md <<'EOF'` with a body mentioning `gh pr create`. Each out-of-scope row is named by its test.
- AC-STE-615.2: The target. Session rooted in FE, checkouts built on `makeSpanFixture` with `git init` and `origin` remotes `git@github.com:org/fe.git` and `https://github.com/org/be` respectively. `cd <BE> && gh pr create` resolves to BE. `gh pr create` resolves to FE. `gh pr create --repo org/fe` and `gh pr create -R github.com/ORG/FE` resolve to FE. `cd <BE> && GH_REPO=org/be gh pr create` resolves to BE. `--repo` wins over `GH_REPO` when both are given. `gh pr create` from FE with `GH_REPO=org/be` in the hook process's environment is known-foreign, and with `GH_REPO=org/fe` there it resolves to FE. `gh pr create --repo org/be` from FE is a known-foreign target naming `org/be`. `gh pr create --repo "$R"` is unresolved, naming `$R`. `gh pr create --repo org/fe`, with the remote lookup made to fail (git absent from the `PATH` the resolver spawns with), is unresolved, naming the remote lookup. `gh pr create --repo org/fe` from a checkout with no remotes is known-foreign. A slug that is neither `OWNER/REPO` nor `HOST/OWNER/REPO` (`--repo fe`) is refused, naming the slug. `cd <FE linked worktree> && gh pr create` resolves to the worktree's root, not to FE's main checkout.
- AC-STE-615.3: The HS-2 table, graded through the shipped bash wrapper on those checkouts, with the session rooted in FE. P01 and P02 (`cd <BE> && gh pr create`, `(cd <BE>; gh pr create)`) resolve to BE. P00, P05 and P07 (bare, push-then-create, newline-chained) resolve to FE. P03 and P04 (`gh -R org/be pr create`, `GH_REPO=org/be gh pr create`) are known-foreign from FE. With no spec-review evidence, P01, P02, P03, P04, P05 and P07 each exit 2, where each exited 0 at HEAD, and P00 still exits 2 with the byte-identical plain-miss refusal. With evidence only for FE (a non-error spec-review Skill call plus an FE receipt), P00, P05 and P07 exit 0 with empty stderr; P01 and P02 exit 2, naming BE and the remedy `/dev-process-toolkit:spec-review` run in BE's checkout, and never the front-door command line; P03 and P04 exit 2, naming the slug. With a BE receipt added in the same session, P01 and P02 exit 0 with empty stderr, so the permitted request is graded as well as the forbidden one. `cd <BE> && gh -R org/be pr create` and `cd <BE> && GH_REPO=org/be gh pr create` exit 0 with empty stderr, because the slug matches BE's own remote. P03 and P04 still exit 2, because evidence cannot vouch for a known-foreign target. Two rows that HEAD gets wrong in opposite directions: `gh pr new`, with no evidence, exits 2 where it exited 0 at HEAD; `gh pr create --help`, with no evidence, exits 0 with empty stderr where it exited 2 at HEAD.
- AC-STE-615.4: The unresolved leg. `gh pr create --repo "$R"`, with evidence for the session's own checkout, exits 1 with a `Reminder:` naming `$R`. The same command without that evidence exits 2.
- AC-STE-615.5: Existing suites. `tests/hook-modules-pre-pr-spec-review.test.ts` and `templates/hooks/__tests__/pre-pr-spec-review.test.ts` keep their `gh pr list`, `git push`, empty-stdin and malformed-stdin cases unedited and green: exit 0, with empty stderr where they assert it. Their evidence-present happy paths gain a front-door-written receipt in the fixture, and each gains a sibling without the receipt that exits 2. `tests/_blocking_gates.ts` still derives the PR gate with its skill and hook names, with no edit by this FR.
- AC-STE-615.6: Announcing surfaces. `skills/pr/SKILL.md` (the sentence at line 41), `docs/honored-contracts.md` (the pre-pr-spec-review section), `docs/hooks-reference.md` (the pre-pr-spec-review Matcher and Requirement lines, plus the PR rows of the "Recognised command shapes" table) and `docs/workflow-overview.md` state which shapes are recognised and that the evidence is read from the checkout the request is opened from. A test reads exactly those four markdown files, asserts the new wording, and carries a control that finds the overstated sentence in the HEAD bytes. The table rows are run through `resolvePrTargetFromPayload` by the table-driven test from STE-601. The frontmatter of `skills/pr/SKILL.md` is byte-identical, which keeps `tests/m_a8e09a-ste-576-pr-title-facts.test.ts` green. Its body gains no `STE-<N>` token, and it stays within the skill line cap.
- AC-STE-615.7: Real-traffic replay, using the helper from STE-601. This FR records, over the corpus, the PR-creating commands matched by the retired anchored regex, the ones recognised by the new resolver, and every command in the difference, hand-classified as real creation or a mention. False positives are zero, or each is named with its fix. The known-foreign and unresolved counts are recorded as well. An empty corpus is reported as not measured.
- AC-STE-615.8: Falsifiability. Every clause asserting new behaviour (AC.1 to AC.4, and AC.5's receipt siblings) fails on the pre-change bytes and passes on the post-change bytes, with the direction recorded per clause in this FR. Controls and byte-identity pins are exempt by construction.

## Technical Design

`pr_target_repo.ts` consumes the shared recogniser's invocation stream, exactly as `commit_target_repo.ts` does. It adds only the gh argv grammar and the slug-to-remote match, and it runs `git remote -v` through the same spawn-and-report pattern the /tdd hook uses, so a spawn failure is a named verdict, never a throw. The hook entry point becomes a thin caller: parse the payload, resolve the target, and demand evidence with the target. The anchored regex is deleted, not kept as a fast path.

## Testing

A new `tests/pr-target-repo.test.ts` covers recognition and target rows with real remotes on real `git init` checkouts. Hook-level pairs run through the bash wrapper, where every forbid row has a permit sibling. The replay helper supplies the real-traffic numbers.

## Notes

- A request to another repository's slug refuses. Evidence here cannot vouch for a checkout elsewhere.
- Both live bypasses happened while the two apps were in separate trackers.
- Closes STE-601's note that this hook still gates on the anchored `gh pr create` regex; STE-601 adopts the rest of STE-601.

## Implementation notes

### Real-traffic replay (AC.7)

Measured 2026-09-20 with `bun run plugins/dev-process-toolkit/tests/_command_traffic_replay.ts --pr-only` and `CLAUDE_CONFIG_DIR=/Users/ns/.claude-st`. The helper's new `--pr-only` mode runs the PR legs alone: the retired anchored regex `/^gh pr create\b/` (copied verbatim from `templates/hooks/_lib/hooks/pre-pr-spec-review.ts:16` at `ff41e4e4`, since this FR is what deletes the line) is the *before* side, and `resolvePrTargetFromPayload` is the *after* side. No base resolver is fetched with `git archive` and no commit classification runs, so the corpus is read once rather than classified twice per command. Roots resolve filesystem-free — every absolute directory is treated as its own checkout — and `git remote -v` is the one real lookup, memoised per root.

**Corpus: 4,075 transcript files, 91,123 distinct Bash commands.** Not empty, so this is measured; an empty corpus would have been recorded as `not measured` and nothing here would carry a figure.

- anchored regex PR-creating: 37
- resolver PR-creating: 80
- newly recognised as PR creation: 44
- no longer recognised as PR creation: 1
- known-foreign targets: 1
- unresolved targets: 1
- false positives: 0

The four sides reconcile: 37 − 1 + 44 = 80.

**Hand-classification, newly recognised (44 of 44 real PR creation, 0 mentions).** Every one carries a `gh … pr create` invocation that survives having each heredoc *body* removed, and in every one that invocation stands at a command position — at the start of the command, after `&&`, after `;`, or at the start of a line — never inside an `echo`, `printf`, `awk` or `cat` argument and never inside quoted prose. Measured shapes, which overlap: 28 begin a line with `gh pr create` (a line-anchored regex would have seen these; the hook's regex is string-anchored and did not); 28 run after a `cd` into the target checkout, 2 of them into a directory held in a variable (`WT=…/m46 && cd "$WT" && gh pr create`); 5 chain after a `git push`; 31 open a heredoc first, almost always `cat > …/pr-body.md <<'EOF'` to write the body; 5 open with a shell assignment. Named examples of the bypass class the FR's Requirement cites: `cd /Users/ns/workspace/st/glacy-app-be && gh pr create --draft …` (a BE request opened from an FE-rooted session), `cd ../glacy-docs && git push -q -u origin docs/… && gh pr create --draft …`, and `cd /Users/ns/workspace/agent-toolkit && … && gh pr create --base main …` — a request into a third repository entirely. None of the 44 is a heredoc, `echo` or quoted mention, so there is nothing to fix and no false positive to name.

**Hand-classification, no longer recognised (1 of 1 a correct *removal*).** The single command is `gh pr create --help 2>/dev/null | grep -i -A2 "alias"; gh --version | head -1` — a `gh` help query run while researching the `pr new` alias, not a creation. The retired anchored regex matched it and would have demanded spec-review evidence before letting a `--help` run; the resolver answers `isPr: false` on `gh.help`, as AC.1 and AC.3 require. This is a false positive *removed*, not one introduced.

**Known-foreign: 1, correct.** `gh pr create --draft --repo StellarTechLab/app_platform --base main --head fix/m_ab_125-analytics-identity-integrity …`, run with `cwd` `/Users/ns/workspace/st/glacy-app-fe`, whose only remote is `git@github.com:StellarTechLab/glacy-app-fe.git`. The slug names a repository no remote of that checkout matches, so it is refused by name — the live cross-repository shape this FR exists to catch, found in the corpus rather than argued for.

**Unresolved: 1, and it is a replay artefact rather than a resolver defect.** `gh pr create --draft --repo StellarTechLab/glacy-app-fe …` ran with `cwd` `/Users/ns/workspace/st/glacy-app-fe-wt-gf86fix`, a linked worktree that no longer exists on this machine. `git remote -v` therefore cannot run there *today*, and the resolver answers "unresolved" (ignorance) rather than "foreign" (knowledge) — exactly the distinction `RemoteLookup`'s `null` third answer exists for. When the command actually ran, the worktree existed and the slug matched its own remote, so the hook would have resolved it to that worktree and demanded evidence there.

**The 37 is lower than this FR's spec-time figure of 64, and that gap is the bypass, not a regression.** Re-scanning the same corpus (91,134 distinct commands — it grew by 11 between the two runs, minutes apart; it is a live store) with several regex shapes: `/^gh pr create\b/` string-anchored, which is what the hook actually ran, matches **37**; the same pattern with the `m` flag, anchored per *line*, matches **68**; a bare `gh pr create` substring matches **167**; the loose `gh … pr … create` mention shape matches **215**. The spec-time scan was line-anchored, so it counted 68-class commands (64 at the time) and over-credited the hook by roughly a factor of two: a multi-line command whose *second* line begins `gh pr create` was never seen by the guard at all. That accounts for 28 of the 44 newly recognised commands; the remaining 16 are `cd`-, assignment- and push-chained shapes that no anchored regex of either flavour would reach.

## Falsifiability

### Direction (AC.8)

Run 2026-09-20. A throwaway worktree was detached at **`fd10d40f`** (`fd10d40fc3ad125e4523f9d30c2eeed24d6f6033`) under `$TMPDIR` — the last commit before STE-615 work began, and the same sha `tests/ste-615-pr-gate.test.ts` pins as its `BASE`, which is how the grading clause joins the two records. Only the suite files were copied into it from the working tree: the three this FR adds (`tests/pr-target-repo.test.ts`, `tests/ste-615-pr-gate.test.ts`, `tests/ste-615-surfaces.test.ts`) and the four whose clauses it changed (`tests/ste-601-shapes-table.test.ts`, `tests/ste-614-evidence-rule.test.ts`, `tests/hook-modules-pre-pr-spec-review.test.ts`, `templates/hooks/__tests__/pre-pr-spec-review.test.ts`). Every source file stayed at base bytes — `tests/_command_traffic_replay.ts` included, because the replay helper is not a collected suite and its change is therefore part of the subject rather than part of the instrument. Each file was run on its own in both trees with `--reporter=junit`, and the results joined by nested describe path plus test name. The worktree was removed with `git worktree remove --force`; the repository's own working tree was never checked out, stashed or reset.

**Two base legs, because the first one proves less than it looks.** `adapters/_shared/src/pr_target_repo.ts` is a file this FR *creates*, so on the base bytes `tests/pr-target-repo.test.ts` dies at module level with `Cannot find module` — one unnamed error standing in for 42 clauses. A red obtained that way credits the change for its own file's absence and records nothing about the pre-change *rule*. So a second base leg was run in the same worktree with a stand-in `pr_target_repo.ts` restating the pre-change rule exactly as the base hook ran it — `isPr = /^gh pr create\b/.test(cmd)`, the retired anchored regex from `templates/hooks/_lib/hooks/pre-pr-spec-review.ts:27` at the base — behind the new module's signature. The stand-in is deliberately **generous** to the base: its target fields answer with the checkout the command runs in, the one thing the pre-change world could have known for free, so that clauses the old rule happens to satisfy are recorded as passing on both rather than as manufactured reds. **Every direction below is read from leg 2.** Leg 1 is reported only as the reason leg 2 was needed, and the two legs are identical for the other six suites (same failure counts, case for case) — the stand-in touched one file's measurement and contaminated nothing else.

**Totals.** 239 joined cases across the seven suites. **153 clauses are added or amended by this FR**; the other 86 are prior-FR clauses that pass in both trees. Of the 153: **87 fail on the pre-change bytes and pass on the post-change bytes**, and **66 pass on both**. **No clause fails on the post-change bytes** — all seven suites are green in the working tree.

| AC | clauses | falsify (red on base → green post) | pass on both |
| --- | ---: | ---: | ---: |
| AC.1 — recognition | 27 | 15 | 12 |
| AC.2 — the target | 15 | 10 | 5 |
| AC.3 — the HS-2 table, through the shipped wrapper | 25 | 15 | 10 |
| AC.4 — the unresolved leg | 2 | 1 | 1 |
| AC.5 — existing suites, incl. both receipt siblings | 14 | 4 | 10 |
| AC.6 — announcing surfaces *(not in AC.8's list)* | 45 | 28 | 17 |
| AC.7 — real-traffic replay *(not in AC.8's list)* | 4 | 4 | 0 |
| AC.8 — this record | 16 | 10 | 6 |
| AC-STE-614.1's five `pr:` rows, amended here | 5 | 0 | 5 |
| **total** | **153** | **87** | **66** |

**AC.5's receipt siblings, named rather than folded in.** AC.5 is mostly a preservation AC: its `gh pr list`, `git push`, empty-stdin and malformed-stdin cases are unedited and green in both trees, and both happy paths pass on the base too, because the base gate read the transcript leg alone and a receipt it never looked at cannot change its answer. The two clauses AC.8's list actually names are the receipt siblings, one per suite — `tests/hook-modules-pre-pr-spec-review.test.ts` › *"SIBLING — the SAME case with the receipt REMOVED exits 2"* and the identically named case in `templates/hooks/__tests__/pre-pr-spec-review.test.ts`. **Both falsify**: exit 0 on the base, exit 2 now. The other two of AC.5's four are in `tests/ste-615-pr-gate.test.ts` — *"the entry point demands evidence with a resolved target"* and *"the retired anchored regex is GONE from the entry point"*.

**Clauses asserting new behaviour that nonetheless pass on the base, named individually.** **Twenty-five**, and each is a weakness in the clause rather than in the code — AC.8's literal text says every new-behaviour clause of AC.1 to AC.4 fails on the base, and twenty-five of them do not.

*How the twenty-five is derived, so it can be re-derived.* It is every clause inside AC.1 to AC.4 that asserts new behaviour, passes on the base, and is covered by no exemption. Read off the table above: AC.1 to AC.4 carry twenty-eight "pass on both" between them (12 + 5 + 10 + 1), of which three are exempt by construction — AC.3's two `git archive` controls and its byte-identical P00 refusal. 28 − 3 = 25. Seventeen of the twenty-five are AC.1's and AC.2's, which is what the generous stand-in was for: it says precisely which rows the retired regex already got right. Eight are AC.3's and AC.4's, and they are disclosed here only after a post-audit re-measurement; items 5 and 6 below say what they are and how they were measured. The three exempt clauses and AC-STE-614.1's five `pr:` rows are listed below as well, for completeness, and are not part of the twenty-five.

*This section first said eighteen.* That figure was 17 + 1: AC.1's and AC.2's, plus AC.7's empty-corpus clause. It omitted the eight of items 5 and 6 entirely, absorbing them instead into the "permit siblings and byte-identity pins" sentence further down — and AC.8's exemption reaches controls and byte-identity pins, never permit siblings. The arithmetic of the correction is 18 − 1 + 8 = 25: the eight are added, and AC.7's clause is removed because it now falsifies (see the note at the end of this list).

1. **AC.1, three creating rows the anchored regex already matched** — `gh pr create`, `gh pr create --repo=o/r` and `gh pr create --dry-run`. All three begin with the literal words the base matched on, so they were never part of the bypass. They are the reason the AC lists sixteen shapes rather than three.
2. **AC.1, nine out-of-scope rows the anchored regex already rejected** — `gh pr list`, `gh pr view 1`, `gh pr merge 1`, `gh api repos/o/r/pulls -X POST`, `hub pull-request`, `git push -o merge_request.create`, `bash make_pr.sh`, `bash -c "$X"` and the heredoc body. None of them starts with `gh pr create`, so a string-anchored regex declined them for the wrong reason and the new grammar declines them for the right one. Only `--help` and `-h` falsify here, and they are exactly the two rows the base got wrong in this direction.
3. **AC.2, five target rows that resolve to the session's own checkout.** A bare `gh pr create`, `--repo org/fe`, `-R github.com/ORG/FE`, `GH_REPO=org/fe` in the hook's own environment, and the `CONTROL` that puts `git` back on `PATH`. Every one of them names FE, and FE is the directory the command already runs in, so a stand-in that reports the running checkout lands on the right answer without doing any slug matching at all. Their forbid twins in the same block — the four known-foreign rows, the two unresolved rows, the `cd`-into-BE rows and the linked worktree — are what carry AC.2's direction, and all ten falsify.
4. **AC.3, the two `git archive` controls** *(exempt; listed for completeness, not part of the twenty-five)* — *"at the base, P01-P05 and P07 all exit 0"* and *"at the base, P00 exits 2 and `gh pr new` exits 0 and `gh pr create --help` exits 2"*. These extract the base plugin tree and run the base wrapper out of it, so they are byte-identical in both trees by construction. They are the falsifiability instrument, not a subject of it.
5. **AC.3, seven permit rows that the base already exited 0 on — every one of them for the wrong reason.** With evidence for FE only: P00, P05 and P07 (*"resolves to FE and exits 0 with empty stderr"*). With a BE receipt added: P01 and P02 (*"exits 0 with empty stderr — the permitted request is graded too"*), and the two slug-matches-its-own-remote rows, `cd <BE> && gh -R org/be pr create` and `cd <BE> && GH_REPO=org/be gh pr create`. Measured 2026-09-20 against the base wrapper on the same two-checkout fixture: **all seven exit 0 with empty stderr at the base.** P05, P07, P01, P02 and the two slug rows do not begin with the literal words `gh pr create`, so the base regex never looked at them and they passed in silence; P00 begins with them, and the base's transcript-leg-alone reading was satisfied by the FE Skill call. Not one of the seven distinguishes "permitted because the target was resolved and its evidence found" from "permitted because nothing was checked" — which is the whole of what this FR changes. They earn their place in the suite as the permit half of each forbid/permit pair, because a gate that refused everything would pass the forbid rows alone; they earn no direction. **A permit sibling that passes on the base is not exempt by construction. It is undirected, and it is named here rather than absorbed.**
6. **AC.4's sibling clause** — *"SIBLING — the same command without that evidence exits 2"*, on `gh pr create --repo "$R"`. Measured 2026-09-20: **exit 2 at the base**, with the base's own plain-miss refusal. The command begins with the literal words the base regex matched, so the base demanded transcript evidence, found none, and refused — the right answer by the old rule, reached without ever resolving `$R`. AC.4's direction is carried entirely by its twin, the exit-1 `Reminder:` clause, which falsifies.
7. **AC-STE-614.1's five `pr:` rows, all five** *(outside AC.1 to AC.4; listed for completeness, not part of the twenty-five)*. This FR re-pointed them from a BE receipt to an FE one, because a bare `gh pr create` now grades the checkout it is opened from. On the base the PR gate read the transcript leg alone, so which checkout held the receipt made no difference to its answer, and all five pass unchanged. A genuine amendment with zero direction, recorded here rather than counted as a pass — the same shape STE-614 recorded as its `(+1)` entries.

*Left this list in the post-audit round: AC.7's empty-corpus clause.* It asserted `expect(out).not.toMatch(/false positives: 0/)`, and `tests/_command_traffic_replay.ts` prints no `false positives:` line in any mode — it cannot, because a false positive is a hand-classification of the difference list, recorded in the Implementation notes above rather than computed. The clause therefore held for every corpus in both trees: not a clause that failed to discriminate, but one with no way to fail at all. It now asserts what the helper does print — that an empty corpus prints **none** of AC.7's six counts — and carries a positive control on a non-empty corpus whose counts are genuinely zero, so a renamed count line cannot make it pass by matching nothing. Measured 2026-09-20 by copying the amended suite file into the base worktree and running it there, leg-2 fashion: the base helper has no `--pr-only` mode, ignores the flag, and prints none of those six lines, so the positive control goes red. **The clause falsifies**, and AC.7's row above reads 4 / 4 / 0 rather than 4 / 3 / 1. That base run is also where AC.6's thirteen and AC.8's six below were re-counted rather than re-argued: `tests/ste-615-surfaces.test.ts` holds fifty of the joined cases (AC.6's thirty in-file, AC.7's four, AC.8's sixteen) and answers **22 pass / 28 fail** on the base bytes — and 13 + 3 + 0 + 6 = 22.

That leaves **thirty-four** of the sixty-six "pass on both": AC.3's byte-identical plain-miss refusal for P00 (1), AC.5's ten, AC.6's seventeen and AC.8's six. Every one of those is a control, a byte-identity pin or a preservation clause — the shapes AC.8's last sentence actually exempts — and the paragraph below names them one by one. Permit siblings are not among them.

**Controls and byte-identity pins are exempt by construction**, as AC.8's last sentence says: a clause that reads `git show fd10d40f:<path>` is identical in both trees by definition, and a pin asserting that something did *not* change cannot change. That reaches exactly these, and the exemption is not stretched past them:

- **AC.6's thirteen base-reading `CONTROL —` clauses** — three per surface across the four surfaces (the overstated sentence, the recognition phrase and "is opened from"), plus the Matcher-line control in the hooks-manual block.
- **AC.6's three budget pins** — the byte-identical frontmatter, the no-new-`STE-<N>`-token pin and the skill line cap.
- **the shapes-table control** that proves the PR grader is blind to the commit rows and the commit grader to the PR rows. 13 + 3 + 1 = 17, which is all of AC.6's "pass on both" and nothing else of it.
- **AC.8's six controls** — its four negative ones, which read a promise-only fixture compiled into the suite, and its two positive ones, which read STE-614's shipped record.
- **AC.5's ten** — the *"derived from unedited `tests/_blocking_gates.ts`"* pin, and the preservation cases AC.5 exists to hold still: `gh pr list`, `git push`, empty stdin and malformed stdin in both suites, and both evidence-present happy paths, which the base answered the same way because it read the transcript leg alone.
- **AC.3's byte-identical plain-miss refusal for P00**, which compares this tree's stderr with the base's and asserts they are the same bytes.

1 + 10 + 17 + 6 = 34, the figure above. **What this does not reach:** a permit sibling. A row that exits 0 under both rules is not a control (it grades the subject, not the instrument) and not a byte-identity pin (it asserts a verdict, not equality of bytes). Item 5 of the list above names the seven of those AC.3 carries, and item 6 AC.4's one, rather than counting them here.

**Limits of this measurement.**

- It grades the direction of *test clauses*, not of the AC prose. A clause that measures the wrong subject falsifies just as cleanly as one that measures the right one.
- Leg 2's stand-in module is a measurement artefact and was never committed. It exists only in the throwaway worktree, and only `tests/pr-target-repo.test.ts` and one clause of `tests/ste-601-shapes-table.test.ts` ever import it; the other six suites produced identical results with and without it.
- AC.7 (replay) is hand-run against a live transcript store and is not re-measurable to the same numbers; its own section above records it.
- AC.8's own ten clauses were red until this section was written, which is the direction the table records for them. They were re-run against these bytes after it was written, and all ten are green.
- **The post-audit round (2026-09-20) re-measured rather than re-reasoned.** The eight clauses of items 5 and 6 were run against the base wrapper directly: a throwaway `git worktree add --detach` at `fd10d40f` under `$TMPDIR`, the same `makeSpanFixture` FE/BE pair with the same `origin` remotes, the same three transcripts, and the same `git archive` extraction of the base plugin tree the suite itself uses — then each of the eight commands driven through **both** wrappers with the same payload. All eight answered identically in both trees. AC.7's empty-corpus clause was measured in the same worktree, by running the amended suite file against the base helper there. The worktree was removed with `git worktree remove --force`; the repository's own working tree was never checked out, stashed or reset. This round changed the record's numbers and one test assertion; it changed no shipped source file, so the seven suites' directions are otherwise unmoved.

<!-- token-stats:begin -->

## Token Stats

| skill | model | input | output | cache-read | cache-creation |
| --- | --- | ---: | ---: | ---: | ---: |
| subtotal |  | 0 | 0 | 0 | 0 |

<!-- token-stats:end -->
