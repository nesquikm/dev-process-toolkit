---
title: Every way of changing directory moves the commit with it
milestone: M_85e846
status: archived
archived_at: 2026-09-20T18:40:05Z
tracker:
  linear: STE-613
created_at: 2026-09-18T13:32:37Z
changelog_category: Fixed
---

# Every way of changing directory moves the commit with it

## Summary

Four ordinary ways of changing directory leave the test-first guard reading the session's own repository while the commit lands in another one.
The guard sees an empty staging area there and waves through a commit that stages a feature request and its test.
The directory model now follows each of them, and resolves the directories a command computes for itself when the answer is fixed by the command.
That shrinks the set of commits whose target nobody can name.

## Requirement

STE-597's directory model moves the running directory only when a segment's first token is exactly `cd`, and `applyCd` takes the first argument verbatim (`adapters/_shared/src/commit_target_repo.ts:546-548`, `:489-500`). Four shapes therefore resolve to the wrong tree. `cd -P <B>` resolves `<session>/-P`, `checkoutRootOf` walks up from there, and the session's own root comes back (`:57-65`). `cd -- <B>` does the same with `--`. `builtin cd <B>` and `pushd <B>` fall through to `parseGit`, which answers null, so the directory never moves. Measured on the installed 2.86.0 hooks (skeptic repro `verify-hooks/drive.ts`, rows W01-W04): the session is in checkout FE0 with nothing staged, and checkout B stages `specs/frs/GF-99.md`, `src/app.py` and `tests/test_app.py`. All four shapes give `repo=FE0`, and the /tdd hook exits 0 in silence. The control `cd <B> && git commit` gives `repo=B`, and the hook exits 2. This is the wrong-tree defect STE-597 set out to close, surviving through four more doors. The gate-check hook still refuses these shapes without evidence, only because it performs no classification.

Separately, STE-597 names every directory computed by a substitution or a variable as unresolvable. That is correct wherever the value is unknowable. Some values are fixed by the command itself, though, and calling them unknowable leaves a residual that STE-614 must then treat as "target unknown". A rough spec-time regex scan of 85,948 distinct Bash commands found the following. The replay in AC.7 replaces these numbers with measured ones.

- 266 commands pair a `$VAR` directory (after `cd` or `git -C`) with a git … commit word sequence. In 213 of them, every such variable is assigned earlier in the same command.
- 7 commands commit through `git -C $(pwd)` or `git -C $(git rev-parse --show-toplevel)`, the idiom STE-597's own test calls "the everyday idiom".

Required model (added to the shared recogniser from STE-601):

1. **`cd` options.** `-L`, `-P`, `-e` and `-@`, alone or combined (`-Pe`), are skipped. `--` ends option parsing. `cd -` and a bare `cd` stay unresolvable, as today. When `CDPATH` is set and non-empty in the hook process's environment, a relative `cd` or `pushd` operand that does not begin with `.` or `..` is unresolvable, naming `CDPATH`: the shell may pick a directory the model cannot.
2. **`builtin cd` and `command cd`** are `cd`.
3. **The directory stack.** `pushd DIR` moves and pushes. `pushd -n DIR` pushes without moving. `popd` returns to the previously modelled directory. `popd` with an empty modelled stack, `pushd +N`, `pushd -N` and `popd +N` are unresolvable, and the reason names the word. A parenthesised subshell restores the stack as well as the directory.
4. **Home.** `~` and `~/…` expand to the home directory of the hook process, which runs as the same OS user as the session's shell. `~user` stays unresolvable.
5. **In-command variables.** A segment that consists only of `NAME=value` assignments, or `export NAME=value`, binds NAME for later segments in the same scope. The value may be a literal or may reference names already bound. A prefix assignment (`NAME=v cmd`) binds nothing for later segments, which is shell semantics. A subshell's bindings do not escape it. `unset NAME` removes the binding. A loop variable (`for NAME in …`) is unresolvable inside the loop. A value built from any substitution other than the fixed forms in item 6, from a backtick, or from an unbound name leaves the name unexpanded, and the existing unresolvable rule applies.
6. **Fixed computed directories.** `$PWD`, `${PWD}`, `$(pwd)`, `` `pwd` ``, `$(pwd -P)` and `$(pwd -L)` are the running directory. `$(git rev-parse --show-toplevel)` and `$(git -C DIR rev-parse --show-toplevel)` are the checkout root of the running directory, or of DIR, through the injected root lookup. Every other substitution stays unresolvable, including `$(dirname $(pwd))` and `$(mktemp -d)`.

Pins amended by item 6, named here rather than slipped in. In `tests/commit-target-repo.test.ts`, the round-2 clauses asserting that `git -C $(pwd) commit`, `git -C $(git rev-parse --show-toplevel) commit`, `(cd /s/b && git -C $(pwd) commit)` and `cd $(pwd)/sub && git commit` are unresolvable now assert the resolved checkout, and the quoted/unquoted agreement clause now asserts both spellings resolve alike. In `tests/hook-modules-pre-commit-tdd-orchestrator.test.ts`, the clause "`git -C $(pwd) commit` with a staged test file and no /tdd evidence → exit 0 + a Reminder naming the substitution" now asserts exit 2, because the target resolves and the staged test raises the requirement. The unexpanded-global-option rule (`git -c user.name=$(whoami) commit`, `--git-dir=$(pwd)/.git`) is unchanged: those options can name any tree.

## Acceptance Criteria

- AC-STE-613.1: `cd -P <B> && git commit`, `cd -L <B> && …`, `cd -Pe <B> && …`, `cd -- <B> && …`, `builtin cd <B> && …` and `command cd <B> && …` each resolve to `<B>`. Controls: `cd - && git commit` and `cd && git commit` stay unresolvable, with the STE-597 wording unchanged. With `CDPATH=/x` in the resolver's environment, `cd b && git commit` is unresolvable naming `CDPATH`, while `cd ./b && git commit` and `cd /abs/b && git commit` still resolve; with `CDPATH` unset or empty, `cd b && git commit` resolves as at HEAD.
- AC-STE-613.2: `pushd <B> && git commit` resolves to `<B>`. `pushd <B> && popd && git commit` resolves to the session's checkout. `pushd -n <B> && git commit` resolves to the session's checkout. `(pushd <B>) && git commit` resolves to the session's checkout, because the subshell restores the stack and the directory. `popd && git commit` with an empty modelled stack, `pushd +1 && git commit` and `popd +1 && git commit` are unresolvable, with `unresolved` naming the word.
- AC-STE-613.3: `cd ~/x && git commit`, where `~/x` is a checkout under the hook process's home, resolves to that checkout. `cd ~other/x && git commit` is unresolvable.
- AC-STE-613.4: `B=<abs B>; git -C "$B" commit`, `B=<abs B> && cd "${B}" && git commit`, `S=<abs parent>; D=$S/b; git -C "$D" commit` and `export B=<abs B>; cd $B && git commit` each resolve to `<B>`. Scope and semantics are graded in both directions. `B=<abs B> git -C "$B" commit` (a prefix assignment) is unresolvable. `(B=<abs B>); git -C "$B" commit` is unresolvable. `B=<abs B>; unset B; git -C "$B" commit` is unresolvable. `R=$(mktemp -d); git -C "$R" commit` is unresolvable and names `$R`. `for d in <A> <B>; do git -C "$d" commit; done` is unresolvable.
- AC-STE-613.5: `git -C $(pwd) commit`, `git -C "$(pwd)" commit`, `git -C $PWD commit` and `` git -C `pwd` commit `` resolve to the running directory's checkout. `git -C $(git rev-parse --show-toplevel) commit` from a subdirectory resolves to its checkout root. `(cd <B> && git -C $(pwd) commit)` resolves to `<B>`. `git -C $(dirname $(pwd)) commit` stays unresolvable. `git -c user.name=$(whoami) commit` and `git --git-dir=$(pwd)/.git commit` stay unresolvable, with their STE-597 clauses unedited. Each pin amendment named in the Requirement is made in place, and no other existing clause in either suite is edited.
- AC-STE-613.6: The wrong-tree defect, graded through the shipped hook wrappers on two `makeSpanFixture` git checkouts. The session is in FE0 with nothing staged, and B stages an FR file, a source file and its test. With no evidence, `pushd <B> && git commit`, `cd -P <B> && git commit`, `builtin cd <B> && git commit` and `cd -- <B> && git commit` each make the /tdd hook exit 2, naming B's staged paths; each exited 0 at HEAD. The permit side: the same four commands with /tdd evidence present proceed (no exit 2). `pushd <FE0> && git commit`, with only a README staged in FE0, exits 0 with empty stderr and no evidence. The unchanged control `cd <B> && git commit` exits 2 before and after.
- AC-STE-613.7: Real-traffic replay, using the helper from STE-601 over the same corpus. This FR's Falsifiability section records the number of commit-bearing commands whose target moves from unresolvable to resolved, the number whose resolved target changed, and every command in either set, hand-classified as correct or wrong. Wrong resolutions are zero, or each is named with its fix. The number of commit-bearing commands still unresolvable after this FR is recorded as the residual that STE-614 handles. An empty corpus is reported as not measured.
- AC-STE-613.8: The "Recognised command shapes" table in `docs/hooks-reference.md` gains the rows of items 1 to 6, each graded by the table-driven test from STE-601. No file under `skills/` changes, and no `STE-<N>` token is added under `skills/`.
- AC-STE-613.9: Falsifiability. Every clause asserting new behaviour (AC.1 to AC.6) fails on the pre-change bytes and passes on the post-change bytes, with the direction recorded per clause in this FR. Controls, unchanged-behaviour pins and the still-unresolvable rows are exempt by construction.

## Technical Design

The directory model becomes a small state record carried through the shared recogniser's scopes: the directory, a modelled stack and a variable map. A parenthesised scope snapshots all three and restores them on close. Expansion stays conservative. Only a closed set of fixed forms and literal in-command bindings resolve, and everything else keeps the STE-597 unresolvable wording. Home expansion reads the hook process's home. The injected checkout-root lookup answers the `rev-parse` form, so tests stay filesystem-free where they are today.

## Testing

Extend `tests/commit-target-repo.test.ts` with option, stack, home, variable and fixed-form groups. Every resolving clause is paired with a still-unresolvable sibling that differs by one token. Hook-level wrong-tree pairs go in `tests/hook-modules-pre-commit-tdd-orchestrator.test.ts`, driven through the bash wrapper. Amended pins are edited in place, and the diff shows only their expectation lines. The replay numbers land in this FR.

## Falsifiability

### Direction (AC.9)

Measured 2026-09-19. A throwaway detached worktree was created at the base, c88f32f6 (the STE-601 commit). Only three test files were copied in: `tests/commit-target-repo-ste613.test.ts`, `tests/commit-target-repo.test.ts` and `tests/hook-modules-pre-commit-tdd-orchestrator.test.ts`. Every source file stayed at base bytes. Each file was run on its own there and in the post-change tree with `bun test --reporter=junit`, and the results were joined by full test name. The worktree was removed afterwards.

| File | Tests | Fail base → pass post | Pass both | Fail post |
| --- | ---: | ---: | ---: | ---: |
| `commit-target-repo-ste613.test.ts` | 73 | 45 | 28 | 0 |
| `commit-target-repo.test.ts` | 98 | 5 (the amended pins) | 93 | 0 |
| `hook-modules-pre-commit-tdd-orchestrator.test.ts` | 55 | 2 | 53 | 0 |

**No clause fails post-change.**

| AC | Fail base → pass post (new behaviour) | Pass both (exempt, by group) |
| --- | --- | --- |
| AC.1 | 12. Seven `cd` option spellings (`-P`, `-L`, `-e`, `-@`, `-Pe`, `--`, `-P --`) resolve to `/s/b`. `builtin cd /s/b` and `builtin cd -P /s/b` resolve. The sibling `builtin cd -` stays unresolvable (at base, `builtin` was not recognised, so this resolved to the session's checkout). With `CDPATH=/x`, `cd b` and `pushd b` are unresolvable naming `CDPATH`. | 10. **Controls:** `cd -` and bare `cd` stay unresolvable, and plain `cd /s/b` resolves. **CDPATH siblings (unchanged behaviour):** `CDPATH` unset or empty, and `./b`, `../b` and `/s/b` under `CDPATH=/x`. **Still-unresolvable siblings:** `cd -P -` and `command cd -`. **Passes on base (see below):** `command cd /s/b`. |
| AC.2 | 9. `pushd /s/b`, `pushd /s/b >/dev/null;`, and `pushd /s/b && pushd /s/a/sub && popd` resolve to `/s/b`. Unresolvable naming the word: `popd` with an empty stack, `(pushd /s/b); popd`, `pushd +1`, `pushd -1`, `popd +1` and `pushd /s/b && popd +1`. | 3. **Passes on base (see below):** `pushd -n /s/b`, `pushd /s/b && popd` and `(pushd /s/b)`, each resolving to the session's checkout. |
| AC.3 | 3. `cd ~/x`, `cd ~ && cd x` and `git -C ~/x` resolve to the checkout under HOME. | 1. **Still-unresolvable sibling:** `cd ~other/x`. |
| AC.4 | 6. `B=/s/b; git -C "$B"`, `B=/s/b && cd "${B}"`, `S=/s; D=$S/b; git -C "$D"`, `export B=/s/b; cd $B`, the later-binding-wins rebinding, and `(B=/s/b; git -C "$B" commit)`. The last is labelled CONTROL in the suite, but it asserts new behaviour (a binding inside a subshell) and is falsifiable. | 8. **Still-unresolvable siblings:** a prefix assignment, a subshell binding that does not escape, `unset`, `$(mktemp -d)`, a backtick value, an unbound `$HOSTDIR`, a `for` loop variable, and a loop variable shadowing an earlier binding. |
| AC.5 | 15 in the FR suite: `$(pwd)`, `"$(pwd)"`, `$(pwd -L)`, `$(pwd -P)`, `` `pwd` ``, `$PWD`, `"$PWD"` and `${PWD}` after `cd /s/b`. Also `$(pwd)` from `/s/a/sub`, `(cd /s/b && git -C $(pwd))`, `cd $(pwd)/sub`, `B=$(pwd)` bound before a `cd`, and the three `rev-parse --show-toplevel` forms. Plus the 5 amended STE-597 pins in `commit-target-repo.test.ts`, each marked "STE-613 amends this pin". | 6. **Controls (STE-597 clauses, unedited):** `git -c user.name=$(whoami)` and `git --git-dir=$(pwd)/.git`. **Still-unresolvable siblings:** `$(dirname $(pwd))`, `$(git rev-parse --git-dir)`, `$(mktemp -d)` and `$OLDPWD`. |
| AC.6 | 2. With no evidence, each of the four wrong-tree shapes exits 2 naming B's staged paths. Also the amended STE-597.4 hook pin: `git -C $(pwd) commit` with a staged test file now exits 2. | 3. **Control:** the unchanged `cd <B> && git commit` exits 2 before and after. **Permit side:** the four shapes with /tdd evidence proceed, and `pushd <FE0>` with only a README staged exits 0 silently. At base both pass trivially, because the base resolves to the session's FE0 checkout, where nothing gated is staged. |

**Clauses that assert new behaviour but pass on base:**

- `command cd /s/b && git commit` resolves to `/s/b` (AC.1). The base already strips `command` as a transparent wrapper (the recogniser's wrapper chain, for example `["env", "time", "command"]`), so the `cd` it wraps already moved the directory. The clause pins behaviour that predates this FR. Only `builtin cd` is new.
- `pushd -n /s/b`, `pushd /s/b && popd` and `(pushd /s/b)` each resolve to the session's checkout (AC.2). The base ignores `pushd` and `popd` entirely, so it answers "the session's checkout" for all three. These clauses are not falsifiable against the base. They guard against over-modelling: they fail against a stack model that moves on `-n`, never pops, or leaks a push out of a subshell. The falsifiable half of the stack model is carried by the nine clauses that fail on base, including `(pushd /s/b); popd`.

### Replay (AC.7)

Measured 2026-09-19 with `bun run tests/_command_traffic_replay.ts --base c88f32f6` from the plugin root. The base is the STE-601 commit, so the delta isolates this FR's change. The corpus is the operator's `$CLAUDE_CONFIG_DIR/projects/`. Both sides resolve with the helper's filesystem-free root lookup (every absolute directory is its own checkout) and no git aliases. `CDPATH` was unset, and home was the operator's. The output went to a temp file outside the repository. No transcript text is committed, and the commands below are described by shape only.

| Measure | Count |
| --- | ---: |
| Transcript files read | 4,037 |
| Distinct Bash commands | 90,190 |
| Commit-bearing, base (c88f32f6) | 773 |
| Commit-bearing, after | 773 |
| Newly resolved (base unresolvable → resolved) | 195 |
| Target changed (both resolved, different directory) | 0 |
| Still unresolvable after (the residual STE-614 handles) | 31 |

Commit recognition did not move: no command became or stopped being commit-bearing, none became unplaced, and none gained an advisory. The 226 commands unresolvable at the base split into the 195 now resolved and the 31 residual.

**Classification of the 195 newly resolved commands, by shape.** Every command was read and its resolved directory compared with where its commit runs. All 195 resolve through item 5 (in-command variables). None resolves through items 1-4 or 6: real traffic holds no commit through `cd` options, `builtin cd`, `command cd`, `pushd`, `~` or a fixed computed directory. This matches the Notes: the wrong-tree fix is latent.

| Shape | Count | Correct | Wrong |
| --- | ---: | ---: | ---: |
| `NAME=<abs>` (often `NAME=$PARENT/sub`) bound earlier, then `cd "$NAME"` or `cd $NAME/sub`, then `git commit` in that directory. Mostly scratch fixture repositories and worktrees built by `git init` or `git worktree add` in the same command | 166 | 166 | 0 |
| `NAME=<abs>` bound earlier, then `git -C "$NAME" commit` or `cherry-pick` | 13 | 13 | 0 |
| Both of the above in one command, every commit in the same directory | 7 | 7 | 0 |
| As the first row, with an `export NAME=value` of an unrelated variable (for example a plugin root or git config path) in the same command | 6 | 6 | 0 |
| As the first row, with `export CLAUDE_CONFIG_DIR=~/…` in the same command. The `~` value is not the commit's directory | 2 | 2 | 0 |
| As the first row, with `"$PWD"` passed to a later non-git command after the commits | 1 | 1 | 0 |

Checked edge shapes within those groups, all correct:

- A commit inside a `for` loop whose loop variable is not the directory: the directory comes from an outer binding.
- A commit inside a parenthesised subshell `(cd $DIR && git commit)`.
- A `cd` inside a `$(…)` substitution before the commit: it does not leak into the commit's directory.
- A later `cd` back to another checkout after the commits: it does not retarget them.
- A shell function defined in the same command: its body commits nothing.

**Wrong resolutions: zero.** No resolver fix and no extra pin was needed.

**Residual for STE-614: 31** commit-bearing commands whose target is still unresolvable after this FR.

## Review record

- Review round 1, behaviour fix. Tilde expansion needs an unquoted leading `~`, so `cd "~/x"`, `cd '~/x'` and `cd \~/x` name a directory literally called `~` under the running directory. Before the fix the model expanded them to the hook process's home, a confident wrong-tree answer. They are now unplaced. The fix is pinned (four shapes plus the unquoted control) and mutation-verified.
- The manual's shape table now grades a `recognised` row's target whenever its verdict names one ("the commit targets `/s/b`"), and runs every row with `HOME=/s` and `CDPATH` unset, so the `~/…` row's promised target is checked too. A row pointed at the wrong checkout reds the table test, which was mutation-verified.
- An audit finding that `CDPATH` pre-empts `~` expansion was refuted by measurement: `isUnexpanded` treats a leading `~` as unexpanded, so the CDPATH rule never fires on it. A pin now holds that behaviour (`cd ~/x` and `pushd ~/x` resolve to home under `CDPATH=/x`).
- Known limit, now stated in the manual. The model reads a command as a straight sequence, as STE-597 did for `cd`: a binding, `cd` or `pushd` inside an `if` branch or a pipeline element counts as if it ran. The replay found no wrong resolution from it.
- Review round 2, two defects found by the quality pass and reproduced before the fix. (a) A binding may reference itself, so `A=$A$A` chained 28 times doubled the value until the resolver threw `Out of memory` — a crashed gate inside a blocking hook. A bound value longer than `MAX_BOUND_VALUE` (4096) now binds null: unresolvable, never a guess, and the chain answers in about a millisecond. (b) `PWD` is maintained by the shell, so `PWD=/s/b; cd /s/a/sub; git -C $PWD commit` lands in the `cd` target, which is what bash prints; the model answered with the stale manual binding, a confident wrong-tree answer. `cd`, `pushd` and `popd` now re-stamp `PWD`. Both are pinned (three chain lengths, both directory builtins, and two controls) and mutation-verified.
- Item 5 applied to the commands that can rewrite a name: `read`, `declare`, `typeset`, `local`, `readonly`, `printf -v`, `let`, `mapfile`, `readarray` and `getopts` make the names they touch unknown; `source FILE` and `. FILE` make every binding unknown; a `select NAME in …` variable is unknown like a `for` variable. Each can only turn a resolved directory into an unplaced one, never resolve wrongly. Review round 1 pinned them (eleven writers, `select`, and an `echo` control) and mutation-verified them.
- AC.7 asks for every changed command to be hand-classified. All 195 were read and checked, and the record groups them by shape, because transcript text stays out of the repository.
- The amended STE-597 hook pin (`git -C $(pwd) commit` now exits 2) had its describe title and comments updated along with its expectation lines, so the comments no longer describe the retired exit-0 behaviour.

## Notes

- Gate-check was never fooled by these shapes, because it classifies nothing. The /tdd hook was.
- Resolving the fixed forms shrinks the residual whose target is unknowable.
- The skeptic measured zero real commits through the four wrong-tree shapes, so this fix is latent.

<!-- token-stats:begin -->

## Token Stats

| skill | model | input | output | cache-read | cache-creation |
| --- | --- | ---: | ---: | ---: | ---: |
| subtotal |  | 0 | 0 | 0 | 0 |

<!-- token-stats:end -->
