---
title: "Delete pre-M18 dead test infrastructure (v2-minimal + Schema M + script-mode)"
milestone: M39
status: archived
archived_at: 2026-04-28T09:04:28Z
tracker:
  linear: STE-141
created_at: 2026-04-28T07:05:00Z
---

# STE-141: Delete pre-M18 dead test infrastructure (v2-minimal + Schema M + script-mode) {#STE-141}

## Requirement

Three artifacts preserve a pre-M18 / pre-STE-76 invariant world that no longer applies to the live repo:

1. **`tests/fixtures/v2-minimal/specs/frs/fr_*.md`** — uses pre-M18 STE-60 `fr_<ULID>.md` filename convention. The fixture is the only place in the codebase where `fr_*.md` filenames still exist; live `specs/frs/` and `specs/frs/archive/` carry zero such files.
2. **`tests/scripts/{verify,capture}-regression.sh`** — shell wrappers never invoked from `bun test` or CI; only invoked by hand.
3. **`tests/scripts/verify-regression.ts:runSchemaMProbe()` and the `if (import.meta.main)` script-mode entry** — asserts pre-M18 invariants (filename matches `fr_<ULID>.md`, `id:` equals filename stem). Only the script-mode entry consumes the v2-minimal fixture; the `verify-regression.test.ts` imported entry uses `runSchemaLProbe` (active) and never `runSchemaMProbe`.

Net: a self-referential pre-M18 island that always passes against itself.

**Important — keep these (NOT dead).** Per the audit's F9 correction: `tests/fixtures/resolver/{linear-and-jira,linear-only,no-trackers,overlapping-prefixes}` are consumed by `adapters/_shared/src/resolve.test.ts:436-486`. Pre-STE-76 `id:` lines in those fixtures are part of the active fixture contract. Out of scope. Likewise, `runSchemaLProbe` and `verify-regression.test.ts` exercise the live Schema L probe used by `bun test` — keep.

Per the no-users-yet principle: subtract.

## Acceptance Criteria

- AC-STE-141.1: `plugins/dev-process-toolkit/tests/fixtures/v2-minimal/` is deleted (the entire directory tree, ~9 files: 3 top-level + archive `fr_*.md` + matching plan files).
- AC-STE-141.2: `plugins/dev-process-toolkit/tests/scripts/verify-regression.ts` no longer exports `runSchemaMProbe` and no longer carries the `V2_FIXTURE` constant. The function definition is removed.
- AC-STE-141.3: `plugins/dev-process-toolkit/tests/scripts/verify-regression.ts` no longer carries the `if (import.meta.main)` script-mode block. The file becomes import-only (re-exports `runSchemaLProbe`).
- AC-STE-141.4: `plugins/dev-process-toolkit/tests/scripts/verify-regression.sh` and `capture-regression.sh` are deleted.
- AC-STE-141.5: `plugins/dev-process-toolkit/tests/scripts/capture-regression.ts` is deleted.
- AC-STE-141.6: `plugins/dev-process-toolkit/tests/fixtures/baselines/*.snapshot` files are deleted (~4 files; only consumed by deleted script-mode block per F9 verification).
- AC-STE-141.7: `runSchemaLProbe` and `verify-regression.test.ts` continue to work unchanged — the test that imports `runSchemaLProbe` and runs it against the live repo is preserved.
- AC-STE-141.8: `bun test` is green (1071 → 1071 - the deleted Schema M test count).
- AC-STE-141.9: `/gate-check` clean across the live repo.
- AC-STE-141.10: A `grep -rn "v2-minimal\|runSchemaMProbe\|capture-regression\|verify-regression\.sh"` returns no surviving references in shipped artifacts after the deletion (every reference was either a consumer to delete or already absent).

## Technical Design

Pure deletion. The work is mechanical; the discipline is in verifying nothing else references the deleted artifacts.

**Step 1.** Map the deletion set — confirm zero `bun test` consumers via:

```sh
grep -rn "v2-minimal\|runSchemaMProbe\|V2_FIXTURE" plugins/ --include="*.ts"
grep -rn "verify-regression\.sh\|capture-regression\.sh" plugins/
```

The audit confirmed zero hits outside the deletion set. Re-confirm at implementation time.

**Step 2.** Delete files:

```
plugins/dev-process-toolkit/tests/fixtures/v2-minimal/  (recursive, ~9 files)
plugins/dev-process-toolkit/tests/scripts/verify-regression.sh
plugins/dev-process-toolkit/tests/scripts/capture-regression.sh
plugins/dev-process-toolkit/tests/scripts/capture-regression.ts
plugins/dev-process-toolkit/tests/fixtures/baselines/*.snapshot  (~4 files)
```

**Step 3.** Edit `tests/scripts/verify-regression.ts`:

- Remove the `runSchemaMProbe` function.
- Remove the `V2_FIXTURE` constant.
- Remove the entire `if (import.meta.main)` script-mode block.
- Keep the `runSchemaLProbe` export and any helpers the active test consumes.

**Step 4.** Verify `tests/verify-regression.test.ts` still imports + runs `runSchemaLProbe` correctly.

**Step 5.** Run `bun test`; confirm green.

## Testing

- **`bun test`** — passes after the deletions; the only test count change is the Schema M test removal (script-mode only, not a `bun test` entry).
- **Manual** — `grep -rn` confirms no surviving references.
- **Manual** — `verify-regression.test.ts` continues to assert Schema L behavior on the live repo.

No new tests; this is a subtraction.

## Notes

The audit's F9-correction is load-bearing: the resolver fixtures (`tests/fixtures/resolver/*`) were initially flagged as dead alongside v2-minimal; on re-verification, `resolve.test.ts:436-486` actively consumes them, so they are NOT in scope. This FR explicitly carves them out (AC-STE-141.7's intent extends — though the resolver fixtures are not in the deletion set at all).

Per the no-users-yet principle, "preserve to keep a probe green" is dead weight when the probe itself encodes a defunct invariant. Schema M asserts `fr_<ULID>.md` filename + `id:` = filename stem, both retired by M18 STE-60. Schema L (the active probe) asserts the live invariants and stays.

Reference: `/tmp/branch-review-2026-04-27.md` § F9 (with the F9-correction folded in: resolver fixtures are LIVE, NOT dead).
