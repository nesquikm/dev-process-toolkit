---
title: Give the tracker-less claim step an instruction shaped like its siblings, and verify the artifact
milestone: M121
status: archived
archived_at: 2026-08-09T14:45:06Z
tracker:
  linear: STE-457
created_at: 2026-08-09T09:33:22Z
changelog_category: Fixed
---

# Give the tracker-less claim step an instruction shaped like its siblings, and verify the artifact

## Summary

The claim step's sibling instructions each name a module path and a call form. Its tracker half points at a runbook. Its tracker-less half names neither — it states a fact about a class rather than instructing anyone to do anything.

On the run that surfaced this, the tracker leg claimed correctly and the tracker-less leg never claimed at all. The difference is not the code, which is implemented and unit tested. The difference is that one leg had something followable to follow.

The verification that would have caught it already exists as a Phase 1 exit self check with a hard refusal and a go back and claim remedy. Tracker-less mode was carved out of it on a rationale that is true of the tracker assertion and false of the lock artifact — the sentinel really is vacuous, but the lock file and its commit are not.

So this is not new machinery. It is an existing house mechanism whose exemption was generalized one surface too far.

## Requirement

Give the tracker-less half of the claim step the same instruction shape as its siblings, replace the Phase 1 exit exemption with a tracker-less arm that verifies the lock artifact, and correct the release proof definition so the shipped prose and the shipped behaviour agree.

## Acceptance Criteria

- AC-STE-457.1: The tracker-less half of § 0.c names a module path AND a call form, matching the shape of 0.b′ and 0.b″, with the correct THREE-way routing. The tracker half's four-way routing is not copied across — `already-released` does not exist on the local claim side. {#AC-STE-457.1}
- AC-STE-457.2: Step 0.d gains a tracker-less arm asserting the lock file and its claim commit, refusing in the NFR-10 canonical shape with a go-back-and-claim remedy. The tracker arm is BYTE-UNCHANGED. {#AC-STE-457.2}
- AC-STE-457.3: `docs/implement-reference.md` Phase 4 Close (c)'s proof-of-release becomes the durable claim witness PLUS the absence. `LocalProvider.releaseLock` is unchanged in code, so AC-STE-65.3 stays satisfied. {#AC-STE-457.3}
- AC-STE-457.4: Two hard caps hold and are pinned by a drift assertion: `skills/implement/SKILL.md` stays within its 358-line cap (measured 342 before the change), and the change adds ZERO new internal-namespace tokens under `skills/`, which sits AT its 246 ceiling. {#AC-STE-457.4}
- AC-STE-457.5: Forcing function on the house two-token mechanism. `## Implementation notes` records exactly one of `zero_c_fits_inline` or `zero_c_site_reopened`. The second HALTS this FR and returns the site question to the operator; the instruction is never trimmed to win the budget. A probe asserts exactly one token is present. {#AC-STE-457.5}
- AC-STE-457.6: A § Recorded supersession entry in `specs/plan/M121.md` names AC-STE-101.5's exemption as true of the tracker assertion and false of the lock artifact. Nothing is weakened; the record IS the deliverable. {#AC-STE-457.6}
- AC-STE-457.7: `specs/plan/M121.md`'s blast-radius table gains its fifth shipped surface and `migration: none` is RE-ARGUED against the widened set rather than inherited from the narrower one. {#AC-STE-457.7}
- AC-STE-457.8: `specs/notes/follow-ups.md` frames the doc-level asymmetry as a symmetry PREFERENCE with an unmeasured probe #37 cost — nothing broken, only uneven — and a reader can tell that in one sentence. It is not filed as a deferred fix. {#AC-STE-457.8}
- AC-STE-457.9: `## Notes` carries the honest boundary in these words: no offline test can prove a `claude -p` child FOLLOWS the improved prose; only the next conformance run shows that, which is why the detection ships first. {#AC-STE-457.9}

## Technical Design

The sibling shape is `call form` FROM `module path`, closing with a documentation pointer. The tracker-less half currently has none of the three. Matching it needs the constructor, the call, its three way return contract, and the artifact the call produces — the lock file plus the commit that makes it durable.

The Phase 1 exit arm is the same shape the tracker arm already uses: read the artifact back, assert it is what the claim should have produced, refuse in the canonical shape naming observed against expected. The tracker arm reads the ticket; the tracker-less arm reads the lock file and the commit. The exemption being replaced is not deleted from history, it is superseded with the reason recorded.

Two budgets bound the whole change and neither has slack to spare. The line budget has room for a short rewrite and not for a long one. The token ceiling has NO room at all, so the new prose must name no internal identifier. If the instruction cannot be made followable inside both, that is the halt condition rather than a trimming exercise.

The release proof correction is prose only. No code changes, so the predecessor criterion pinning the release implementation stays satisfied by construction.

## Testing

The drift pins assert the sibling shape textually — a module path and a call form present in the tracker-less half — plus both caps, so a future edit that reintroduces the asymmetry or overruns a budget goes red.

Both caps are asserted with their own measurements rather than inherited numbers, because a ceiling that another branch has consumed is a ceiling that reads as headroom.

What CANNOT be tested here is the thing the FR exists to change. A drift pin proves the prose says the right thing; nothing offline proves a child obeys it. The detection FR is what makes that observable, which is why it lands first.

Full gate green from the plugin root.

## Notes

Depends on STE-456, and the dependency is load bearing rather than a preference about ordering: the detection is this FR's ONLY falsifier.

**The honest boundary of this fix:** no offline test can prove a `claude -p` child FOLLOWS the improved prose; only the next conformance run shows that, which is why the detection ships first.

The tracker-less lock has never promised cross tree prevention and this FR does not add such a promise. The existing statements that collision is detected within one working tree and never prevented across trees remain exactly true. What the claim actually buys is single tree and real: the resume path after an abort, a durable witness that makes release provable, and a merge time conflict when two branches meet.

Retiring the lock instead was considered and rejected. It would strand a fixture group that shipped in this same milestone, rewrite five documentation surfaces, and give up a guard that works in the only place it ever claimed to work.

Build order: this FR lands THIRD and LAST.

## Implementation notes

Baseline owned before any edit, re-run from the plugin root rather than inherited: **7012 pass / 15 skip / 0 fail** at `0dee06e`, clean tree.

### The halt outcome — `zero_c_fits_inline`

Both budgets held with nothing trimmed, and the reason is worth stating because it is not the reason the FR anticipated. **The line budget was never the binding constraint.** Steps 0.c and 0.d are single-line bullets, so rewriting them in place turned a 341-character sentence into an 891-character one and a 481-character bullet into an 889-character one at a cost of **zero lines**. Measured before and after: `skills/implement/SKILL.md` is **342** split-counted on both sides, 16 lines under the 358 cap.

The token ceiling *was* binding — `skills/` sits at **246 / 246** with no headroom at all — and the rewrite spends none of it, because prose that names module paths and call forms names no criteria. Measured after: **246**, unchanged.

**One correction to the FR's own headline measurement, and it is a counting-method question rather than an error.** AC.4 records "measured 342 before the change". `wc -l` reports **341**. Both are right: `tests/skill-nfr-1-length.test.ts` counts `split("\n").length`, which is `wc -l` + 1 on a trailing-newline file, and 342 is the number the gate actually enforces against. Recorded because measuring this cap the obvious way returns a number the gate does not use, and a future editor who trusts `wc -l` will believe they have one more line of headroom than they do.

### The detector transition — executed on one file, both sides, checksum-verified

STE-456 shipped one assertion whose documented job was to announce this FR's arrival by going RED: *"the ordering arrow is intact — STE-457's fix has NOT landed"*, which asserted `adapters/_shared/src/local_provider` was ABSENT from the § 0.c bullet. It flipped exactly as designed.

| Bytes under test | `bun test … -t "ordering arrow"` |
|---|---|
| pre-fix (`git show HEAD:…/skills/implement/SKILL.md`) | **1 pass** — the defect is live |
| post-fix | **1 fail** at the module-path conjunct |

**It failed for the right reason, and that is checkable rather than asserted.** The two assertions ahead of the flipping one stayed green — the `**0.c Claim**` selector still resolved to exactly one line, and that line still contained the identifying phrase — so the failure is the detector firing on its subject, not the detector losing its subject. Restore was by absolute path and verified by `md5` comparison with a hard abort, on `follow-ups.md` § 0b's rule that a relative-path `cp` after a `cd` fails silently and leaves the mutation on disk.

**The assertion is then RETIRED AND INVERTED, on its own written instruction** (*"the correct response is to retire this assertion and record that every subsequent proof of this detector is against a reconstruction"*), not deleted: it now asserts the module path is PRESENT, keeps the by-name resolution that let the flip land where it was aimed, and gains a second arm asserting § 0.b stayed free of the path so a fix applied to the wrong bullet cannot read as this one. A ban is also satisfied by deleting the subject; the replacement is positive.

**What the retirement costs is recorded in the test file rather than left to be re-derived.** Every proof of this detector from here is against a reconstruction. The sibling cases stage their own disks and keep working, but the disk they stage is now built by the suite rather than observed in the wild, and the one run that exercised the genuine defect was 2026-08-08 and cannot be re-run.

### The honest boundary — restated because it is the whole shape of this FR

**No offline test can prove a `claude -p` child FOLLOWS the improved prose; only the next conformance run shows that, which is why the detection ships first.** What is proved here is narrower and is exactly what a drift pin can prove: the shipped prose now carries a followable instruction, a verification arm that reads the artifact back, and a release proof that a claim which never fired cannot satisfy — and an edit undoing any of those goes RED.

### Scope decisions taken, each with its reason

- **No `docs/implement-mode-none.md`.** Considered and rejected: it would spend a new documentation surface with an **unmeasured** probe #37 exposure to close an asymmetry that is cosmetic rather than functional. Filed at `specs/notes/follow-ups.md` § 0n as a symmetry *preference* — nothing broken, only uneven — and explicitly not as a deferred fix.
- **`docs/workflow-overview.md:203` corrected in the same change.** Its row said `mismatch → refuse Phase 2 (mode:none skips)`, which the 0.d rewrite makes false. Leaving it is `follow-ups.md` § 0i's family — a guard that keeps running and stops describing what it holds — and it is a direct consequence of this edit rather than adjacent work.
- **`implement-tracker-claim-runbook.test.ts`'s AC-STE-101.5 title corrected, assertion BYTE-UNCHANGED.** Its title advertised the carve-out this FR replaces. The enforcing regex (`/mode:\s*none|mode none|local-no-tracker/`) is untouched and still passes for a legitimate reason; only the sentence advertising it moved. A shipped predecessor's guard is not weakened to flatter a successor's phrasing.
- **`--code-only` still skips 0.c/0.d entirely** (§ 0.a), so the tracker-less arm does not reach it. That is pre-existing and unchanged by this FR — a degraded tracker-skipped run was always documented as skipping the claim — but it means the new verification arm covers `mode: none` and not the `--code-only` path that borrows its semantics. No AC authorizes widening 0.a's skip list, and doing so silently would be a shipped-contract change.

### Two open sites the operator named — both already clean, verified rather than assumed

The handoff named `specs/frs/STE-448.md:42` and a test title in `tests/m121-ste-448-mode-none-leg.test.ts` as still carrying the retired *"after the archive commit"* wording, to be handled in their own `docs(specs)` commit. **Neither reproduces.** Line 42 now reads *"is absent at end of run"* — corrected by `71be18c` on this branch. And no test *title* in that suite carries the phrase: the three surviving occurrences are a tripwire corpus input (`:833`), a labelled must-fire case (`:824`) and a comment recording the retirement (`:730`), all of which need the retired wording to do their jobs. No `docs(specs)` commit was made, because there was nothing to correct.

### What the post-completion stages found — the EIGHTH consecutive FR, and eleven of the sixteen were mine

An end-of-FR AUDIT and a 41-agent adversarial pass across four lenses both ran after the full gate was green at 7046/15/0 and twenty-eight mutations had come back RED. **Between them they confirmed sixteen defects with the gate green**, and the single most important one is behavioural rather than a pin.

**THE ONE THAT CHANGED WHAT SHIPPED — `claimLock` cannot commit under the toolkit's own `commit-msg` hook.** Verified by my own execution against the real module and the real shipped hook: the claim subject is **97** characters, the hook caps subjects at **72**, so `claimLock` writes and stages the lock and then **throws**. Before this FR, 0.d skipped `mode: none` and the run continued with a dirty tree. After it, 0.d is a hard entry gate, so the two conjuncts split — lock present, witness absent — and `/implement` refuses with a remedy (*"go back and run 0.c"*) that **cannot succeed**. **This FR made a latent defect reachable and blocking**, and a refusal whose remedy loops forever is worse than the silent skip it replaced. The autonomous legs never see it because `/setup`'s hook install is best-effort and is expected to be model-layer-blocked under `bypassPermissions`; interactive downstream projects do install it. Filed at `specs/notes/follow-ups.md` § 0p. **The repair is a code change to a commit subject that three shipped surfaces pin byte-for-byte — one of them shipped by AC-STE-456.7 specifically to prove the fence matches a real claim — and no AC here authorizes it.** What 0.d does instead is name the state and decline to prescribe the impossible remedy for it.

**Five vacuous pins, all mine, four of them measured by an idiomatic rewrite rather than a contrived one.** The strongest: AC.3's step-(c) pin was a token-order test, and a *natural* one-sided rewrite — "…is the absence of `.dpt/locks/<id>` after step (b), **plus** nothing else. The durable claim witness … is optional context…" — kept the **full gate** at 7046/15/0 while asserting the exact opposite of the AC. Also: the cross-reference-table ban was evaded by adding one space; the routing pin's positive arm was satisfied by "no `already-released`-specific handling", a phrase asserting the opposite; the detector-retirement pin grepped another test's SOURCE, so commenting the assertion out and replacing it with `expect(true).toBe(true)` stayed green; and the halt-token probe counted a mention, so a section *denying* an outcome was reached satisfied it.

**One over-claim I introduced while correcting an over-claim.** I retitled the shipped AC-STE-101.5 test to say the `mode: none` path "is named, not left implicit" — but its byte-unchanged regex slices 0.d **through 0.f**, and 0.e's own "Vacuous on archived FRs, `mode: none`" satisfies it. Measured: deleting my entire tracker-less arm leaves that suite at 4 pass / 0 fail. The regex is correct and stays untouched; the repair is a **sibling** assertion in this FR's own suite, bound to the 0.d line. Retitling a test to describe a successor's behaviour is exactly the class this FR exists to remove, reintroduced by the FR removing it.

**And the record I assembled from the AC's wording instead of from the diff.** AC.7 asked for "its fifth shipped surface", so the first pass added one row — while the change touched **three** shipped files, one of them `docs/implement-reference.md`, the direct subject of sibling AC.3. A table whose stated purpose is *"audit the verdict against this list rather than take it on faith"* is worth nothing when the list is copied from the criterion rather than derived from `git status`.

### Correction tally, carried forward — the milestone is now at SEVENTY-SEVEN

STE-456 left it at sixty. This FR produced **seventeen**, eleven of them found after the gate was green and twenty-eight mutations had passed:

61. **AC.5's halt-token probe read the token from the FR's own AC text**, not from `## Implementation notes` — it computed `halted === true` on a run that had not halted, and would have done so on every possible outcome.
62. **AC.6's supersession pin asserted MENTIONS, not the discriminator.** Inverting "**False of the lock artifact**" into "**Also of the lock file**" — which turns the record into an agreement with the exemption it supersedes — was 0 RED.
63. **AC.9's boundary pin went vacuous against prose I wrote myself**, in the same FR: the restatement in § Implementation notes satisfied a document-wide pin whose AC names `## Notes`. § 0i's once-occurring-token class, created rather than inherited.
64. **AC.3's step-(c) pin was a token-order test.** An idiomatic one-sided rewrite kept the FULL gate green while asserting the opposite of the requirement.
65. **AC.2's cross-reference pin was a byte-exact ban** whose only discriminating power one added space evaded; its positive arm pinned a row LABEL that predates the change.
66. **AC.1's routing pin was punctuation-exact**, so comma separators evaded it, and its positive arm matched a sentence asserting the opposite. The arity word — the test's own subject — was pinned nowhere.
67. **The detector-retirement pin grepped another test's SOURCE TEXT.** A commented-out assertion satisfies a substring search, so a neutered detector reads exactly like a live one.
68. **AC.4's `toBe(342)` over-constrained the AC** (≤ 358, 16 lines of headroom) onto the whole file, deferring a false RED onto whoever next edits the most-run skill the toolkit ships.
69. **`/nothing is weakened|weaken/i` collapsed to its looser arm** — an alternation whose second alternative is a substring of the first cannot see a recorded weakening.
70. **`toContain("0.d Claim verification")` was a tautology**, downstream of a filter on a superstring of itself.
71. **AC.5's probe counted a token, not a record.** Replacing the outcome section with one denying an outcome was reached kept all three tests green.
72. **The blast-radius table was assembled from the AC's wording rather than from the diff**, omitting two shipped surfaces including a sibling AC's own subject.
73. **The 0.d witness fence used `--grep`, whose `^` anchors at EVERY line of a message.** Executed: a commit whose body merely quotes the subject certified a claim that never fired.
74. **The same fence had no decidable predicate** — `git log --grep` exits 0 whether or not it matches, so "names a commit" was not checkable by exit code.
75. **The 0.d arm dropped the OWNERSHIP conjunct its tracker twin has.** Executed: a lock claimed by another branch passed both conjuncts, so a `taken-elsewhere` run that claimed nothing scored green on someone else's evidence. The tracker arm asserts state AND `assignee == currentUser`; mine asserted existence twice. All three of 73/74/75 are closed by one stronger form — subject-only, fixed-string, whole-line, branch-pinned — which also removes the regex-flavour hazard entirely.
76. **`claimLock` cannot commit under the toolkit's own `commit-msg` hook** (97-char subject vs a 72-char cap), and this FR promoted that from latent to blocking. Not authored here; made reachable here, which makes it this FR's to surface. `follow-ups.md` § 0p.
77. **"Four of the nine FRs" was stale, and my first repair replaced it with "several"** — trading a falsifiable wrong number for a word that can never be wrong. Restored to a real count (seven of thirteen) and pinned.
