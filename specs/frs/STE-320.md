---
title: Code-reviewer scope + capability-key registry expansion
milestone: M84
status: active
archived_at: null
tracker:
  linear: STE-320
created_at: 2026-05-20T13:41:06Z
---

# STE-320: Code-reviewer scope + capability-key registry expansion {#STE-320}

## Requirement

Close two related self-contradictions in the toolkit's own canon, both surfaced by the 2026-05-20 review.

**A7 — `agents/code-reviewer.md` self-contradicts on spec-compliance scope**: L10 says "Spec compliance is **not** your job — `/spec-review` owns AC→code traceability." L63 then explicitly makes Pass-1 deliver "one line per AC" spec-compliance. Two doc surfaces (`docs/adaptation-guide.md:113`, `docs/skill-anatomy.md:152`) echo the L10 disclaimer. Spec-research confirms the canonical contract: HG95TM AC-23.6 (M10, archived) documents `code-reviewer` Pass-1 (spec-compliance) + Pass-2 (code-quality) as **parallel-not-subordinate** to `/spec-review`. `/spec-review` (STE-308, M80) is the deep-traceability audit; `code-reviewer` Pass-1 is the inline per-PR check inside `/implement` Phase 3 Stage B. Both own spec-compliance in different contexts. The L10 disclaimer is the regression: it was added during STE-308 as an over-reaching cross-reference and now mis-teaches the agent's own scope.

**A10 — Capability-key registry under-covers ~6 of ~18 emitted tokens**: `adapters/_shared/src/closing_summary_capability_keys.ts:43-56` exports 12 canonical keys. `grep "MUST emit \`" plugins/dev-process-toolkit/skills/spec-write/SKILL.md` finds 18 distinct tokens with `MUST emit` directives. The 6+ missing tokens (verified):

- `spec_write_draft_declined` (spec-write SKILL.md L103)
- `spec_write_commit_declined` (L261)
- `branch_gate_created` (L191)
- `branch_gate_edited` (L192)
- `branch_gate_declined` (L193)
- `branch_gate_remote_probe_skipped` (L196)
- `tracker_local_orphan_local` (L327)
- `tracker_status_forced` (and rest of the `tracker_status_*` family)

Probe #44 `closing_summary_capability_keys` only validates the 12 declared keys, leaving a false-negative gap for the 6+ unregistered tokens. STE-238's stated mission "byte-checkable structural verification is required" is half-fulfilled.

Bundled because both are "the toolkit's own canon contradicts itself" — same audit rubric (pick the canonical winner, drop the conflicting surface).

## Acceptance Criteria

- AC-STE-320.1: `agents/code-reviewer.md` L10 disclaimer is removed. Pass-1 (Spec Compliance) at L63 stays as canonical per HG95TM AC-23.6. Replace L10's prose with one line that clarifies the parallel-not-subordinate split: "`/spec-review` runs the deep-traceability audit; `code-reviewer` Pass-1 is the inline per-PR check inside `/implement` Phase 3 Stage B — both own spec-compliance in different contexts."
- AC-STE-320.2: `docs/adaptation-guide.md:113` and `docs/skill-anatomy.md:152` are rewritten to drop the "Spec-compliance checks are **not** code-reviewer's job" claim. Replacement prose matches AC.1's parallel-not-subordinate framing. After update, `git grep -nE "Spec-compliance checks are \\*\\*not\\*\\* code-reviewer" plugins/dev-process-toolkit/{docs,agents}` returns zero matches.
- AC-STE-320.3: `adapters/_shared/src/closing_summary_capability_keys.ts` is updated to register the 6 missing token families verified in the requirement: `spec_write_draft_declined`, `spec_write_commit_declined`, the 4 `branch_gate_*` outcomes (`created`, `edited`, `declined`, `remote_probe_skipped`), `tracker_local_orphan_local`, and the `tracker_status_*` family (`tracker_status_forced`, `tracker_status_skipped`, `tracker_status_cancelled`, `tracker_status_unknown_encountered`, `tracker_tolerance_refused_non_tty`, `tracker_status_advisory_non_tty`, `tracker_status_genuine_drift`). Updated registry has ≥ 18 keys (matching the `MUST emit` directive count in `/spec-write` SKILL.md).
- AC-STE-320.4: Probe #44 `closing_summary_capability_keys` validates the expanded registry — every key declared in the const MUST appear in a `MUST emit \`<key>\`` directive somewhere in `/spec-write` SKILL.md AND vice-versa (bidirectional). Adding a new key to the const without the matching SKILL.md directive fails the probe; emitting a `MUST emit` directive without registering the key fails the probe. `bun test plugins/dev-process-toolkit/tests/gate-check-closing-summary-capability-keys.test.ts` GREEN.
- AC-STE-320.5: `agents/code-reviewer.md` Pass-1 return-contract example at L63+ is unchanged in shape (one line per AC, `[passed|failed]` + brief reason). No prose drift in the canonical contract; only the L10 disclaimer is removed.

## Technical Design

**A7 fix (3 edits)**: targeted prose replacements at the three drift sites. The new wording uses the architectural twin model verified by spec-research:

> `/spec-review` runs the deep-traceability audit (forked-auditor pattern per STE-308); `code-reviewer` Pass-1 is the inline per-PR check inside `/implement` Phase 3 Stage B (parallel pass pattern per HG95TM). Both own spec-compliance in different contexts.

**A10 fix (1 .ts file edit + probe expansion + test)**: `closing_summary_capability_keys.ts` const list grows from 12 to ~20 keys. The `tracker_status_*` family is enumerated explicitly (each token gets its own const entry — that's the byte-checkable contract per STE-238 AC.4). Probe shape stays unchanged; the registry grows.

Probe-bidirectionality: probe #44 today walks the registry and asserts each key appears in a `MUST emit` directive. The expanded version adds the reverse walk — every `MUST emit` directive in `/spec-write` SKILL.md must have a matching const entry. Catches the regression where a new token is added to SKILL.md but not registered.

Spec-research seed (STE-230 AC-STE-230.6): `spec_research_invoked` — 4 precedents returned (HG95V0 original Pass-1 framing, HG95TM canonical two-stage contract, STE-238 probe scope origin, STE-308 forked-auditor introducer). Decision validated by HG95TM AC-23.6: `code-reviewer` retains Pass-1 authority.

Deps-research vacuous (`specs/deps.yaml` absent — zero entries → fork skipped).

## Testing

`tests/gate-check-closing-summary-capability-keys.test.ts` expands to cover the bidirectional invariant from AC-STE-320.4: (a) PASS with byte-exact synthetic registry + SKILL.md fixture, (b) FAIL when a SKILL.md directive lacks a const entry (false-negative gap), (c) FAIL when a const entry lacks a directive (orphan registration).

Manual reviewer pass on `agents/code-reviewer.md` after the L10 edit — verify Pass-1 + Pass-2 deliverable framings still read coherently end-to-end.

End-to-end cross-check: `bun test plugins/dev-process-toolkit/tests/gate-check-closing-summary-capability-keys.test.ts` GREEN; `/gate-check` PASS on `main` with the expanded probe #44.

No `claude -p` regression coverage — pure source-file edits, no runtime LLM-decision path.

## Notes

- Spec-research seed: `spec_research_invoked` — 4 precedents returned (HG95V0, HG95TM, STE-238, STE-308).
- Deps-research vacuous (`specs/deps.yaml` absent — zero entries → fork skipped).
- A10 finding lists `branch_gate_default_applied` and `branch_gate_skipped_already_non_main` as already-registered tokens via STE-238 AC.6; both are confirmed present in the existing 12-key const. The 6 missing tokens are the `branch_gate_{created,edited,declined,remote_probe_skipped}` foursome plus the `spec_write_{draft,commit}_declined` pair plus the `tracker_local_orphan_local` singleton plus the `tracker_status_*` family.
- Design decision recap: `code-reviewer` Pass-1 owns inline-per-PR spec compliance; `/spec-review` owns deep-traceability audit. Parallel-not-subordinate; both authoritative in different contexts. Per HG95TM AC-23.6.
- Origin: 2026-05-20 review, problem areas A7 + A10.
