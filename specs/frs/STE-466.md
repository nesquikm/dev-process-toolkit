---
title: /best-practices management skill + /setup manifest seeding
milestone: M124
status: active
archived_at: null
tracker:
  linear: STE-466
created_at: 2026-08-15T17:46:17Z
changelog_category: Added
---

# /best-practices management skill + /setup manifest seeding

## Summary

The catalog needs a first-class management surface so entries are curated deliberately rather than hand-edited. A new command mirrors the dependency-manifest manager verbatim, and project setup offers to seed the catalog behind the same re-entrant diff-approval gate used for tracker configuration.

## Requirement

User-invocable `/dev-process-toolkit:best-practices` skill (add/edit/delete/list on `specs/best-practices.yaml`, all writes through STE-465's module) + an optional re-entrant `/setup` seeding step (STE-303 pattern).

## Acceptance Criteria

- AC-STE-466.1: New user-invocable skill `skills/best-practices/SKILL.md` mirroring the /deps idiom (add/edit/delete/list); every count-pinned surface updated (user-invocable 17→18, skills 25→26, README + CLAUDE.md + meta-tests).
- AC-STE-466.2: Add/edit flows are Socratic (AskUserQuestion per the Socratic Loop Contract) and validate through the STE-465 module — shape errors refuse; invalid entries are never written.
- AC-STE-466.3: `/setup` gains an optional best-practices seeding step per the STE-303 re-entrant pattern: existing manifest is the baseline, fresh proposal presented as a unified diff behind an approval gate, cancel keeps the file untouched.
- AC-STE-466.4: Meta-test pins on the new SKILL's load-bearing literals; NFR-1 line caps green on every touched SKILL.

## Technical Design

Mirror `skills/deps/SKILL.md` structure verbatim where applicable; the skill never hand-writes YAML — every mutation routes through the STE-465 module. The /setup step is additive prose in the existing interview sequence, guarded by the same diff-approval idiom as tracker-config seeding. Note the `skills/` STE-token ceiling has ZERO headroom (246/246 as of M123) — any tracker-ID tokens this FR adds to `skills/` prose must be offset or avoided.

## Testing

`bun test`; meta-test grep pins on the SKILL's add/list flow literals; count-pin sweep; NFR-1 checks; probe #43 (Socratic) auto-scopes the new SKILL.

## Notes

- Precedents: STE-301 (/deps skill idiom), STE-303 (re-entrant /setup seeding), STE-237 (Socratic contract).
- Depends on STE-465; build order within M124 is STE-465 → STE-466 → STE-467.
- Risk: /setup SKILL is large — keep the seeding step tight for NFR-1.

<!-- token-stats:begin -->

## Token Stats

| skill | model | input | output | cache-read | cache-creation |
| --- | --- | ---: | ---: | ---: | ---: |
| subtotal |  | 0 | 0 | 0 | 0 |

<!-- token-stats:end -->
