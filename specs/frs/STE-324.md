---
title: M84 LOW polish bundle — 10 mechanical fixes
milestone: M84
status: active
archived_at: null
tracker:
  linear: STE-324
created_at: 2026-05-20T14:04:15Z
---

# STE-324: M84 LOW polish bundle {#STE-324}

## Requirement

Close the 10 remaining LOW-severity items from the 2026-05-20 review (`/tmp/dpt-review-findings-2026-05-20.md`). Each is a 1–10 line targeted edit, bundled for diff coherence since all are mechanical fixes with no architectural implication. Bundling closes M84 at 30/30 problem areas, the full review file scope.

The 10 items by category:

**Code allowlist/validator hygiene (4 items)**:

- **A2 LOW tail** — `tests/tdd-live-smoke.test.ts:82` ("orchestrator + 3 forked subagents" comment) + `adapters/_shared/src/tdd_orchestrator_integrity.ts:6` docstring ("(a) the four skill paths exist (tdd + tdd-write-test + tdd-implement + tdd-refactor)" — accurate for probe #39's scope but misleading post-STE-296). Both reference outdated stage count after the AUDIT stage shipped.
- **A18** — `adapters/_shared/src/branch_proposal.ts:14` `ALLOWED_TYPES = new Set(["feat", "fix", "chore"])`. The commit-msg hook (`templates/git-hooks/commit-msg.sh:56`) accepts 11 CC types; `clampType()` silently coerces `docs:`/`refactor:`/etc. to `feat:`. Expand to the 10 non-`ci` CC types (`ci` excluded per STE-228 TRUNK_OK carve-out).
- **A19** — `adapters/_shared/src/release_config.ts` `bumpYaml` only handles top-level fields (regex anchor `^${field}: <semver>$`), but `validateEntry` accepts any dotted path. Add per-kind path-shape validation in `validateEntry`: yaml rejects dotted paths until `bumpYaml` learns nested-path support.
- **A20** — `adapters/_shared/src/commit_producing_skill_branch_gate.ts:72` `NON_COMMIT_PRODUCING_SKILLS` lists `report-issue`, `spec-research`, `deps-research`, `spec-review-audit`. The four TDD child forks (`tdd-write-test`, `tdd-implement`, `tdd-refactor`, `tdd-spec-review`) are also `context: fork` skills that don't commit (the orchestrator does), but they're absent. Add for symmetry.

**File / path hygiene (3 items)**:

- **A25** — `skills/gate-check/probes/tracker_local_reconciliation_drift.ts` is the only probe at this path; 55 of 56 sibling probes live at `adapters/_shared/src/<name>.ts`. Decision: move probe #49 to the canonical path; update traceability matrix at `specs/requirements.md:337`.
- **A29** — `templates/CLAUDE.md.template` L72-78 Workflows section shows three workflows (Bugfix / Feature / Refactor) without mentioning `/tdd`, `/visual-check`, `/docs`, `/ship-milestone`, `/deps`, `/report-issue`. Add the missing skills inline in the Feature workflow (call `/ship-milestone` after `/gate-check`, before `/pr`).
- **A30** — `specs/notes/jira-smoke-5.md` is a partial Jira smoke trace from 2026-04-29 (M43; current is M83) marked `status: partial`. `specs/notes/` has no archival convention. Decision: move under `specs/notes/archive/` since the trace is dated and superseded by later smokes (M45+).

**Doc cosmetic / description (3 items)**:

- **A26** — `examples/bun-typescript.md:33,46` references probe `bun-zero-match-placeholder` (kebab-case). Actual `PROBE_ID` in `adapters/_shared/src/bun_zero_match_placeholder.ts` is snake_case (matches error messages at L151, L157, L206, L211). Cosmetic — match the snake_case canon used elsewhere.
- **A27** — `skills/spec-review/SKILL.md:3` description still reads main-context after STE-308 migrated `/spec-review` to the forked-auditor pattern. Add a `[Orchestrator]` tag or short fork mention to surface the architectural pattern in the agent-discovery view.
- **A28** — `disable-model-invocation: true` asymmetry: only `/setup` has it; `/ship-milestone` (release commits) and `/spec-archive` (mass `git mv`) carry the same destructive blast radius but lack the guard. Decision: add `disable-model-invocation: true` to both. Symmetric protection.

## Acceptance Criteria

- AC-STE-324.1: `tests/tdd-live-smoke.test.ts:82` comment + `adapters/_shared/src/tdd_orchestrator_integrity.ts:6` docstring both updated to reflect 4-stage architecture per STE-318 / STE-296 canon. After update, `git grep -nE "(orchestrator \\+ 3 forked subagents|the four skill paths exist \\(tdd \\+ tdd-write-test \\+ tdd-implement \\+ tdd-refactor\\))" plugins/dev-process-toolkit/{tests,adapters}` returns zero matches.
- AC-STE-324.2: `adapters/_shared/src/branch_proposal.ts:14` `ALLOWED_TYPES` expanded from 3 to 10 entries: `feat`, `fix`, `chore`, `docs`, `style`, `refactor`, `perf`, `test`, `build`, `revert` (`ci` excluded per STE-228 TRUNK_OK_TYPES carve-out — `ci:` commits land on trunk). `clampType()` no longer coerces these 7 types. `bun test plugins/dev-process-toolkit/tests/branch_proposal.test.ts` GREEN with new fixtures covering each newly-allowed type.
- AC-STE-324.3: `adapters/_shared/src/release_config.ts` `validateEntry` dispatches per-kind path-shape checks. For `kind: yaml`, reject any `field:` containing a `.` (dot) with NFR-10 canonical refusal "yaml kind only supports top-level fields; got dotted path '<value>' at line <N>". For `kind: toml`, accept top-level and one-level-dotted (matching current `bumpToml` capability). For `kind: json`, accept any path including array-indexed (`plugins[0].version`). New unit tests cover each kind's accept/reject matrix.
- AC-STE-324.4: `adapters/_shared/src/commit_producing_skill_branch_gate.ts:72` `NON_COMMIT_PRODUCING_SKILLS` expanded to include `tdd-write-test`, `tdd-implement`, `tdd-refactor`, `tdd-spec-review`. After update, the list reads `["report-issue", "spec-research", "deps-research", "spec-review-audit", "tdd-write-test", "tdd-implement", "tdd-refactor", "tdd-spec-review"]` (8 entries). Wording rationale (per STE-308 AC-6 template): all 8 are `context: fork` read-only or write-but-orchestrator-driven skills; the orchestrator owns the commit boundary.
- AC-STE-324.5: Probe #49 module moves from `skills/gate-check/probes/tracker_local_reconciliation_drift.ts` to `adapters/_shared/src/tracker_local_reconciliation_drift.ts`. The traceability row at `specs/requirements.md:337` is updated to the new path. `bun test plugins/dev-process-toolkit/tests/gate-check-tracker-local-reconciliation-drift.test.ts` GREEN with the new import path.
- AC-STE-324.6: `examples/bun-typescript.md:33,46` rewrites probe references from `bun-zero-match-placeholder` (kebab) to `bun_zero_match_placeholder` (snake). After update, `git grep -nE "bun-zero-match-placeholder" plugins/dev-process-toolkit/examples/` returns zero matches; canonical snake_case PROBE_ID is the sole form.
- AC-STE-324.7: `skills/spec-review/SKILL.md:3` description prefixes the skill name with `[Orchestrator]` tag (mirrors `/tdd` description convention) and references the forked-auditor pattern via "delegates to `spec-review-audit` fork + `spec-reviewer` subagent". Description stays under 200 characters (SKILL frontmatter length convention).
- AC-STE-324.8: `disable-model-invocation: true` added to `skills/ship-milestone/SKILL.md` and `skills/spec-archive/SKILL.md` frontmatter. Both skills are now model-invocation-blocked (operator must explicitly call them). `/setup` already has the flag; this AC achieves symmetry across the three destructive-blast-radius skills.
- AC-STE-324.9: `templates/CLAUDE.md.template` L72-78 Workflows section rewritten to mention all 6 missing user-callable skills (`/tdd`, `/visual-check`, `/docs`, `/ship-milestone`, `/deps`, `/report-issue`). The Feature workflow shows the full chain: `/spec-write → /implement → /tdd (auto) → /gate-check → /docs → /ship-milestone → /pr`. After update, `grep -cE "(/tdd|/visual-check|/docs|/ship-milestone|/deps|/report-issue)" templates/CLAUDE.md.template` returns ≥ 6.
- AC-STE-324.10: `specs/notes/jira-smoke-5.md` moved to `specs/notes/archive/jira-smoke-5.md` (git mv preserves history). Frontmatter updated: `status: archived` + `archived_at: 2026-05-20T<commit-time>Z`. A one-line README at `specs/notes/archive/README.md` documents the archival convention: "dated smoke / conformance traces older than the current shipped release land here once superseded". `mkdir -p specs/notes/archive` first if missing.

## Technical Design

10 targeted edits across 10 files. Each item is independent — no ordering dependency between them. Bundle ships as one commit on the existing `chore/m84-public-surface-drift-cleanup` branch.

Per-AC implementation outline:

- **AC.1, AC.6, AC.7** — single Edit per file.
- **AC.2** — 1 const edit + 7 test cases (one per newly-allowed type).
- **AC.3** — new helper function `validateEntryPathShape(kind, field)` called from `validateEntry`; 5+ new unit tests covering each kind's accept/reject matrix.
- **AC.4** — single const edit; AC text mirrors STE-308 AC-6 wording template per spec-research seed.
- **AC.5** — `git mv` + import-path update across consumers (probe #49's `skills/gate-check/SKILL.md` registration + traceability matrix). `grep -rn 'gate-check/probes/' plugins/dev-process-toolkit/` for consumer list.
- **AC.8** — single frontmatter line per skill, copied verbatim from `/setup`'s existing entry.
- **AC.9** — single Edit replacing L72-78.
- **AC.10** — `git mv` + frontmatter edit + new README.md write.

Spec-research seed (STE-230 AC-STE-230.6): `spec_research_invoked` — 3 precedents returned (STE-228 ALLOWED_TYPES source + probe-#49 commit gate, STE-167 per-kind release-config validator, STE-308 NON_COMMIT_PRODUCING_SKILLS allowlist wording template).

Deps-research vacuous (`specs/deps.yaml` absent — zero entries → fork skipped per /spec-write § 0b step 2.5b).

## Testing

Per-AC unit test additions (AC.2, AC.3, AC.5). Existing tests touched by the moves (probe #49) re-asserted GREEN with updated import paths.

Manual reviewer pass on the doc/cosmetic items (AC.6 + AC.7 + AC.9 + AC.10 README) — verify each reads coherently end-to-end per STE-161 AC-STE-161.4 rubric.

End-to-end: `bun test` GREEN unchanged; `/gate-check` PASS on `main` post-bundle (probe #57 + #58 + relocated #49 all pass).

No `claude -p` regression coverage — pure mechanical edits with no LLM-decision path.

## Notes

- Spec-research seed: `spec_research_invoked` — 3 precedents returned (STE-228, STE-167, STE-308).
- Deps-research vacuous (`specs/deps.yaml` absent — zero entries → fork skipped).
- This FR brings M84 to 10 FRs total, closing all 30 problem areas from the 2026-05-20 review. No deferred items remain from that source.
- AC.2 expansion follows STE-228's documented contract: ALLOWED_TYPES is the single source of truth (STE-228 §AC-1); downstream gates consume `branch_proposal.ts` re-exports, no duplication. The expanded list mirrors the commit-msg hook accept set (`templates/git-hooks/commit-msg.sh:56`) minus `ci:` (TRUNK_OK_TYPES carve-out).
- AC.10 establishes the first `specs/notes/archive/` convention. Future smoke / conformance traces follow the same pattern: write to `specs/notes/<slug>.md` while live, move to `archive/` once superseded by the next dated trace.
- Out of scope: extending the bumpYaml validator to support nested paths. AC.3 rejects dotted paths in `kind: yaml` to surface the limitation cleanly; full nested-path support is a separate FR.
- Origin: 2026-05-20 review, problem areas A2 LOW + A18 + A19 + A20 + A25 + A26 + A27 + A28 + A29 + A30 (the 10 LOW polish residuals).
