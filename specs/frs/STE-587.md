---
title: A two-root fixture, and a written acceptance of the smoke-leg gap
milestone: M_8f07e0
status: active
archived_at: null
tracker:
  linear: STE-587
created_at: 2026-09-10T08:27:24.546Z
changelog_category: Added
---

# A two-root fixture, and a written acceptance of the smoke-leg gap

## Summary

Cross-repository behaviour has no shared test shape, so each consumer would invent its own two-directory setup and test the same thing three ways.
One fixture now builds two real repository roots with plans and bound work.
A five-state test proves each state gives a different answer.
No conformance leg drives two roots; the plan records that gap in writing, with the command that measures what closing it would cost.

## Requirement

A shared test helper must build two real roots, a falsifiable five-state test must run over the ship-ready predicate on them, and the milestone plan must carry a dated, re-derivable written acceptance that no smoke leg covers cross-repository behaviour.

## Acceptance Criteria

- AC-STE-587.1: `makeSpanFixture("M_GF_78")` from `tests/_span_fixture.ts` yields roots `a` and `b` that are both directories, differ after `realpathSync`, and each contain `specs/plan/` and `specs/frs/archive/`.
- AC-STE-587.2: Five states give five answers on `shipReadyMilestones(a)`: sibling-busy returns `[]`; sibling-clear returns `["M_GF_78"]`; sibling-unlocatable returns `["M_GF_78"]` plus a `sibling-unlocatable` note absent from the sibling-clear notes; sibling-undeclared returns `["M_GF_78"]` with no span note; a malformed declaration rejects with `SpansReposError`. The same test asserts that the sibling-busy answer differs from the sibling-clear answer and that the sibling-unlocatable notes differ from the sibling-clear notes.
- AC-STE-587.3: After `cleanup()`, `existsSync(a)` and `existsSync(b)` are both false, including when the test body throws before cleanup is reached.
- AC-STE-587.4: One home: `grep -rln 'makeSpanFixture' --include='*.test.ts' tests/` returns at least 3 files.
- AC-STE-587.5: The leg roster is unmoved: `SMOKE_LEGS` is exactly `["linear", "jira", "none"]`.
- AC-STE-587.6: The acceptance exists and is re-derivable. The milestone plan, read at either `specs/plan/` or `specs/plan/archive/`, contains the heading `### Accepted gaps`, the literal `SMOKE_LEGS`, a date matching `\b20\d{2}-\d{2}-\d{2}\b`, the phrase `byte-identically green`, and the full `grep -rlE` command string with its measured output stated on the same line. No test pins the bare number.
- AC-STE-587.7: The acceptance is honest about itself: the same section contains the phrase `not a substitute`.
- AC-STE-587.8: No new probe, leg or key: `grep -cE '^[0-9]+\. \*\*' skills/gate-check/SKILL.md` returns 85, `CANONICAL_CAPABILITY_KEYS.length` is 45, and `SMOKE_LEGS.length` is 3.
- AC-STE-587.9: The full `bun test` from `plugins/dev-process-toolkit` reports zero failures with the skip count unchanged from the 15 measured at v2.81.0.

## Technical Design

The helper sits beside the other shared test helpers as a leading-underscore module, so it is never collected as a suite. Two `mkdtempSync` roots each get `specs/plan/` and `specs/frs/archive/`; `planA` and `planB` write tracker-mode plans with no `id:` key. Real directories matter because `defaultRepoProbe().locate` requires `existsSync` and `isDirectory`. Teardown runs both `rmSync` calls in a `finally`. Every root comparison goes through `realpathSync`, since macOS `mkdtempSync` returns a `/var/…` path that resolves to `/private/var/…`. The acceptance test reads the plan from both the live and the archive path, so archiving the milestone cannot red it.

## Testing

The helper is created with the spanning-reader suite as its first consumer and graded here.

## Notes

A test asserting that the acceptance text exists is a prose pin, not coverage, and the plan says so in its own words.
