---
title: Audit every fixture group's leg roster and resolve the setup 7c contradiction
milestone: M121
status: active
archived_at: null
tracker:
  linear: STE-449
created_at: 2026-08-07T13:38:20Z
changelog_category: Changed
---

# Audit every fixture group's leg roster and resolve the setup 7c contradiction

## Summary

Seven of the eight runtime fixture groups are tracker-agnostic, and only one carries an explicit exemption. If the tracker-less leg simply inherits that default, its runtime-check phase re-proves what the linear leg already proves and adds coverage of nothing.

So before any new group is written, every group's leg roster gets re-derived from what that group actually asserts. The exercise is expected to produce findings rather than merely confirm the status quo.

One case is already known to be live. The setup skill contains two statements about branch templates under tracker-less mode that cannot both hold, and they contradict each other independently of anything the smoke driver does. The contradiction sharpens under this milestone because a tracker-less milestone identity is an opaque minted token with no sequential number for the template to substitute.

Resolving it decides whether one group is legitimately not applicable on the tracker-less leg or is a genuine gap. Those are different verdicts with different meanings, and the vocabulary to express both already exists.

## Requirement

Re-derive the leg roster of all eight canonical fixture groups from what each group actually asserts, and record a rationale per group. Resolve the setup step 7c contradiction, since one group's roster depends on the answer.

The contradiction, verified in-session:

- `plugins/dev-process-toolkit/skills/setup/SKILL.md:190` calls `{type}/m{N}-{slug}` "the single seeded default in both tracker mode and `mode: none`".
- `plugins/dev-process-toolkit/skills/setup/SKILL.md:194` says `mode: none` projects that elected option 1 in step 7b "skip writing; branch automation stays disabled."

Those two lines contradict each other before the smoke driver's pre-baked answers block is considered. Separately, the driver bakes a branch-template answer unconditionally, and under a minted milestone identity the `{N}` placeholder has no referent.

## Acceptance Criteria

- AC-STE-449.1: Each of the eight canonical fixture groups in `CANONICAL_FIXTURE_GROUPS` carries an explicit `legs` roster re-derived from what that group actually asserts, with a one-line recorded rationale per group. {#AC-STE-449.1}
- AC-STE-449.2: The setup step 7c contradiction between lines 190 and 194 is resolved with a stated winner, and the losing statement is corrected rather than left standing. {#AC-STE-449.2}
- AC-STE-449.3: Fixture group 5's outcome on the `none` leg is assigned per AC-STE-449.2's resolution using only the existing `FIXTURE_GROUP_OUTCOMES` vocabulary — `not-applicable` when branch automation is off under tracker-less mode, `not-reached` only if it genuinely did not run. No new outcome token is introduced. {#AC-STE-449.3}
- AC-STE-449.4: Audit findings that fall outside this milestone's scope are recorded in `specs/notes/follow-ups.md` rather than silently absorbed or dropped. {#AC-STE-449.4}

## Technical Design

The audit is per-group and mechanical: read what the group's runtime-check line asserts, determine whether that assertion has meaning under each registered leg, and record the answer. Group 2 is the existing precedent — it is linear-only because the probe it tests is vacuous elsewhere.

Group 5 is the group in question. Its subject is the branch-gate marker contract, so its applicability under tracker-less mode turns entirely on AC.2's resolution. If branch automation is disabled there, the group is not applicable on that leg and saying so is accurate. If the seeded default does apply, the group runs and the template's missing placeholder becomes its own finding.

The distinction between the two outcome tokens is load-bearing and already modeled in the code: collapsing them would either report a phantom gap on every tracker-less run or hide a real gap behind a by-design exemption.

## Testing

Roster assertions live in the existing fixture-group module tests. The audit's own output is the recorded rationale per group, which a meta-test can assert is present and non-empty for all eight.

Any correction made under AC.2 needs its own assertion so the resolved statement cannot silently revert.

Full gate green from the plugin root.

## Notes

Gates STE-450 and STE-451. Neither new group should be written before this audit lands, because the audit may change what those groups need to assert about rosters.

This FR is expected to produce findings. Anything it turns up in the same class as the setup contradiction is a real result of the milestone, not a distraction from it.

**The 7c resolution is forced, not chosen, and carries no behavior change.** `branch_template` is a Schema L key and Schema L is the `## Task Tracking` section; step 7b confines the canonical key set to that section and instructs tracker-less projects to emit no such section at all. Gate probe #21 goes vacuous when the section is absent and probe #10 documents the backward-compat reading as absent-means-disabled. So there is nowhere for the key to be written under tracker-less mode, line 194 is the only reachable behavior, and line 190's "in both tracker mode and `mode: none`" is the false clause. What `/setup` writes does not change for any project in any mode. See `specs/plan/M121.md` § Blast radius.

If implementation finds this analysis wrong — that some path does write the key under tracker-less mode — that is a behavior change to a shipped skill. Stop and surface it rather than absorbing it into a prose fix.

> **The two paragraphs above are the FR AS AUTHORED and are left unedited as spec history — but one clause in them is now known FALSE. Read `## Implementation notes` before relying on either.** The write analysis held and the stop-and-surface trigger did not fire: no path writes the key. What did not hold is the inference that "line 194 is the only reachable behavior" — 194's own consequence clause, *branch automation stays disabled*, is false for the STE-228 universal pre-commit gate, which never reads Schema L. **Both** lines carried a false clause and both were corrected. The `{N}`-has-no-referent premise in § Requirement is likewise refuted by execution.

This FR modifies a SHIPPED plugin skill, not a project-local one. That does not change the milestone's `migration: none` verdict, which rests on the absence of convention changes rather than on the work being repo-local.

A second documentation contradiction is already known and is explicitly NOT in this FR's scope: `docs/layout-reference.md:108` lists the tracker key among mode-invariant schema keys, contradicting `specs/technical-spec.md:260` and the gate-check skill. The identity probe enforces the technical-spec reading. Record it; do not let it block.

## Implementation notes

Baseline owned before any edit: **6768 pass / 15 skip / 0 fail** at `e54cdde`, clean tree. Final: **6786 / 15 / 0**.

### The headline result — the pre-analysis was half wrong, and the wrong half was the surviving line

This FR's `## Notes` and `specs/plan/M121.md` § Blast radius both pre-concluded that line 194 was the truth and line 190 the sole false clause. **Half of that is right, and BOTH lines carried a false clause.**

- **Right, and re-verified:** Schema L *is* the `## Task Tracking` section; a tracker-less project emits none; there is nowhere to write `branch_template:`. The escalation trigger the plan set — "if some path DOES write the key under tracker-less mode, STOP and surface it" — was checked against every consumer and **did not fire**. **What `/setup` writes is byte-identical in every mode.**
- **Wrong:** 194's consequence clause *"branch automation stays disabled"*. Two mechanisms read the key and only one is gated on it. `/implement` § 0.b″'s branch PROPOSAL is Schema-L-gated ("Absent `branch_template:` ⇒ skip entirely") and genuinely is disabled. The **universal pre-commit branch gate** (`requireCommittableBranch`) is a different mechanism — `docs/patterns.md` § Pattern 28 says outright *"The gate has no tracker awareness"* — and `/spec-write` § 7a calls it unconditionally, taking its name from `canonicalBranchTemplate({ milestone })` in CODE, never from CLAUDE.md.

**The milestone's stated REASON for expecting 194 to win is refuted by EXECUTION.** The claim was that a minted `M_<TAIL>` identity leaves `{N}` with no referent. It has one: `M_<tail>` parses as an *epic* token under the `milestone_token.ts` union grammar, so `milestoneBranchToken` renders it in sanitizer form. Run, not reasoned — `mintMilestoneId` → `M_WV047W` → `canonicalBranchTemplate` → `{type}/m{N}-{slug}` → `buildBranchProposal` → **`feat/m_wv047w-tracker-less-thing`**. Both arms are now standing tests.

**Behaviour-change question, answered explicitly because the FR demanded it not be left ambiguous: NO.** `/setup`'s write path does not change for any project in any mode, so no `/upgrade` migration is implied. It is a prose correction — but it retires a false *documented claim* about a shipped skill's downstream behaviour, so it earns its own changelog line rather than folding in as a tidy-up.

### The audit moved a roster, and it was not the one the FR expected

Group 5 was the group under suspicion and it **keeps** its full roster: its SUT is the unconditional gate, so the marker contract fires identically with no tracker configured. AC.3 offers `not-applicable` "when branch automation is off under tracker-less mode" — it is not off — and `not-reached` "only if it genuinely did not run". **Neither exemption applies, and that is the assignment.** No token was invented.

The group that moved is **group 4**, for an unrelated reason: it is constituted as exactly two tracker-parameterized sub-fixtures (4a Linear / 4b Jira) whose step 4 asserts a tracker ticket reaching `Done`. It had no tracker-less instance yet rostered the leg. **MEASURED before the change: a tracker-less run rendered `STE-227 runtime check: NOT-REACHED`, aggregate FAIL, rc 1 — that leg could never report a fixture-group pass however well it ran.** The two exemptions are declared separately because they differ in kind: group 2's is by CAPABILITY (permanent — probe #26 does not exist off Linear), group 4's is by FIXTURE CONSTITUTION (retired the moment a sub-fixture is written). Group 4's genuine coverage gap is recorded at follow-ups § 0f(a); do not read its `N/A` as coverage.

### An assertion that had been blind for a whole milestone

`m117-ste-425`'s heading test asserted `heading matches /Linear \+ Jira/` for every non-Linear-only group. STE-446 widened all seven of those rosters to three legs and **every heading kept announcing two** — the substring still matched, so the pin stayed green through an entire milestone while the thing it guarded was stale. It was scanning for a shape the content no longer took. Rewritten to DERIVE the expected annotation from the roster, so a group gaining or losing a leg without a heading update is RED in both directions; the six stale headings are corrected.

Its sibling ("group 2 is the only Linear-only group, and all the rest have three legs") was a two-bucket check that could not distinguish a group AUDITED onto every leg from one that had merely inherited the alias. Replaced with the full eight-row table, hand-restated.

### Falsifiability — fourteen mutations, fourteen RED, every restore checksum-verified

Each ran with absolute paths on both sides, an explicit assert-the-mutation-applied step, and `cmp` after restore (follow-ups § 0b).

| # | Mutation | Result |
|---|---|---|
| 1 | group 4 roster back to `ALL_LEGS` | 4 RED across two files |
| 2 | group 5 heading back to `(Linear + Jira)` | 1 RED |
| 3 | blank one group's `rationale` | 1 RED |
| 4 | duplicate one `rationale` onto another group | 1 RED |
| 5 | restore "branch automation stays disabled" on the write bullet | 1 RED |
| 6 | restore "in both tracker mode and `mode: none`" on line 190 | 1 RED |
| 7 | break epic handling in `milestoneBranchToken` (the EXECUTED arm) | 1 RED |
| 8 | restore the seeded-in-both-modes claim in the delegated long form | 1 RED |
| 9 | restore "Branch automation stays disabled" in the long form | 1 RED |
| 10 | restore the false consumer-scope sentence | 1 RED |
| 11 | revert group 5's BODY to the two-leg roster + "4 fixture instances" | 1 RED |
| 12 | revert group 6's BODY to "(Linear + Jira, runs on both legs)" | 1 RED |
| 13 | delete one pattern from the stale-roster matcher (blinding it) | 1 RED — caught by the anchor test |
| 14 | rename the § Consumer scope heading (the REPAIRED non-vacuity guard) | 1 RED — the pre-repair guard passed this |

### Correction tally, carried forward — the milestone is now at TWENTY

STE-448 left it at fifteen. This FR produced **five** — three found by me, two by the AUDIT stage after I had reported the work complete. That is the **third consecutive FR** in which AUDIT found real vacuity post-completion, and the second in which the finding was in the implementer's own new test.

16. **I verified a shipped cap against the wrong oracle.** My SKILL prose breached both zero-headroom budgets (the `setup/SKILL.md` line cap and the `skills/` STE-token ceiling). I compressed, measured with `wc -l`, got 358 against a cap of 358, and declared it fixed — but the assertion counts `split("\n").length`, which is one HIGHER on a trailing newline. The gate caught it, not me. Exactly the class of error this milestone is about, committed while fixing a different one: I checked the property with a tool that answers a subtly different question from the one the test asks. Neither cap was weakened — the paragraph was folded into an existing bullet for net-zero lines, and both STE tokens were dropped from shipped prose (the M111 lesson).
17. **My own new test could not fail, and it was the one guarding AC.3's "no new token" constraint.** It collected the outcomes `reconcileFixtureGroups` actually emitted and asserted each was one of the four — but that function's return type IS the four, so it re-checked a guarantee the type system already gives. Adding a fifth token, the precise thing the AC forbids, would have left it green: a token nobody emits never appears in the collected set. Found by re-reading my own additions against the milestone thesis before reporting, not by the gate. Re-pointed at the exported vocabulary, restated by hand.

18. **I RELOCATED the contradiction instead of resolving it, and the gate was green at 6781/0 while I had.** I corrected `skills/setup/SKILL.md` step 7c and stopped — but that file's own closing line delegates the long form to `docs/setup-tracker-mode.md` § Branch template, which carried BOTH retired claims verbatim (`"seeded in both tracker mode and mode: none"`, `"Branch automation stays disabled."`) plus a third I had not looked for: a § Consumer scope sentence listing `/spec-write` and `/spec-archive` among skills that "continue to run on whatever branch they're invoked from", which the gate demonstrably disproves by moving `HEAD`. A reader following the skill's own pointer would have landed on the text I had just retired. **My AC.2 assertions were scoped to one file, so they could not see it** — the pin was correct and its scope was wrong, which is Pattern 31 read from the other end. Found by an adversarial pass whose brief was to REFUTE my conclusion, not by the suite. The delegated long form is now corrected and pinned with its own three assertions, all three mutated RED; the three remaining surfaces are genuinely separate documents and are recorded at follow-ups § 0f(e) with the scope line stated.

19. **My heading repair stopped at the heading LINE — found by AUDIT.** The whole point of correction-class #18's neighbour was that six group headings had gone stale after STE-446 widened the rosters. I fixed the headings and the **body prose immediately beneath four of them went on asserting the retired two-leg roster**, pinned by nothing. Group 5 — the group this FR is *about* — even carried the arithmetic: *"4 fixture instances per smoke run"*, when two sub-fixtures across three legs is six. My new heading assertion reads `split("\n")[0]`, so it could not see any of it. **I diagnosed a stale-after-widening defect and then reproduced it one line lower in the same file.** Bodies repaired and pinned by a new body-scoped assertion — which, when first written as a bare `both legs` word ban, flagged **two more** the audit had not listed and both were legitimate (group 8's dated *"MEASURED on both legs of 2026-07-27"* is history, group 1's is the recorded § 0f(d) symmetry claim). Narrowed to the operative present-tense roster shape per Pattern 31, and given an anchor test so narrowing cannot blind it (mutation 13 proves the anchor bites).

**This is a DISTINCT failure shape from a blind assertion, and worth naming separately.** #17 and #20 are guards that could never fail — they were wrong from the first character. This one is a *fix whose scope stopped one line short of the defect it was fixing*. The assertion is sound, the repair is real, and the diagnosis was correct; only the reach was wrong. That makes it far more dangerous on review than a blind guard, because **it reads as fixed**: the commit shows the right defect named, the right mechanism identified, and a genuine correction applied, so a reviewer's eye stops at the heading and never travels to the line beneath it. A blind assertion at least looks suspicious when read closely. A short-scoped fix looks finished. The general lesson: when a defect class is "X went stale after Y widened", the repair must be scoped to the CLASS, not to the instance that surfaced it — enumerate every place the stale statement could live before fixing the one you found.
20. **A second can't-fail non-vacuity guard in my own test — found by AUDIT.** `const consumerScope = section.slice(section.indexOf("**Consumer scope"))` then `expect(length).toBeGreaterThan(0)`. On a renamed heading `indexOf` returns `-1`, `slice(-1)` yields the section's **last character**, and length 1 passes. The guard proved nothing. Same shape as #17 and in the same file: I wrote two guards that could not fail while writing an FR whose thesis is that such guards are the defect. Repaired to assert the index; mutation 14 confirms it now bites where it previously would not have.

**The pattern-31 collision did NOT fire this time, and only because it was designed around.** My corrections QUOTE the false clauses they retire, so a whole-file `not.toContain("branch automation stays disabled")` would have fired on the paragraph explaining the fix — the eleventh instance. Every ban in the new test is scoped to the operative instruction line and paired with a positive assertion, since "the bad string is gone" is also satisfied by deleting the rule. First time in this milestone the trap was anticipated rather than hit.

### Recorded, not fixed

Five findings at `specs/notes/follow-ups.md` § 0f: group 4's real tracker-less coverage gap, probe #10's description repeating the same false absent-⇒-disabled reading, the `layout-reference.md` ↔ `technical-spec.md` `tracker`-key contradiction this FR named as out of scope, sub-fixture 1b's two-leg cross-tracker symmetry claim with no third-leg form, and the three further surfaces still carrying the retired claims, with the scope line stated.
